# AGENTS.md

本文件只保存仓库级长期规则。默认使用中文沟通；Issue、Pull Request、Commit、
报告、前端文案和自动化评论必须使用中文。代码标识、数据库字段和第三方错误可以
保留英文，但面向用户时应补充中文解释。

## 按任务读取

所有任务先读 `CONTEXT.md`，并按工作范围补充读取：

- 架构或领域变更：`docs/adr/`、`docs/agents/domain.md` 和
  `docs/architecture/code-map.md`。
- Issue 驱动任务：目标 Issue、父 Issue、原生阻塞关系、验收条件，以及
  `docs/agents/issue-tracker.md`。
- 具体策略研究、分析、评估、回测或比较：完整阅读
  `docs/research/contracts/strategy-evaluation-standard.md`。
- 生产部署、数据库迁移、受控切换或新电脑访问：
  `docs/operations/production-deployment-and-home-access.md`。
- 页面、组件、布局或视觉调整：`.codex/skills/frontend-design/SKILL.md`。
- 验证方法：`docs/agents/validation.md`。
- 其他文档入口：`docs/index.md`。

易变化的生产提交、表行数、磁盘余量、运行 ID、Issue 状态和部署状态必须现场
核验，不能只引用历史记录。

## 产品边界

本仓库是以美股为主的个人投研工作台与离线量化研究系统，不是真实交易系统。

允许按显式来源授权同步、持久化、质检和展示美股实际市场数据，也允许使用 point-in-time、历史
universe、复权和下一交易日执行口径开展离线研究。

禁止：

- 连接真实券商、交易账户或资金账户。
- 自动下单、撤单、调仓、融资融券、申购赎回或执行任何真实资金动作。
- 导入真实持仓、真实成交或券商导出，除非用户先批准新的数据治理方案。
- 把数据、sample、研究结论或观察池写成买入、卖出、持有、评级、收益承诺或
  实盘指令。

## 工作规则

- GitHub Issues 是唯一活动路线图；`TODO.md` 和 dated 计划不得承担当前状态。
- 只有自动领取 Issue 时，才要求其无未完成阻塞、带有 `可由智能体处理` 标签，
  并在开始前分配给执行者。
- 用户直接提出的小型修改、排查或咨询不要求先创建 Issue；若工作扩展为独立工程
  任务、正式研究或生产变更，应转入对应 Issue。
- `需人工处理`、`待补充信息`、正式研究、生产数据库变更和不可逆操作需要明确
  人工授权。当前请求中明确、具体的授权有效，无须重复询问；范围不清时必须停下。
- Issue 驱动的代码任务使用独立分支或 worktree，执行与风险匹配的验证并创建中文
  Pull Request，不得自动合并。用户明确指定的临时或本地小改动可直接在当前工作区
  完成。
- 只修改当前目标所需内容；保留用户既有修改，不使用 `git add -A` 混入无关文件，
  不顺手重构。
- 所有新标签使用中文。

## 正式研究治理

- 系统可以创建和排序研究提案，但只有 GitHub 用户 `Jettlin927` 对冻结计划发布
  `批准研究 <plan_sha256>` 后才能启动正式研究。标签不能代替批准。
- 假设、规则、范围、期间、基准、成本、门禁、参数空间、试验预算或停止条件变化
  后，原批准失效，必须创建新版本并重新批准。
- 正式研究只执行已合并、CI 通过并静态登记的策略代码；自动化不得自行合并策略、
  公式、schema 或编排代码。
- 自动优化必须限制在冻结的搜索空间、目标函数、试验预算和停止规则内，不得根据
  同一 OOS 临时扩张搜索空间。
- 研究运行、策略生命周期、研究评价和研究结论分别建模；运行成功不等于研究通过。
- 研究结论只允许：`研究通过`、`有条件候选`、`证据不足`、`受阻`、`不通过`。
- 所有终态都要发布，包括失败运行；已发布研究不得原地覆盖，修正必须创建替代版本。
- 新增市场数据不得自动覆盖旧结论；监测只能形成观察或新的未批准提案。

## 策略研究最低门槛

具体交付顺序和完整指标以策略评价规范为准。以下红线不得省略：

- 必须说明策略画像、经济假设、适用条件、失效条件和可复现身份。
- 必须使用 point-in-time 数据、历史 universe、明确执行时点、净成本、test/OOS、
  匹配基准、风险、交易、容量和市场环境证据。
- 必须登记试验，并检查 walk-forward、参数邻域和成本压力；多次筛选时增加
  DSR/PBO。
- 必须同时给出支持证据、反对证据、尚缺证据和限制项。
- 缺少匹配基准、净成本、test/OOS、市场环境覆盖、关键可交易性、试验登记或复现
  身份时，不得输出 `研究通过`。
- 用户报告优先使用中文方案名称；确需保留编号时，先给出“编号—名称—具体规则”
  对照，后文使用“名称（编号）”。
- 研究期首若存在上一信号日触发的首个执行日，收益、基准、成本和回撤必须从显式
  初始净值计算，不得用首个收盘净值重新归一化而漏掉首日收益或费用。

## 持久化与代码边界

- PostgreSQL 保存结构化计划、运行、事件、评价、证据引用、结论和发布状态。
- 工件目录保存冻结输入、运行清单、账本、复现证据、机器摘要和原始 HTML；大型
  工件不得提交进 Git。
- GitHub Issue 保存冻结计划、批准、可读进度和最终中文结论；API 与前端只提供
  投影。数据库、工件、Issue、API 和前端读回同一评价版本与指纹后才算发布完成。
- `status=ok` 只能表示源执行成功，不能映射为 `研究通过`。
- 长同步和正式研究不得在 API 进程内执行；数据任务与研究执行必须保持职责隔离。
- `backend/app/models.py` 是应用 schema 合同；生产演进必须使用
  `backend/migrations/` 中的 Alembic revision。
- `backend/app/quant_research/` 负责研究协议、模拟、指标、复现和报告统计口径；
  渲染脚本只组装 canonical 工件，不得复制成交、成本或绩效公式。
- `frontend/` 是只读产品界面，不承担研究计算或批准逻辑。
- 已退役的 A 股、`us_experiment_*` 和 `my_quant` 路径不得恢复为运行时入口；其
  Alembic 历史表和历史研究证据只用于迁移兼容、审计与 schema-only 测试。
- 不得恢复 `strategy_research`、`backtest-reports`、`strategy-lab`、
  `research_engine` 等旧主线目录。

## 数据与安全红线

- PostgreSQL 和研究工件是持久资产。未经明确授权，禁止执行
  `docker compose down -v`、`docker volume rm`、覆盖恢复、删除 canonical 工件或
  清理生产服务器或其他未知来源的持久数据。
- 不得把 `.env`、Tushare token、数据库密码、SSH 私钥、GitHub token、真实账户
  数据或其他凭据写入源码、Issue、前端、日志、README、测试或工件。
- API 数值必须 JSON-safe；`NaN`、`Infinity` 转为 `null` 或明确兜底。
- 退役市场表继续保持既有 migration 身份和自然键；不得为了清理运行时代码改写
  Alembic 历史。调整 schema 前必须检查模型、migration 和 PostgreSQL 集成测试。
- 美股实际市场数据、合成测试夹具、个人手工持仓和正式研究证据必须在 schema、
  API、前端和研究门禁中明确隔离。

## 前端边界

- 视觉方向是高信息密度、克制、网格化的工业数据终端，不做营销落地页。
- 必须显著区分实际市场数据、sample、运行状态、研究结论和系统健康。
- 不得出现实盘、账户、下单、买卖评级或收益承诺入口。
- 原始 HTML 只是证据入口，不能代替结构化研究界面。

## 生产门禁

- `quant-trading-prod` 是唯一生产服务器和唯一数据权威；原旧服务器已经退出系统，
  不承担生产、灾备、回滚、Staging 或计算角色。
- 服务器连接使用本机 SSH config 与 `.env`；私钥路径和 IP 不写入长期文档。
- 数据库、API 和前端端口默认只监听 `127.0.0.1`，PostgreSQL 不开放公网。
- 当前远程访问只使用 SSH 隧道；除非用户明确变更决定，不购买或申请域名，不部署
  Cloudflare Tunnel、Cloudflare Access 或 Tailscale Serve，也不开放公网 IP 端口。
- 新电脑上的智能体必须先按生产访问文档现场核验目标主机、精确 `main` 提交、SSH
  config、受保护的 `.env` 和当次授权；旧文档、Issue 或历史记录不构成持续授权。
- 生产 Alembic upgrade、baseline stamp、`DROP INDEX`、覆盖恢复、生产切换、持久
  数据清理和凭据变更必须获得针对精确操作的明确授权。
- 未来若迁移生产主机，必须包含停写、备份恢复、工件同步、schema/行数/日期/指纹
  读回、API/前端验收和独立回滚方案；不得默认恢复已退役旧服务器。
- 代码合并、CI 通过、镜像构建、容器启动、生产读回和研究结论是不同事实，报告时
  必须分开。

## 历史证据

- 代码与文档变更以 Git 提交和 Pull Request 为准。
- 活动任务、决策、人工批准与研究结论以 GitHub Issue 为准。
- CI、部署和正式研究运行以自动化记录、数据库事实及冻结工件为准。
- 不重复维护仓库内流水日志；不可逆操作和生产门禁必须在对应 Issue 或 Pull
  Request 中留下可审计记录。
