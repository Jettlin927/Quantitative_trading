# AGENTS.md

本文件是本仓库的“项目主管规则”。在上下文窗口有限、模型无状态的前提下，它替代人类主管：消除歧义成本、约束决策空间、传递代码里看不出的隐性知识。

用户常用语言是中文。除非用户明确切换语言，说明、计划、提交说明和交互文案默认使用中文。

当需要库/API 文档、代码生成、设置或配置步骤时，默认使用 Context7 MCP 查询当前文档，不需要用户额外提醒。

详细代码导航看 `docs/agent-code-map.md`。`AGENTS.md` 只保留默认决策、红线和流程约束，不要写成 README 或模块索引。

后续 Agent 接手时，先读 `docs/agent-handoff.md`。它记录当前 PR/分支状态、推荐验证命令、活跃研究阶段、常见本机坑点和不要混入本仓的外部路径；`AGENTS.md` 仍然是最高优先级项目规则。

## 项目目标

这是一个本地 A 股量化研究工作台。它既要给人类提供 Web 工作台，也要给 AI Agent 提供可理解、可验证、可复盘的研究工具链。

长期目标是让 AI 按接近科学研究的流程自动迭代策略：

1. 提出假设：说明策略想利用的市场结构、适用边界和失败条件。
2. 编码验证：用最小代码实现假设，不混入无关重构。
3. 回测评估：用 PostgreSQL 本地数据跑单票、标的池或全市场验证。
4. 复盘反思：记录证据、指标、失败样本和下一步假设。

当前系统仍是研究和复盘工具，不是生产交易系统。不要自动下单，不要连接真实券商或账户，不要处理真实资金。任何涉及真实交易、账户、券商接口、资金变动的功能都必须先停下来问用户。

## 用户投资画像与跨市场研究口径

用户画像不是保守型，而是进攻型主线趋势投资者：市场主线清楚时需要足够进攻性，市场主线不清时需要主动收手。评估策略时不能只问“够不够稳”，还要问“主线明确时够不够锋利，主线模糊时够不够克制”。

必须区分用户真实风险偏好和当前 A 股策略原型。某个 A 股策略即使仓位上限、最大持仓数和回撤控制看起来克制，也不代表用户偏好保守；它可能只是服务于进攻型目标的风险控制外壳。后续分析要同时评价收益弹性、上涨捕获、下跌防守、主线失效退出速度、最长回撤持续和资金效率。

A 股研究口径：不要假设境内 A 股有和美股一样可直接使用的 2x 个股 ETF 工具箱。A 股策略的任务是用市场宽度、主线行业识别、横截面排序、小组合集中度、止损/冷却/风险过滤，在工具约束下尽量实现“主线清楚时进攻、主线不清时收手”。默认基准应包含宽基指数、Risk-On 持有指数/Risk-Off 现金、候选池随机选股、简单动量排序、主线行业 ETF 或行业篮子。

美股研究口径：美股操作层仍只保存 sample、脱敏结构和研究辅助报告，不提交真实持仓或券商导出。在研究用户主线进攻能力时，可以把“Risk-On 持有 2x 杠杆 ETF、Risk-Off 持有现金”作为对照基准，但必须模拟日度重置、复利路径依赖、波动拖累、滑点/费用、最大回撤、最长回撤和主线失效退出速度。任何真实下单、账户读取、仓位同步或杠杆执行都属于灰区，必须先问用户。

## 默认架构

默认架构是三容器本地系统：

- `frontend`：React + Vite，目录 `frontend/`，宿主机端口默认 `15173`，容器内监听 `5173`。
- `api`：FastAPI + SQLAlchemy 2.0，目录 `backend/`，宿主机端口默认 `18000`，容器内监听 `8000`。
- `db`：PostgreSQL 16，使用 Docker volume 持久化本地行情、基本面、同步记录和研究池。

`docker-compose.yml` 是启动入口。不要把项目退回到单文件静态 HTML 方案，除非用户明确要求。

后端是策略和数据语义的准绳，前端只负责提交参数和展示结果。不要让前后端各维护一套分叉的策略逻辑。

## 四区职责

后续改造按四区推进：

- 美股操作层：默认落在 `my_quant/us_research/`，只保存 sample、脱敏结构、观察池配置、数据快照和研究辅助报告；不要提交真实持仓、成交明细或券商导出。
- A 股数据沙盘：默认仍由 `backend/`、`docker-compose.yml` 和 PostgreSQL volume 承载，Tushare 数据用于大样本验证；不要为了美股第一版改 A 股表结构或删除 volume。
- 策略思想库：默认落在 `docs/research/strategy-lab/`，每条规则必须写清假设、A 股验证口径、美股映射边界、失败条件和负证据。
- 回测证据档案：默认落在 `docs/research/backtest-reports/`、`my_quant/strategy_research/results/` 和 `my_quant/strategy_research/web_report/`，指标应来自已落盘 CSV/JSON/manifest 或数据库结果。

Phase 0 只立边界和盘点，不搬大文件、不迁移真实持仓、不改 Docker DB、不改变回测语义。

## AI 研究闭环规则

新增或修改策略时，先写清楚假设和成功标准，再动代码。例：`假设：BOLL 收口后放量突破在当前样本中提高胜率；验证：同一日期区间全市场回测，比较收益、最大回撤、胜率、纪律评分和交易数。`

截至 2026-06-27，旧 Risk8、B1、RAM、Kronos 等历史策略已全部退出当前主线，生命周期状态统一为 `legacy_reset`。它们只能作为历史材料和负证据，不得再被前端、后端聚合 API 或新研究报告表述为 active、frozen、观察候选、当前 baseline 或“已有可参考策略”。

从 `001-research-reset` 起，新策略必须从零开始：先写市场假设、数据字段、失败条件、三段评估窗口和禁止重复路线，再写回测代码。不要把旧策略改名、换参数、换 UI 包装后当作新策略。

策略实验必须可复现：记录数据区间、候选范围、核心参数、是否调用外部 AI、关键指标和失败样本。阶段性事实写入 `操作日志.md`；更详细的研究报告可放到 `docs/` 下的新文档。

自动化研究只能产生研究建议、代码草案、回测结果和复盘结论。不要把“买入/卖出/评级”表述成确定收益或真实交易指令。

防止数据泄漏：不要在入场逻辑中使用未来行情、未来财报、回测结束后才知道的标签或人工挑出的幸存样本。若数据时间点不清楚，先标注不确定性。

优先建立基线再改策略。修改核心回测语义时，要说明对历史结果可比性的影响；无法保持可比时，应新建实验口径，而不是悄悄覆盖旧含义。

## 阶段性交付汇报规则

每个阶段性任务结束前，除了文字回复，还要主动判断是否需要一个可长期打开、可复盘、可转交的交付物。策略研究、回测复核、backend 接入、阶段验收、参数实验、失败窗口归因和重要前端工作，默认都应提供 HTML 报告；很小的查错、修字、单命令任务可以只在最终回复中说明不生成 HTML 的原因。

HTML 报告默认放在与任务最相关的目录下：策略研究优先放 `my_quant/strategy_research/web_report/`，阶段研究报告可放 `docs/research/` 或对应 stage/session 目录。报告必须尽量由已落盘的 CSV、JSON、manifest 或数据库结果生成，不要手填关键指标；若是一次性 HTML，也要记录输入数据路径和生成命令。

策略或回测类 HTML 报告至少包含：

- 策略描述：说明假设、适用市场、标的池过滤、数据窗口和结论标签。
- 执行规则：写清信号规则、买入规则、卖出规则、仓位规则、风控规则、交易成本、成交假设和不可交易处理。
- 回测情况：展示核心指标、窗口/样本外表现、收益门和回撤门是否通过、失败窗口和失败样本。
- 图表和明细：至少包含净值曲线；如有交易流水，优先包含每笔交易盈亏金额分布、每笔交易盈亏比例分布、闭环交易盈亏明细和原始买卖明细。
- 复现信息：列出输入产物、输出产物、生成命令、验证命令和仍未解决的问题。

报告结论必须与阶段门槛一致，只能写 `阶段通过`、`观察` 或 `淘汰`；不得把研究报告写成实盘买卖建议、收益承诺或可直接执行的交易指令。

## 长期目标与阶段推进

本项目的终极量化目标和完整阶段路线写在 `docs/research/long-term-goal.md`：年化收益率 `>= 100%`、最大回撤绝对值 `< 10%`、已完成交易盈亏比 `>= 3:1`。这是长期北极星目标，不代表当前观察级策略已经接近达成。

策略研究必须按 `docs/research/stages/` 分阶段推进。当前活跃阶段以 `docs/research/stages/README.md` 和对应阶段目录为准；截至 2026-06-27，本轮大重构后的活跃阶段是 `001-research-reset`，目标是在没有旧策略主线的前提下重新建立假设、数据口径和评估流程。

阶段推进规则：

1. `docs/research/long-term-goal.md` 是阶段顺序、阶段名称和硬门槛的唯一来源；不得在 session 内自行改名、插队、重排或跳级。
2. 当前阶段未验收前，只能在当前阶段目录追加证据，不得新建更高阶段目录。
3. 当前阶段通过后，先写当前阶段的 `next-stage.md`，再新建下一个阶段目录。
4. 新阶段必须先写 `README.md`，明确阶段目标、硬门槛、数据窗口、失败条件和禁止重复尝试的负证据。
5. 任何“通过”结论都必须写清楚通过的是哪个阶段，不能笼统说“策略已通过”。
6. `001-research-reset` 未完成前，不得复活旧策略、复用旧参数调优、跳过数据字段定义或把历史胜率当作新策略证据。后续进入新策略验证阶段后，仍不得跳过失败窗口归因、排序因子消融、数据质量和成交现实性检查，直接调参数冲年化 100%。

## 并行 Session 研究规则

当前设计允许多个 Agent/session 并行研究，但只能在活跃阶段内部并行，不能并行改全局结论文件。并行研究的目标是让不同假设互不覆盖、结果可合并，而不是让多个 session 同时抢同一份文档。

并行 session 启动前必须先认领一个独立目录：

```text
docs/research/stages/<stage-id>/sessions/<YYYYMMDD-HHMM>-<short-topic>/
```

每个 session 目录至少包含：

- `session.md`：session id、研究问题、假设、数据窗口、允许写入的文件、计划使用的 run id 前缀、状态。
- `session-log.md`：本 session 的事实日志，替代直接写全局 `操作日志.md`。
- `evidence.md`：本 session 的证据、命令、run id、指标和失败样本。

并行写入边界：

- 允许写入：本 session 目录；唯一 run id 对应的 `docs/research/runs/<run-id>/`；为本 session 新建的临时诊断脚本或输出文件。
- 不允许并行写入：`AGENTS.md`、`docs/research/long-term-goal.md`、`docs/research/stages/README.md`、当前阶段 `README.md`、当前阶段总 `evidence.md`、`docs/research/research-runs.json`、`docs/research/next-strategy-brief.md`、固化策略规格、全局 `操作日志.md`。
- 这些全局文件只能由一个明确的“整合 session”在读取各 session 结果后统一更新。

命名规则：

- 并行 run id 必须带阶段和 session 主题前缀，例如 `001-industry-exposure-001`、`001-scoreparts-001`。
- 不得复用既有 run id，不得覆盖其他 session 的 run 目录。
- 若发现目标 run 目录已存在，必须换新 run id 或停止确认，不能删除或覆盖。

合并规则：

1. 单个并行 session 完成后，只把状态写到自己的 `session.md` 和 `evidence.md`。
2. 整合 session 逐个读取并行 session 的 `evidence.md`，再更新当前阶段总 `evidence.md`、`research-runs.json`、`next-strategy-brief.md` 和 `操作日志.md`。
3. 只有整合 session 可以判断阶段是否通过，并决定是否创建 `next-stage.md` 和下一阶段目录。
4. 并行 session 之间如果结论冲突，不做投票式合并；必须保留冲突证据，并在整合时写清楚数据窗口、参数、样本或口径差异。
5. 如果一个 session 需要改变核心回测语义、成交假设、阶段门槛或共享上下文，必须停止并请求用户确认，不能在并行状态下直接改。

## 量化策略最低合格标准

本项目的“合格研究级量化策略”不是一条好看的回测曲线，而是在明确市场假设下，用当时可获得的数据、可复现的规则和真实交易成本，经过全市场、样本外、滚动窗口和压力测试后，仍能取得正的风险调整收益，并且最大回撤、交易频率、仓位、流动性和失败样本都在预先设定的风控边界内。

收益、回撤和盈亏比硬门槛分两层：

- 阶段门槛：以当前 `docs/research/stages/<stage-id>/README.md` 为准，用于判断是否推进到下一阶段。
- 终极门槛：年化收益 `>= 100%`、最大回撤 `< 10%`、盈亏比 `>= 3:1`，用于判断是否达成长期目标。

任何候选策略至少必须同时过以下 6 道门，缺一项只能标为“观察”或“淘汰”，不能称为合格策略：

1. 研究假设合格：必须说清楚策略赚的是什么钱，例如趋势、反转、波动收缩、质量因子、风险过滤或情绪退潮规避。不能只说“参数回测好看”。
2. 数据与回测合格：必须使用当时可获得的数据，明确处理 ST、退市、停牌、涨跌停、上市不足、复权、财报公告日、未来函数和幸存者偏差。
3. 成本后收益合格：必须满足当前阶段门槛；若要宣称达成终极目标，则必须满足年化收益 `>= 100%`、最大回撤 `< 10%`、盈亏比 `>= 3:1`。收益必须扣除佣金、印花税、过户费、滑点，并按 A 股 100 股一手、T+1 和不可成交场景处理。
4. 风险调整合格：不能只看总收益。最低要输出年化收益、波动率、最大回撤、回撤持续时间、Sharpe、Sortino、Calmar、胜率、平均盈利/亏损、profit factor、交易次数、换手率、资金使用率、收益前 10 和收益后 10。
5. 稳健性合格：至少通过样本内/样本外、滚动窗口、不同年份/牛熊震荡分段、参数敏感性、成本翻倍压力测试、标的池外验证和消融实验。若调了很多参数，必须承认多重测试和过拟合风险。
6. 工程与风控合格：策略必须可复现、可审计、可停止；保存版本、参数、数据区间、过滤规则、回测结果、失败样本和下一步结论。任何进入自动化或实盘前的交易前检查、上线前测试、上线后监控、异常处置和合规风控，必须另行确认。

对进攻型主线策略，还必须补充一组资本效率基准：A 股侧至少比较 Risk-On 指数、候选池随机选股、简单动量和主线行业篮子；美股侧在研究场景中可比较 2x Beta 对照。若策略跑不赢对应基准，必须解释它是否用更低回撤、更短回撤持续、更少尾部风险或更好的退出速度换来了可接受的资本效率。

策略文件或研究报告必须包含：假设、适用市场、标的池过滤、数据区间、信号规则、买入规则、卖出规则、仓位规则、风控规则、交易成本、成交假设、基准指数、回测指标、稳健性测试、失败样本、参数表、版本号和结论。

结论只允许三类：

- `阶段通过`：满足当前阶段目录写明的收益、回撤、盈亏比和稳健性门槛，并说明可进入哪个下一阶段。
- `观察`：收益达标，但样本外、成本压力、滚动窗口、参数稳定性或失败样本证据不足。
- `淘汰`：存在未来函数/幸存者偏差、未扣成本、只在手选样本有效、收益依赖少数极端交易，或最大回撤超过硬线。

旧策略退场结论使用 `legacy_reset`，含义是“用户决定从头开始，旧策略不再参与当前主线”。它不是 `观察`、`候选` 或 `淘汰` 的替代品，也不能被前端重新展示成当前策略。

中国境内程序化交易还要注意证监会《证券市场程序化交易管理规定（试行）》自 2024-10-08 实施；本仓库仍是研究和复盘工具，不得把研究策略直接升级为真实交易指令。

## 交易行为边界：能做与不能做

AI 必须同时理解“能做什么”和“不能做什么”。交易相关能力默认只服务研究、复盘、模拟和风控，不服务真实资金执行。

允许做的事：

- 提出研究假设、策略草案、风险标签、复盘结论和下一步验证计划。
- 编写和运行本地回测、组合模拟、纸面交易、压力测试、参数敏感性、滚动窗口和失败样本分析。
- 生成观察清单、候选池、信号解释、风险提示和交易前检查清单，但必须标注为研究或模拟输出。
- 估算仓位、止损、止盈、交易成本、滑点、成交失败、涨跌停、停牌和流动性影响，用于验证策略是否合格。
- 设计真实交易前需要的人审流程、风控阈值、监控指标、异常暂停条件和审计日志格式。
- 在发现数据异常、回测异常、风控越界或不可成交假设不清时，主动暂停研究结论并要求补充证据。

禁止做的事：

- 不得连接真实券商、交易账户、资金账户、下单接口或任何可能触发真实资金变化的系统。
- 不得自动下单、撤单、改单、调仓、申购、赎回、融资融券、加杠杆或执行任何真实交易动作。
- 不得把“研究信号”“观察候选”“回测通过”表述成确定的买入、卖出、持有、评级或收益承诺。
- 不得绕过止损、仓位、交易频率、流动性、黑名单、冷却期、市场退潮过滤或人工确认流程。
- 不得使用未来数据、事后标签、幸存者样本、未披露财报、未建模成本或编造成交来让策略达标。
- 不得把缺失数据、停牌、涨跌停不可成交、滑点不足、样本太少或尾部亏损解释成“可以忽略”。
- 不得把 token、账户、密码、券商配置、真实持仓、真实成交、个人资金信息写入源码、前端、日志或研究报告。

灰区必须先问用户：

- 任何实盘、半自动交易、交易提醒、定时推送、券商 API、账户同步、资金读取、真实持仓导入或可被人直接执行的买卖指令。
- 任何会改变核心回测语义、风控硬线、成交假设、端口、凭据、数据库持久化数据或历史结果可比口径的修改。
- 任何从“研究工具”升级到“执行系统”的设计。没有明确确认时，默认停留在研究和模拟层。

## 默认优化方向

后续优化默认按以下顺序推进，不要跳过基础设施直接盲目调参：

1. 研究上下文：先固定资金假设、持仓/观察池、交易成本、回测窗口、目标指标、排除规则和实验记录格式。
2. 数据质量与风险过滤：优先补 ST/退市/停牌/上市不足、流动性、市值、基本面质量和异常财务过滤；全市场复核的入池过滤必须写入研究 `context.json`，不能事后从结果里挑掉输家；新闻只做风险标签和复盘解释。
3. 组合级回测：在单票和全市场逐标的扫描之后，补共享资金、最大持仓数、调仓顺序、资金占用和组合回撤，避免把单票适配结果误当组合策略。
4. 稳健性验证：每个候选策略都要做消融实验、不同时间窗口、标的池外样本、参数敏感性和交易次数检查。
5. 全市场复核：在 `001-research-reset` 期间，先固定新策略假设、数据字段、样本池、三段评估窗口和失败条件，不允许围绕旧策略继续参数微调。新策略进入验证阶段后，每完成一次策略进化都必须跑三年全市场回测复核；长期目标见 `docs/research/long-term-goal.md`，终极目标是年化收益率不低于 100%、盈亏比不低于 3:1、最大回撤低于 10%。当前阶段目标以 `docs/research/stages/` 的活跃阶段为准，不能跳过失败窗口归因直接调参冲收益。复核必须同时输出收益前 10 和收益后 10；收益后 10 的亏损、回撤和盈亏比是硬约束，不能只看头部样本。
6. 自动研究循环：只有在研究上下文、结果存档和复盘传递稳定后，才加入自动调度、策略搜索或多轮 Agent 协作。
7. 展示与操作：前端优化服务于研究决策和复盘追踪，不为了视觉效果牺牲信息密度或改变策略语义。

## 简单与手术改变

用最少代码解决当前问题。不要为一次性需求做抽象，不要加入未要求的灵活性，不要顺手重构无关代码。

每一行修改都应该能追溯到用户请求或验证需要。发现无关死代码可以指出，但不要擅自删除。

如果存在多种解释，先说出假设；如果选择会影响数据、交易语义、端口、凭据或大依赖，先问用户。

## 启动与构建

日常启动使用：

```powershell
.\启动回测系统.cmd
```

它应该只执行 `docker compose up -d`。不要在日常启动脚本里加入 `--build`。

只有在修改 Dockerfile、依赖文件、后端代码、前端代码，或用户明确说需要重建时，才使用：

```powershell
.\重新构建并启动回测系统.cmd
```

停止服务使用：

```powershell
.\停止回测系统.cmd
```

`.cmd` 启动脚本必须保留：

```bat
chcp 65001 >nul
cd /d "%~dp0"
```

## 数据与安全红线

PostgreSQL volume 是 Tushare 行情和同步记录的持久化来源。不要执行以下操作，除非用户明确要求并确认会丢数据：

```powershell
docker compose down -v
docker volume rm ...
```

不要把 `.env`、Tushare token、DeepSeek token、数据库密码或任何真实凭据写入源码、前端代码、日志或 README 示例之外的配置。

Tushare token 默认来自 `.env` 的 `TUSHARE_TOKEN`。请求体临时传 token 只作为调试兜底，不应成为前端常规交互。

DeepSeek token 默认来自 `.env` 的 `DEEPSEEK_TOKEN` 或兼容别名。没有 token 或调用失败时，必须允许降级为本地规则评价。

## 后端规则

数据库模型位于 `backend/app/models.py`。API 入口位于 `backend/app/main.py`。回测引擎位于 `backend/app/backtest_engine.py`。Tushare 字段映射、日期格式和空值处理集中在 `backend/app/tushare_client.py` 与后端同步逻辑，不要散落到前端。

`stock_daily_bars` 按 `ts_code + trade_date` 去重 upsert；`stock_daily_basic` 按 `ts_code + trade_date` 去重；财务指标按报告期和公告日去重。不要插入重复行情或重复基本面样本。

API 返回必须 JSON-safe。指标计算中的 `NaN`、`Infinity` 必须转成 `None/null` 或可展示兜底值。

全市场/标的池回测默认不调用外部 AI；单票复盘和质量诊断可以在有 token 时调用 DeepSeek，但必须保留本地规则兜底。

## 前端规则

前端使用 React + Vite，目录 `frontend/`。涉及页面、组件、布局或视觉优化时，必须先阅读 `.codex/skills/frontend-design/SKILL.md`。

审美方向是工业化风控终端：高信息密度、克制但有记忆点、网格感、纪律感、交易台感。不要做营销落地页、泛 SaaS 大卡片首页、装饰性 hero 或空洞说明区块。

第一屏必须是可操作工作台。核心工作流是：检测 API、同步列表/日线/基本面、筛选标的池、调整策略参数、运行单票/池内/全市场回测、查看指标、权益曲线、交易流水、AI/本地复盘和质量诊断。

按钮优先使用 `lucide-react` 图标加短文本。工具型动作要清楚，例如“检测 API”“补齐日线”“全市场验证”“质量诊断”。

## 回测语义

默认纪律来自用户投资笔记：

- 每周最多交易 2 次。
- 单票仓位上限默认 20%。
- 单笔风险上限默认 1%。
- 默认止损 5%。
- 第一止盈 3% 减半，第二止盈 5% 清仓。
- 退潮/弱势市场禁止开新仓。
- 盈利卖出当天禁止新买入。
- A 股默认按 100 股一手取整。

修改这些语义必须谨慎。如果改的是规则本身，而不是代码 bug，要在回复和日志里说明对回测结果解释的影响。

## Windows 与编码

用户在 Windows/PowerShell 环境工作。处理包含中文或特殊字符的路径时必须正确引用。

创建或编辑 PowerShell 脚本使用 UTF-8 with BOM。读取或输出中文文档时显式使用 UTF-8，避免把文件写成乱码。

不要把 Windows 路径硬编码到容器内部。容器内路径默认以 `/app` 为准。

## 必须先问用户

以下情况必须先停下来问用户：

- 会删除数据库 volume 或持久化行情数据。
- 会改变 Tushare token、DeepSeek token、数据库密码或端口约定。
- 会引入新的大型依赖、框架替换或数据库迁移工具。
- 会改变核心回测规则语义或历史结果可比口径。
- 会连接真实券商、交易账户或任何可触发真实资金变化的接口。

## 阶段操作日志

仓库根目录维护 `操作日志.md`。每个阶段性任务开始或结束时，Codex 应追加一条日志，至少包含：

- 时间：使用本机时间，格式尽量为 `YYYY-MM-DD HH:mm +08:00`。
- 阶段目标：本阶段打算解决什么问题。
- 实际操作：列出改过的文件、关键命令和重要决策。
- 验证结果：写明已运行的检查；如果没验证或验证失败，如实写原因。
- 后续事项：仍未完成、需要用户确认或下一阶段要接着做的事。

日志只记录事实和工程判断，不写入 `.env`、token、密码或真实凭据。

## 验证清单

修改任何代码后，必须运行最小范围检查。本仓库配置了项目级 Codex hook：

```text
.codex/hooks.json
.codex/hooks/post_tool_use_check.py
```

该 hook 监听 `PostToolUse` 的 `apply_patch/Edit/Write`，并调用：

```powershell
.\scripts\check-file.ps1 <刚修改的文件路径>
```

第一次启用或修改 `.codex/hooks.json` 后，需要在 Codex 的 `/hooks` 里 review/trust。未 trust 前，Codex 会跳过项目本地 hook。

改后端时至少运行：

```powershell
python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py
```

改前端时优先运行：

```powershell
docker compose run --rm frontend npm run build
```

改 Compose 时运行 `docker compose config`。如果 Docker daemon 权限不足或 Docker Desktop 未运行，不要假装已验证。
