# Python dependencies for the migrated my_quant research workspace.
# Install from the repository root with either:
#   uv venv .venv --python 3.12
#   . .venv/bin/activate
#   uv pip install -r my_quant/requirements.txt
# or:
#   python3.12 -m venv .venv
#   . .venv/bin/activate
#   python -m pip install -r my_quant/requirements.txt
#
# Versions are pinned from the source virtualenv used before migration.
akshare==1.18.64
matplotlib==3.11.0
numpy==2.4.6
pandas==3.0.3
tushare==1.4.29
yfinance==1.4.1
