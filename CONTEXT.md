# 美股个人投资工作台领域语言

本文件定义当前产品统一使用的领域术语。旧量化研究术语不属于当前产品模型。

## 组合事实

**手工持仓**：用户显式录入并维护的美股标的、数量和成本事实；不来自券商同步。

**现金余额**：用户显式维护的美元现金事实；不代表已连接资金账户。

**持仓版本**：每次成功修改持仓或现金后递增的并发控制版本，用于拒绝基于旧状态的写入。

**已实现交易事实**：用户手工确认的卖出及其成本、收入和已实现盈亏记录；不是系统执行的订单。

**权益快照**：在明确市场日期和价格观察下计算的组合权益记录；缺失或过期价格必须保留其数据质量语义。

## 市场与规则

**市场观察**：Alpaca 在特定 `as_of`、来源授权和用途下返回的行情事实；不是交易报价或执行承诺。

**数据缺口**：价格、时间、授权或来源健康不足，导致系统无法可靠形成某项投影的状态；不能用零值伪装。

**观察规则**：用户为持仓标的启用的确定性检查条件；它只生成提醒，不执行交易。

**规则评估**：按 XNYS 交易日与时段幂等计算规则是否命中的过程。

**注意事项**：今日工作台聚合的持仓异常、规则命中或数据缺口；不是买卖建议。

## 个人 AI 分析

**分析草稿**：在调用模型前展示给用户确认的目标、上下文和外发预览。

**分析运行**：用户确认后由独立 Worker 执行的一次 AI 工具调用过程，可成功、失败、取消或超时。

**工具证据**：AI 工具返回的、带可校验 `evidence_id`、来源和时点的数据；没有证据的模型陈述不是事实。

**分析结果**：模型基于当次上下文与工具证据生成的个人参考材料；不构成评级、收益承诺或自动执行指令。

## 系统边界

**私有工作台**：存放手工持仓、规则和分析状态的隔离 `private_workbench` schema 及其受控 API。

**个人分析 Worker**：唯一可读取模型 secret 并执行 AI 分析与持仓规则周期评估的私有进程。

**生产读回**：在目标服务器核验精确提交、容器、schema、API 和前端结果；本地或 CI 通过不能替代。
