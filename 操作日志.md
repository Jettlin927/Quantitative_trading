# 操作日志

## 2026-05-29 20:59 +08:00 - 全市场策略验证启动

- 阶段目标：把策略实验从默认单票回测/AI评价扩展为数据库内全市场策略验证，让同一套策略参数可以批量扫已有行情的股票。
- 实际操作：
  - 复查 `backend/app/main.py`、`backend/app/backtest_engine.py`、`backend/app/schemas.py` 和 `frontend/src/main.jsx` 的单票回测链路。
  - 使用 Context7 查询 FastAPI 当前文档，确认继续使用 Pydantic 请求体模型声明 POST 接口。
  - 确认本阶段不触发 Tushare 同步、不连接真实交易、不改变核心策略语义；批量验证默认不调用 DeepSeek/AI。
- 验证结果：尚未修改业务代码；下一步新增数据库内全市场回测接口和前端结果面板。
- 后续事项：完成接口、UI、最小检查与运行态验证。

## 2026-05-29 21:04 +08:00 - 全市场策略验证完成

- 阶段目标：让策略实验页可以对数据库内已有行情的股票做全市场批量回测，并把 AI 复盘改成单票次级动作。
- 实际操作：
  - 修改 `backend/app/backtest_engine.py`，给 `run_backtest` 增加 `include_ai` 参数；单票回测保持默认调用 DeepSeek，批量验证传入 `include_ai=False`。
  - 修改 `backend/app/schemas.py`，新增 `MarketBacktestRequest`，支持日期区间、策略配置、关键词/行业/市场过滤、最少 K 线数和最大股票数。
  - 修改 `backend/app/main.py`，新增 `POST /api/backtests/market`，从 PostgreSQL 读取已有日线，按股票批量运行同一套策略，返回候选数、测试数、跳过数、正收益占比、平均/中位收益和股票排行。
  - 修改 `frontend/src/main.jsx` 与 `frontend/src/styles.css`，把主动作改为“全市场验证”，新增全市场结果面板；“单票AI复盘”保留为次级按钮。
  - 执行 `docker compose restart api frontend`，让本地服务加载最新代码。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\backtest_engine.py backend\app\schemas.py backend\app\main.py frontend\src\main.jsx frontend\src\styles.css 操作日志.md` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py` 通过。
  - `POST /api/backtests/market` 使用 `q=002594` 返回 `tested=1` 且响应不含 `aiAnalysis`；全市场运行返回 `candidates=5524`、`tested=3`、`skipped=5521`，符合当前数据库只有少量股票已加载三年日线的状态。
  - 使用 Chrome headless 触发页面“全市场验证”，截图保存到 `artifacts/market-backtest-check.png`，页面显示全市场验证汇总。
- 后续事项：下一阶段可做批量数据预热任务，把 `skipped` 的股票补齐日线和基本面后，全市场验证才会覆盖更多标的。

## 2026-05-29 20:51 +08:00 - 指标图层与策略状态排查启动

- 阶段目标：修复交互式 K 线图中 BOLL/MA 指标线位置异常，并排查 MACD 预设切换后策略展示是否存在串台。
- 实际操作：
  - 复查前端 `InteractiveMarketChart` 的 lightweight-charts 图层配置、后端 BOLL/MA/MACD 指标计算，以及当前页面标的 `002594.SZ` 的最近日线指标值。
  - 使用 Context7 查询 `lightweight-charts` v5.2.0 文档，确认 `priceScaleId: ""` 会创建 overlay 独立价格轴，`priceScaleId: "right"` 才会与蜡烛图共用右侧价格轴。
  - 初步判断 MACD 预设疑点不是核心 MACD 金叉语义，而是切换策略/参数后旧回测结果与买卖点没有清空，容易把上一轮策略信号误看成当前 MACD 策略信号。
- 验证结果：尚未修改代码；下一步将进行最小范围前端修复并运行检查。
- 后续事项：修复 MA/BOLL 价格轴绑定；切换预设或参数时清理旧回测结果；完成前端构建和页面复核。

## 2026-05-29 20:55 +08:00 - 指标图层与策略状态修复完成

- 阶段目标：完成 BOLL/MA 图层价格轴修复，并避免 MACD 等策略预设切换后继续显示上一轮回测结果。
- 实际操作：
  - 修改 `frontend/src/main.jsx`，将 `addLineSeries` 默认 `priceScaleId` 从 overlay 空轴改为 `"right"`，让 MA 与 BOLL 默认共用蜡烛图右侧价格轴；MACD 和成交量仍保留各自副图 overlay 轴。
  - 修改 `frontend/src/main.jsx`，在 `updateForm`、`applyPreset`、`selectCandidate` 中清空 `result`，避免策略/参数/标的变化后旧交易流水和买卖点继续显示。
  - 执行 `docker compose restart frontend`，让本地前端服务加载最新代码。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 frontend\src\main.jsx 操作日志.md` 已通过 ESLint、TypeScript 和 Vite build。
  - `docker compose ps` 确认 `api`、`db`、`frontend` 均在运行。
  - 使用 Chrome headless 访问 `http://localhost:15173/?view=lab` 并生成 `artifacts/chart-scale-fixed-check.png`；截图确认 BOLL/MA 已与 K 线价格轴对齐。
- 后续事项：未改变 MACD 金叉本身语义；如需调整 MACD 入场规则、量能过滤或小资金一手股数约束，需要另起阶段确认规则影响。

## 2026-05-29 02:33 +08:00 - 仓库治理初始化

- 阶段目标：把当前项目初始化为 Git 仓库，并建立后续 Codex 阶段操作的可追踪记录。
- 实际操作：
  - 已运行 `git init`，在 `E:\coding_things\Quantitative_trading` 初始化本地 Git 仓库。
  - 已设置仓库本地 Git 配置：`core.quotepath=false`，让中文路径在 `git status` 中正常显示；`core.excludesfile=.git/info/exclude`，避免读取用户级 ignore 文件时触发权限警告。
  - 更新 `AGENTS.md`，新增“阶段操作日志”规则。
  - 新增 `.gitignore`，避免 `.env`、依赖目录、构建产物和本地噪声文件被误提交。
  - 新增本文件 `操作日志.md`，从本阶段开始记录后续工作。
- 验证结果：
  - `git init` 已成功返回。
  - 本阶段仅新增/修改文本治理文件，后续功能代码修改后仍需运行对应检查。
- 后续事项：
  - 继续推进三年回测、股票名称映射、常见指标、动态策略因子和前端工作台重做。

## 2026-05-29 02:40 +08:00 - 后端指标与标的解析扩展

- 阶段目标：让后端支持股票代码/名称映射、带常见指标的 K 线数据，以及可动态调整的策略因子。
- 实际操作：
  - 重写 `backend/app/backtest_engine.py` 的指标计算层，新增 MA5/10/20/30/60、BOLL、MACD、RSI、KDJ、ATR、成交量均线等字段。
  - 在回测配置中新增可调因子：趋势均线周期、BOLL 容差、中轨容差、成交量放大倍数、MACD/RSI 过滤开关与参数等。
  - 更新 `backend/app/schemas.py`，让 `DailyBarOut` 能返回常见指标。
  - 更新 `backend/app/main.py`，让 `/api/stocks` 支持代码、简称、名称、行业搜索；同步日线、读取 K 线和回测时可解析股票名称到 `ts_code`。
- 验证结果：
  - 已运行 `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py`，通过。
- 后续事项：
  - 前端需要接入标的搜索、三年默认区间、指标图层和动态策略因子面板。

## 2026-05-29 02:55 +08:00 - 前端工作台重做与端到端验证

- 阶段目标：按 `frontend-design` skill 重做前端页面，让标的搜索、三年区间、指标/K线和动态策略因子都能直接操作。
- 实际操作：
  - 重写 `frontend/src/main.jsx`，新增股票名称/代码/行业搜索、最近三年日期按钮、动态策略因子面板、MACD/RSI 过滤开关、指标快照、K线+MA+BOLL+MACD canvas 图层和交易流水展示。
  - 重写 `frontend/src/styles.css`，改为高信息密度的工业化风控终端视觉，避免原来的表单堆叠感。
  - 保留核心工作流：检测 API、同步股票列表、同步日线、载入行情、数据库回测、导出报告。
- 验证结果：
  - 首次运行 `.\scripts\check-file.ps1 ...` 被 PowerShell ExecutionPolicy 拦截，随后用 `powershell -NoProfile -ExecutionPolicy Bypass -File ...` 执行。
  - 普通权限下 Docker frontend 检查被 Docker Desktop 权限拦截，并且本机未安装前端依赖；经用户授权提升权限后完成检查。
  - 已通过 `py_compile`、ESLint、TypeScript `tsc --noEmit` 和 Vite build。
  - 已通过本地 HTTP 验证：`/api/health` 正常，前端首页 `http://localhost:15173/` 返回 200，三年区间 `/api/daily-bars` 返回 75 根数据且包含指标字段。
  - 已通过 `/api/stocks?q=三安` 验证中文名称搜索可返回 `600703.SH`。
  - 已通过 `/api/backtests/run` 验证三年区间请求可执行，并且回测结果中 MA、MACD、RSI、KDJ、ATR 至少有有效字段。
  - in-app 浏览器插件仍因 `windows sandbox failed: spawn setup refresh` 无法执行视觉截图验证。
- 后续事项：
  - 如果需要像素级视觉复核，需要等 Codex in-app 浏览器插件恢复，或由用户直接查看当前打开的 `http://localhost:15173/` 页面反馈。

## 2026-05-29 03:00 +08:00 - 运行态视觉复核与缓存迁移

- 阶段目标：确认新版前端不是只通过构建，而是真的在本地运行页面中渲染出来；同时避免旧 localStorage 日期让“三年回测”不直观。
- 实际操作：
  - 发现 `http://localhost:15173/src/main.jsx` 仍在服务旧模块缓存后，执行 `docker compose restart frontend` 重启前端容器。
  - 更新 `frontend/src/main.jsx`，新增 `formSchemaVersion`，旧缓存首次进入新版时会把日期迁移到最近三年。
  - 再次执行 `docker compose restart frontend`，让 Vite dev server 读取最新补丁。
  - 使用 Chrome headless 对 `http://localhost:15173/` 截图，输出到 `artifacts/frontend-visual-check.png`。
  - 更新 `.gitignore`，忽略 `artifacts/`，避免截图和浏览器临时 profile 被误提交。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 frontend\src\main.jsx frontend\src\styles.css` 通过 ESLint、TypeScript 和 Vite build。
  - 运行态源码检查确认包含 `formSchemaVersion`、`three-year-factors` 和 `dateYearsAgo(3)`。
  - 使用 UTF-8 JSON 请求 `/api/backtests/run`，传入中文标的 `三安`，三年区间返回 560 根 K 线、75 笔交易，且 MA、MACD、RSI、KDJ、ATR 均有有效字段。
  - 视觉截图确认新版深色风控终端页面已渲染，顶部和表单日期显示为 `2023-05-29 / 2026-05-29`。
- 后续事项：
  - 目标功能已具备；后续如果继续打磨，可根据用户实际看页面后的反馈优化交互细节。

## 2026-05-29 03:03 +08:00 - 选股与策略研究台升级启动

- 阶段目标：把当前回测页面升级为“选股池 + 基本面入口 + 策略实验 + AI客观评价”的研究工作台，并补充 MACD、BOLL、RSI、ATR、均线等常见策略因子。
- 实际操作：
  - 复查 `AGENTS.md`、`.codex/skills/frontend-design/SKILL.md` 和现有前后端文件。
  - 使用 Context7 查询 React 与 FastAPI 当前文档，确认表单状态、派生渲染、查询参数和响应模型写法。
  - 联网查阅 Fidelity 等公开资料，把 MACD、BOLL、RSI、ATR 的常见用途和失效边界作为策略库设计依据。
- 验证结果：本阶段为设计和实施前准备，尚未修改业务代码；后续改动完成后运行最小范围检查。
- 后续事项：实现股票筛选 API、策略分析结果、前端分区工作台，并用本地服务和截图复核页面是否真正可用。

## 2026-05-29 03:18 +08:00 - 选股/策略/AI评价第一版落地

- 阶段目标：完成用户要求的可操作选股界面、基本面入口、常见技术策略因子和策略 AI 客观评价板块。
- 实际操作：
  - 更新 `backend/app/backtest_engine.py`，新增 MACD 金叉、BOLL 上轨突破、BOLL 收口突破、RSI 反转、均线金叉等入场模式，并新增 BOLL 带宽、ATR 止损参数和本地 `aiAnalysis` 评价结果。
  - 更新 `backend/app/main.py`，新增 `/api/stocks/screen` 选股接口，按关键词、行业、市场、技术形态返回候选股票、基础资料入口、最新行情和技术评分。
  - 更新 `backend/app/schemas.py`，新增 `StockScreenOut`，并在日线返回中加入 `bollBandwidthPct`。
  - 重写 `frontend/src/main.jsx` 与 `frontend/src/styles.css`，把页面改成顶部命令条、左侧标的/基本面上下文、右侧“选股池 / 策略实验 / AI复盘”三页签。
  - 执行 `docker compose restart frontend`，让 Vite dev server 读取最新前端代码。
  - 使用 Chrome headless 截图 `http://localhost:15173/`，输出到 `artifacts/research-desk-check.png`。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\backtest_engine.py backend\app\schemas.py backend\app\main.py frontend\src\main.jsx frontend\src\styles.css` 已通过。
  - `/api/health` 返回 ok。
  - `/api/stocks/screen?q=三安&technical=all&limit=5&start_date=2023-05-29&end_date=2026-05-29` 返回 `600703.SH` 候选，包含技术评分、标签和基本面入口。
  - `/api/backtests/run` 使用 `macd-cross` 返回 23 笔流水，并包含 `aiAnalysis.score` 与 `aiAnalysis.verdict`。
  - 前端首页返回 200，运行态源码确认包含 `research-desk`、`aiAnalysis` 和 `ENTRY_PRESETS`，截图确认新工作台已渲染。
- 后续事项：
  - 真实财务/估值数据仍未接入；后续可新增 Tushare `daily_basic`、`fina_indicator` 等持久化表，再把基本面筛选从入口升级为真实因子。
  - 当前 AI评价为本地规则版，不调用外部大模型；如果需要自然语言深度研判，可在用户确认模型、费用和隐私边界后再接入。

## 2026-05-29 03:21 +08:00 - DeepSeek AI评价接入启动

- 阶段目标：按用户指定的 `DEEPSEEK_TOKEN` 和 `deepseek-v4-flash`，把策略评价从本地规则版升级为后端调用 DeepSeek 的客观分析，同时保留失败兜底。
- 实际操作：
  - 使用 Context7 查询 DeepSeek 官方 API 文档，确认 `https://api.deepseek.com/chat/completions`、Bearer token、`model`、`messages`、`response_format` 等参数。
  - 复查当前 `backend/app/backtest_engine.py`、`frontend/src/main.jsx`、`docker-compose.yml` 和 `.env.example`。
- 验证结果：本阶段尚未改业务代码；后续会运行最小范围检查和实际 API 请求验证。
- 后续事项：新增 DeepSeek 客户端、把 `DEEPSEEK_TOKEN` 传入 API 容器、前端展示模型来源。

## 2026-05-29 03:31 +08:00 - DeepSeek AI评价接入完成

- 阶段目标：让回测完成后优先调用 DeepSeek `deepseek-v4-flash` 生成策略评价，并在失败时明确降级到本地规则评价。
- 实际操作：
  - 新增 `backend/app/ai_client.py`，使用 Python 标准库调用 DeepSeek chat completions，要求 JSON 输出，并限制发送给模型的数据为回测摘要、最近交易、最新指标和本地基线评价。
  - 更新 `backend/app/backtest_engine.py`，把 `build_strategy_analysis` 的本地结果交给 DeepSeek 增强，返回 `provider`、`model`、`llmStatus`、`usage` 等状态字段。
  - 更新 `docker-compose.yml`，向 API 容器传入 `DEEPSEEK_TOKEN`、`DEEPSEEK_MODEL`、`DEEPSEEK_API_BASE`。
  - 更新 `.env.example`，补充 DeepSeek 相关环境变量示例，但不写入真实 token。
  - 更新 `frontend/src/main.jsx` 和 `frontend/src/styles.css`，AI复盘页展示 DeepSeek 模型来源；如果失败则显示“本地规则兜底”和错误摘要。
  - 发现当前 Codex/PowerShell 进程没有继承用户环境变量；确认 Windows 用户环境变量中 `DEEPSEEK_TOKEN` 存在后，在本次 `docker compose up -d api frontend` 命令中注入该变量并重建容器。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\ai_client.py backend\app\backtest_engine.py docker-compose.yml .env.example frontend\src\main.jsx frontend\src\styles.css` 已通过。
  - API 容器内验证 `DEEPSEEK_TOKEN` 存在且模型为 `deepseek-v4-flash`，未打印 token 内容。
  - `/api/backtests/run` 实测返回 `provider=deepseek`、`model=deepseek-v4-flash`、`llmStatus=ok`。
  - 前端首页 `http://localhost:15173/` 返回 200，并使用 Chrome headless 截图到 `artifacts/deepseek-ai-check.png`。
- 后续事项：
  - 如果以后从旧终端启动脚本，可能仍读不到刚新增的 Windows 用户环境变量；重新打开终端或重新双击启动脚本即可继承。

## 2026-05-29 03:34 +08:00 - 交互式K线图改造启动

- 阶段目标：替换当前手绘 canvas K线图，改成更美观、可缩放、可平移、带十字光标和指标开关的交互图表。
- 实际操作：
  - 使用 Context7 查询 TradingView `lightweight-charts` v5 文档，确认 `createChart`、`addSeries`、十字光标订阅、resize 和 cleanup 用法。
  - 复查 `frontend/src/main.jsx` 里的 `MarketCanvas` 和 `frontend/src/styles.css` 里的图表样式。
- 验证结果：本阶段为改造前准备，尚未完成代码验证。
- 后续事项：安装 `lightweight-charts`，替换图表组件，运行前端构建和页面截图验证。

## 2026-05-29 03:45 +08:00 - 交互式K线图改造完成

- 阶段目标：把原来的静态手绘 canvas 图表替换为可交互、可缩放、可平移、视觉更接近交易终端的 K 线分析图。
- 实际操作：
  - 在 `frontend/package.json` 和 `frontend/package-lock.json` 中新增 `lightweight-charts@5.2.0`。
  - 更新 `frontend/src/main.jsx`，用 `InteractiveMarketChart` 替换 `MarketCanvas`，接入 K线、MA、BOLL、MACD、成交量、买卖点标记、十字光标悬浮读数、3M/6M/1Y/ALL 区间切换和指标开关。
  - 更新 `frontend/src/styles.css`，重做图表控制条、读数条、图层开关和图表容器样式。
  - 新增页面初始自动载入当前标的本地日线，并支持 `?view=lab` 直达策略实验页，避免打开后是空图。
  - 执行 `docker compose restart frontend`，让 Vite dev server 读取新依赖和新模块。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 frontend\package.json frontend\src\main.jsx frontend\src\styles.css` 已通过 ESLint、TypeScript 和 Vite build。
  - `node -e "JSON.parse(require('fs').readFileSync('frontend/package-lock.json','utf8'))"` 已确认 `package-lock.json` 可解析；`check-file.ps1` 对大 lockfile 的 PowerShell JSON 解析存在已知兼容问题，因此单独用 Node 校验。
  - `npm list lightweight-charts --depth=0` 确认版本为 `5.2.0`。
  - `http://localhost:15173/?view=lab` 返回 200，运行态源码包含 `InteractiveMarketChart` 和 `lightweight-charts`。
  - 使用 Chrome headless 截图到 `artifacts/interactive-chart-check.png`，确认交互式图表已渲染，读数条显示 RSI/MACD。
- 后续事项：
  - 目前图表支持基础交互；如果继续增强，可再加画线工具、区间收益测量、成交点点击详情和副图分屏。

## 2026-05-29 04:07 +08:00 - 基本面/消息面选股池重设计启动

- 阶段目标：根据用户提供的 Tushare 文档、waditu-tushare skills 和 alphaear-news skill，把“待接入”的基本面/估值做成真实数据，并重新设计更直观的选股池。
- 实际操作：
  - 读取 `AGENTS.md`、`.codex/skills/frontend-design/SKILL.md` 和 `E:\coding_things\notebook4me\.claude\skills\alphaear-news\SKILL.md`。
  - 读取 alphaear-news 的 `references/sources.md` 和 `scripts/news_tools.py`，确认可用消息源包括财联社、华尔街见闻、雪球热榜等，流程是抓取明确来源后聚合，不伪造消息。
  - 查阅 Tushare 官方文档和 `waditu-tushare/skills`，确认 `daily_basic` 可提供 PE/PB/PS/换手率/市值等每日指标，`fina_indicator` 可提供 ROE/毛利率/净利率/资产负债率/增长率等财务指标。
- 验证结果：本阶段为设计与资料确认，尚未完成代码验证。
- 后续事项：新增后端持久化表、同步接口、消息面接口和前端选股池卡片式改造。

## 2026-05-29 04:18 +08:00 - 交互 K 线图高度膨胀修复

- 阶段目标：修复用户指出的交互 K 线图区域高度会随着悬浮/交互持续增加的问题。
- 实际操作：
  - 修改 `frontend/src/main.jsx`，让 `InteractiveMarketChart` 初始化时读取容器真实高度，并用 `rangeRef` 避免切换 3M/6M/1Y/ALL 时重建整张图。
  - 修改 `frontend/src/styles.css`，把 `.market-chart` 从只有 `min-height` 改为明确的响应式高度 `clamp(420px, 50vh, 560px)`，并移除会干扰图表内部 canvas 布局的宽度覆盖。
  - 执行 `docker compose restart frontend`，让 Vite dev server 读取最新 CSS。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 frontend\src\main.jsx frontend\src\styles.css` 已通过 ESLint、TypeScript 和 Vite build。
  - 使用 Chrome headless 对 `http://localhost:15173/?view=lab` 连续移动十字光标 8 次，`.market-chart` 高度稳定为 `560px`，`.chart-panel` 稳定为 `726px`，页面 `scrollHeight` 稳定为 `1741px`。
  - 截图保存到 `artifacts/chart-height-fixed-check.png`。
- 后续事项：继续执行基本面/估值、消息面和选股池重设计目标。

## 2026-05-29 04:42 +08:00 - 基本面/消息面选股池第一版落地

- 阶段目标：把“待接入”的财务/估值入口改成真实 Tushare 数据，并把选股池从表格改成更直观的候选卡片 + 消息面面板。
- 实际操作：
  - 修改 `backend/app/models.py`，新增 `stock_daily_basic` 与 `stock_financial_indicators` 两张持久化表。
  - 修改 `backend/app/schemas.py`，新增基本面同步、基本面查询和消息面返回模型；`StockScreenOut` 增加 `fundamental_score`、`fundamental_tags` 和 `news_state`。
  - 修改 `backend/app/main.py`，新增 `/api/tushare/sync-fundamentals`、`/api/stocks/{ts_code}/fundamentals`、`/api/news/trends`；同步字段覆盖 PE/PB/PS/换手率/市值、ROE/毛利率/净利率/资产负债率/营收与利润增速。
  - 处理 Tushare `fina_indicator` 批量返回中同一唯一键重复的问题，新增批内去重后再 upsert。
  - 修改 `frontend/src/main.jsx` 和 `frontend/src/styles.css`，把选股池改为候选卡片，展示技术评分、基本面评分、估值/质量指标、标签和真实消息面；移除“待接入 Tushare daily_basic / fina_indicator”占位文案。
  - 调整消息面刷新逻辑：关键词像股票代码时展示通用财经热点，中文名/行业词才做标题过滤，避免默认代码搜索导致空面板。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\models.py backend\app\schemas.py backend\app\main.py frontend\src\main.jsx frontend\src\styles.css 操作日志.md` 已通过；后续小修也分别通过 `backend\app\main.py` 与 `frontend\src\main.jsx frontend\src\styles.css` 检查。
  - 执行 `docker compose restart api frontend`，服务恢复正常；`/api/health` 返回 ok，前端 `http://localhost:15173/` 返回 200。
  - `POST /api/tushare/sync-fundamentals` 对 `600703.SH` 同步成功：`daily_basic_rows=726`，`fina_indicator_rows=12`。
  - `/api/stocks/600703.SH/fundamentals` 返回 2026-05-28 估值和 2026-03-31 财务指标；选股接口返回三安光电候选卡片，包含 `fundamental_score=32` 和真实估值/财务字段。
  - `/api/news/trends?sources=cls,wallstreetcn,xueqiu&count=2` 返回真实来源消息；前端截图验证候选卡片 1 张、消息面 18 条、页面不再包含“待接入 Tushare”文本。
  - 截图保存到 `artifacts/fundamental-screener-check.png`。
- 后续事项：如果继续增强，下一步可以把基本面筛选条件做成显式控件，例如 PE/PB 区间、ROE 下限、负债率上限、营收/利润增速条件，并把消息面升级为股票名称/行业相关性评分。

## 2026-05-29 20:48 +08:00 - README 与远端发布准备

- 阶段目标：为上传 GitHub 远端准备中文 README、配套截图，并确认真实 `.env` 不会被提交。
- 实际操作：
  - 使用 Chrome headless 生成两张 README 截图，保存到 `docs/images/readme-screener.png` 和 `docs/images/readme-lab.png`。
  - 重写 `README.md`，补充项目定位、功能截图、技术栈、快速启动、环境变量、数据表、交易纪律、安全边界和发布前检查。
  - 更新 `.gitignore`，显式忽略 `.agent/` 本地目录；保留 `.env`、`artifacts/`、依赖目录和构建产物忽略规则。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 README.md .gitignore .env.example` 已执行。
  - `git check-ignore -v .env artifacts .agent docs\images\readme-screener.png docs\images\readme-lab.png` 确认 `.env`、`artifacts/`、`.agent/` 被忽略，`docs/images` 截图不会被忽略。
  - `git status --short --ignored` 显示 `.env` 为 ignored，`docs/` 为待提交。
- 后续事项：配置 GitHub 远端 `https://github.com/Jettlin927/Quantitative_trading.git`，提交并推送。

## 2026-05-29 20:56 +08:00 - GitHub 远端推送完成

- 阶段目标：把本地仓库发布到用户指定的 GitHub 远端。
- 实际操作：
  - 配置远端 `origin` 为 `https://github.com/Jettlin927/Quantitative_trading.git`。
  - 发现远端 `main` 已有一个 README 初始提交，先 `git fetch origin main` 检查远端内容。
  - 执行 `git add -A`，确认 `.env` 没有进入暂存区。
  - 提交 `Initial local quant research workbench`。
  - 将本地分支改名为 `main`，用 `git merge origin/main --allow-unrelated-histories -s ours` 保留远端历史但采用本地完整项目树。
  - 执行 `git push -u origin main`，已推送到远端。
- 验证结果：
  - 推送返回 `main -> main`，本地 `main` 已跟踪 `origin/main`。
  - 远端原始提交 `0169e22` 已保留在合并历史中，最新本地合并提交为 `565f165`。
- 后续事项：本条日志需要再单独提交并推送，确保远端操作记录完整。
## 2026-05-29 21:08 +08:00 - 全市场数据预热入口启动
- 阶段目标：把全市场回测前的数据准备从“只同步当前标的”扩展为可按交易日同步全 A 日线和全 A 每日估值指标，避免全市场验证只覆盖已手动同步过的少量标的。
- 实际操作：
  - 复查 Tushare `daily`、`daily_basic` 和 `trade_cal` 文档，确认日线与每日估值都可以按 `trade_date` 拉取全市场数据。
  - 设计新增后端接口 `/api/tushare/sync-market-daily` 与 `/api/tushare/sync-market-daily-basic`，保留原有单票同步接口。
- 验证结果：尚未完成代码检查；下一步运行最小范围检查、重启服务，并用最近一个交易日做小范围实测。
- 后续事项：如果接口验证通过，前端提供“全量日线”和“全量估值”入口；季报财务指标仍需单独评估批量接口和额度。
## 2026-05-29 21:17 +08:00 - 全市场数据预热入口完成
- 阶段目标：修正全市场回测只能覆盖已同步标的的问题，新增全 A 日线与全 A 每日估值的数据库预热能力。
- 实际操作：
  - 修改 `backend/app/schemas.py`，新增 `SyncMarketDataRequest`，支持日期区间和小范围测试用的 `max_trade_dates`。
  - 修改 `backend/app/main.py`，新增 `/api/tushare/sync-market-daily` 与 `/api/tushare/sync-market-daily-basic`；按交易日循环拉取全市场数据，并用分块 upsert 避免 PostgreSQL 参数上限。
  - 修改 `frontend/src/main.jsx`，命令区新增“全量日线”和“全量估值”，原单票入口改为“单票日线”和“单票基本面”。
  - 发现 Tushare `daily_basic` 全市场拉取需要走 `pro.query("daily_basic", ts_code="", trade_date=...)`；直接调用 `pro.daily_basic(ts_code="", trade_date=...)` 会返回空表。
- 验证结果：
  - `check-file.ps1 backend\app\schemas.py backend\app\main.py frontend\src\main.jsx 操作日志.md` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py` 通过。
  - `POST /api/tushare/sync-market-daily` 对 `2026-05-27` 实测写入 `5506` 条日线。
  - `POST /api/tushare/sync-market-daily-basic` 对 `2026-05-27` 实测写入 `5506` 条每日估值。
  - 数据库查询确认 `stock_daily_bars` 与 `stock_daily_basic` 在 `2026-05-27` 均为 `5506` 条；`/api/backtests/market` 用 `min_bars=1,max_stocks=5` 可读到新同步的全市场日线。
  - 使用 Chrome headless 截图 `artifacts/full-market-sync-buttons.png`，确认前端入口已渲染且按钮不溢出。
- 后续事项：三年全量日线会写入数百万行，建议后续加后台任务进度、断点续跑和“同步最近 N 个交易日”快捷入口；季报财务指标批量同步仍需单独评估接口和积分额度。
## 2026-05-29 21:18 +08:00 - 消息面刷新问题排查启动
- 阶段目标：修复消息面面板点击刷新后经常显示“暂无真实消息面数据”的问题，明确区分新闻源不可用和股票关键词无匹配。
- 实际操作：
  - 复查前端 `refreshNews` 与后端 `/api/news/trends` 链路。
  - 直接请求 NewsNow 财联社、华尔街见闻、雪球源，确认新闻源可返回数据。
  - 发现当前问题主要来自关键词过滤：例如输入“比亚迪”时，如果实时热榜标题没有命中该词，后端会把可用通用财经热点全部过滤掉。
- 验证结果：尚未修改代码；下一步增加关键词无命中时的通用热点回退，并在前端显示提示。
- 后续事项：如果新闻源本身不可用，前端也应显示明确错误/提示，而不是只显示空态。
## 2026-05-29 21:31 +08:00 - 消息面刷新问题修复完成
- 阶段目标：让消息面刷新在股票关键词没有命中时仍展示可用财经热点，并避免冷启动新闻源请求串行拖慢页面。
- 实际操作：
  - 修改 `backend/app/main.py`，`/api/news/trends` 在关键词无匹配时返回通用财经热点和提示文案；新闻源无返回时返回明确 `message`。
  - 修改 `backend/app/main.py`，把财联社、华尔街见闻、雪球三路新闻源改为并发抓取，并在首次无数据时短暂重试一次。
  - 修改 `frontend/src/main.jsx`，新增 `newsMessage` 状态，把后端提示显示到消息面面板和全局状态。
  - 修改 `frontend/src/styles.css`，新增 `news-note` 提示样式。
- 验证结果：
  - `check-file.ps1 backend\app\main.py frontend\src\main.jsx frontend\src\styles.css 操作日志.md` 通过。
  - `python -m py_compile backend\app\main.py` 通过。
  - `GET /api/news/trends?sources=cls,wallstreetcn,xueqiu&count=6&q=比亚迪` 返回 `status=fallback`，带通用财经热点和“未匹配到”提示。
  - 冷启动后同接口耗时约 `2.11s`。
  - 使用 Chrome headless 点击页面消息面刷新按钮，面板展示 `12` 条真实来源消息，截图保存到 `artifacts/news-panel-refreshed.png`。
- 后续事项：后续可做股票名称、行业、概念的相关性评分，而不是只按标题硬匹配。
## 2026-05-29 21:33 +08:00 - 顶部操作区重设计启动
- 阶段目标：按 `frontend-design` skill 重新整理页面顶部，解决右上角操作按钮过多、视觉臃肿的问题。
- 实际操作：
  - 复查 `frontend/src/main.jsx` 的顶部 `command-strip` 结构和 `frontend/src/styles.css` 的按钮/响应式布局。
  - 使用 Context7 查询 React 官方组件传递事件处理器的写法，确认以小组件承载工具组，不改变现有业务函数。
  - 设计调整方向：状态与输入保留在主行，主动作只保留“全市场验证”，其余同步/载入操作收进分组工具带。
- 验证结果：尚未修改代码；下一步改前端 JSX 和 CSS，并跑前端构建与页面截图。
- 后续事项：保持工业化风控终端风格，不新增无关装饰组件。
## 2026-05-29 21:38 +08:00 - 顶部操作区重设计完成
- 阶段目标：减少顶部右侧按钮拥挤感，把同步、预热、载入和回测动作改成更清晰的工作流分组。
- 实际操作：
  - 修改 `frontend/src/main.jsx`，把原来右上角 8 个按钮的 `command-actions` 改为主行 + `operation-dock`：主行只保留状态、标的/日期输入和“全市场验证”主按钮。
  - 新增 `ActionCluster`，把操作分成“连接”“全市场预热”“当前标的”三组；复用现有业务函数，不改变同步或回测语义。
  - 修改 `frontend/src/styles.css`，新增 `command-inputs`、`operation-dock`、`action-cluster`、`action-chip` 等布局样式，并补齐桌面/移动端响应式规则。
  - 执行 `docker compose restart frontend`，让页面加载最新前端代码。
- 验证结果：
  - `check-file.ps1 frontend\src\main.jsx frontend\src\styles.css 操作日志.md` 通过 ESLint、TypeScript 和 Vite build。
  - Chrome headless 桌面截图保存到 `artifacts/redesigned-command-top.png`，确认顶部按钮已分组，不再堆在右上角。
  - Chrome headless 移动端截图保存到 `artifacts/redesigned-command-mobile.png`，确认小屏下按钮纵向分组展示。
- 后续事项：如果继续打磨，可把“全量同步”类长任务改成后台进度条/任务中心，进一步减少主界面操作压力。
## 2026-05-29 22:06 +08:00 - 全量日线按钮长任务锁定修复启动
- 阶段目标：解释并修复点击“全量日线”后页面长时间无法继续操作的问题，避免重复全区间同步造成前端全局等待。
- 实际操作：
  - 排查 API 日志与数据库状态，确认按钮会按当前三年区间触发全 A 日线同步；前端 `busy` 会在请求完成前禁用操作按钮。
  - 数据库已写入约 235 万条日线，说明长任务不是无响应，而是在持续补数据。
  - 设计修复：后端支持跳过已完整同步的交易日；前端长区间同步前弹确认，按钮文案从“全量日线/全量估值”调整为“补齐日线/补齐估值”。
- 验证结果：尚未完成检查；下一步运行后端/前端最小检查并重启服务。
- 后续事项：后续更彻底的方案是把全市场同步改为后台任务与进度条。
## 2026-05-29 22:09 +08:00 - 全量日线按钮长任务锁定修复完成
- 阶段目标：避免点击全市场日线同步后重复补全已同步日期，并让用户在长区间同步前明确确认。
- 实际操作：
  - 修改 `backend/app/schemas.py`，给 `SyncMarketDataRequest` 增加 `skip_existing` 与 `min_existing_rows`。
  - 修改 `backend/app/main.py`，新增稀疏交易日过滤逻辑；全市场日线/估值同步会跳过本地已有不少于 `5000` 行的交易日。
  - 修改 `frontend/src/main.jsx`，长区间补齐前弹出确认；按钮文案从“全量日线/全量估值”改成“补齐日线/补齐估值”。
  - 执行 `docker compose restart api frontend`，取消旧的长请求并加载修复后的代码。
- 验证结果：
  - `check-file.ps1 backend\app\schemas.py backend\app\main.py frontend\src\main.jsx 操作日志.md` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py` 通过。
  - 对 `2026-05-27` 调用 `/api/tushare/sync-market-daily` 返回 `trade_dates=0, skipped_trade_dates=1, rows_upserted=0`，确认完整日期会跳过。
  - 对稀疏的 `2026-05-28` 调用同接口返回 `trade_dates=1, rows_upserted=5506`，确认缺失日期会补齐。
  - Chrome headless 截图 `artifacts/market-sync-guard-check.png` 确认按钮文案已更新。
- 后续事项：更完整的体验仍应做后台任务、进度条和取消按钮，避免大任务依赖单个 HTTP 请求。
## 2026-05-29 22:10 +08:00 - 长任务数据库进度条启动
- 阶段目标：给全市场日线/估值补齐这类可预期长时间 busy 的按钮增加基于数据库覆盖情况的进度条。
- 实际操作：
  - 读取 `frontend-design` skill，沿用工业化风控终端的紧凑工具带风格。
  - 设计口径：不做前端假进度；后端按数据库中每个交易日的记录数、稀疏日期和更新时间聚合，前端长任务期间轮询显示。
- 验证结果：尚未完成检查；下一步运行后端/前端最小检查并重启服务。
- 后续事项：如果仍希望按钮完全不锁住，需要把同步任务进一步改造成后台任务队列。
## 2026-05-29 22:19 +08:00 - 长任务数据库进度条完成
- 阶段目标：让全市场日线/估值补齐按钮在长时间 busy 时有可恢复的数据库进度反馈。
- 实际操作：
  - 修改 `backend/app/main.py`，新增 `GET /api/tushare/sync-progress`，按目标表的 `trade_date`、行数、`updated_at` 和最近一次 `data_sync_runs` 聚合覆盖进度。
  - 修改 `frontend/src/main.jsx`，新增 `syncProgress` 状态、3 秒轮询和 `SyncProgressStrip`；长任务开始时切换进度目标，当前目标写入 `localStorage`，刷新后仍会恢复显示。
  - 修改 `frontend/src/styles.css`，新增顶部数据库覆盖进度条样式，保持在操作坞下方的紧凑横条布局。
  - 执行 `docker compose restart api frontend` 与后续 `docker compose restart frontend`，加载最新后端接口和前端页面。
- 验证结果：
  - `check-file.ps1 backend\app\main.py frontend\src\main.jsx frontend\src\styles.css 操作日志.md` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py` 通过。
  - `GET /api/tushare/sync-progress?target=daily&start_date=2023-05-29&end_date=2026-05-29&min_existing_rows=5000` 返回 `completeDates=567,totalDates=785,progressPct≈0.7224,rows=3028431`。
  - `GET /api/tushare/sync-progress?target=daily_basic&start_date=2023-05-29&end_date=2026-05-29&min_existing_rows=5000` 返回 `completeDates=1,totalDates=785,progressPct≈0.0016`，确认估值全市场数据仍需补齐。
  - Chrome headless 截图 `artifacts/sync-progress-strip-check.png` 确认顶部进度条渲染正常且文字不截断。
- 后续事项：进度条仍依赖当前 HTTP 请求完成后解除全局 busy；如果要同步期间继续操作，需要把全市场同步升级为后台任务、轮询任务状态并提供取消入口。
## 2026-05-29 23:02 +08:00 - 全市场验证内存优化启动
- 阶段目标：修复点击“全市场验证”后 API 容器内存飙升的问题，把单请求、一次性全量读库的验证改成异步任务和有界并发。
- 实际操作：
  - `docker stats` 确认 `quant_trading_api` 内存达到约 `10.67GiB`，先执行 `docker compose restart api` 释放正在运行的验证请求。
  - 排查 `backend/app/main.py`，发现 `/api/backtests/market` 会一次性把全部候选标的的日线加载到 `bars_by_code`，这是内存放大的主要来源。
  - 使用 Context7 查询 SQLAlchemy 2.0 文档，确认 `Session` 不能跨线程共享，大结果集应分批/流式消费。
- 验证结果：尚未修改代码；下一步改为主线程分批读库、worker 只处理普通 dict 数据，并让前端轮询任务进度。
- 后续事项：并发数必须有上限，不能为了“异步”无限开线程或进程。
## 2026-05-29 23:14 +08:00 - 全市场验证内存优化完成
- 阶段目标：把全市场验证改成后台异步任务，并通过分批读库和有界并发降低 Docker 内存占用，同时给页面增加回测进度条。
- 实际操作：
  - 修改 `backend/app/main.py`，保留 `/api/backtests/market` 兼容入口，但内部改为 `execute_market_backtest` 分批执行；每批最多读取 `80` 只股票行情，策略 worker 上限为 `4`。
  - 修改 `backend/app/main.py`，新增 `POST /api/backtests/market/jobs` 与 `GET /api/backtests/market/jobs/{job_id}`；后台任务使用独立数据库 Session，避免跨线程共享 SQLAlchemy Session。
  - 修改 `frontend/src/main.jsx`，全市场验证按钮改为创建后台 job，页面每 `2s` 轮询任务进度；新增回测进度条并把 job id 写入 `localStorage`，刷新后可恢复轮询。
  - 修改 `frontend/src/styles.css`，新增全市场验证进度条和策略实验页运行提示样式。
  - 执行 `docker compose restart api frontend`，加载最新后端和前端。
- 验证结果：
  - `check-file.ps1 backend\app\main.py frontend\src\main.jsx frontend\src\styles.css 操作日志.md` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py` 通过。
  - 小样本后台验证 `max_stocks=20` 完成，返回 `tested=20, skipped=0, failed=0`。
  - 中等样本后台验证 `max_stocks=800` 完成，过程中 API 内存约 `104.8MiB` 到 `110.4MiB`，未再出现 10GiB 级别占用。
  - 页面截图 `artifacts/market-backtest-progress-check.png` 确认回测进度条可见，显示处理数、完成数、跳过数、失败数、并发数和当前标的。
  - 运行中样本 `max_stocks=2500` 最终完成，`processed=2500,tested=2495,skipped=5,failed=0`，结束后 API 内存约 `119.4MiB`。
- 后续事项：如需支持多个用户同时验证，可把当前内存 job registry 换成数据库任务表或 Redis 队列；当前本地单用户工作台先限制为后台单任务队列更稳。
## 2026-05-30 01:50 +08:00 - 全市场验证吞吐调优启动
- 阶段目标：提高全市场回测吞吐量，目标至少约 `500只/分钟`，同时控制 Docker 内存占用。
- 实际操作：
  - 观察用户当前触发的三年区间全市场验证任务，`batchSize=80,workers=4`，完成 `5525` 只标的耗时约 `25分21秒`。
  - 估算当前吞吐约 `218只/分钟`，低于目标；同时 API 内存峰值约数百 MiB，说明还有提升并发的空间。
  - 判断策略回测属于 Python CPU 密集型任务，继续单纯增加线程可能受 GIL 限制；下一步把策略 worker 改成进程池，并提高批次大小和 worker 数。
- 验证结果：尚未修改代码；下一步调整后端并运行三年区间基准。
- 后续事项：如果进程池内存压力过高，需要回退到较低 worker 或增加可配置阈值。
## 2026-05-30 02:00 +08:00 - 全市场验证吞吐调优完成
- 阶段目标：在不明显增加内存风险的前提下，把三年区间全市场验证吞吐提高到至少 `500只/分钟`。
- 实际操作：
  - 修改 `backend/app/main.py`，把策略 worker 从线程池改为进程池，避免 Python CPU 密集型回测受 GIL 限制。
  - 修改 `backend/app/main.py`，把默认批次和并发调为 `MARKET_BACKTEST_BATCH_SIZE=360`、`MARKET_BACKTEST_WORKERS=12`；同时保留环境变量覆盖能力。
  - 修改 `backend/app/main.py`，父进程批量读取行情时使用轻量 tuple，进入 worker 后再转成回测需要的 dict，降低批次放大后的主进程内存和序列化压力。
  - 测试更激进的 `batch=480,workers=16`，吞吐反而下降且内存升高，因此回退到 `360/12`。
  - 执行 `docker compose restart api`，让新参数生效。
- 验证结果：
  - `check-file.ps1 backend\app\main.py 操作日志.md` 通过。
  - `batch=240,workers=8` 三年区间 `max_stocks=1000`：约 `697只/分钟`，运行期 API 内存约 `0.8GiB`。
  - `batch=360,workers=12` 三年区间 `max_stocks=1000`：约 `844只/分钟`，运行期 API 内存约 `1.1GiB`，估算全市场约 `6.5分钟`。
  - `batch=480,workers=16` 三年区间 `max_stocks=1000`：约 `763只/分钟`，峰值内存约 `1.45GiB`，不如 `360/12`。
  - 最终参数确认任务返回 `batchSize=360,workers=12`，小样本任务完成。
- 后续事项：若后续要压到约 `5分钟`，更可能需要优化 `run_backtest/enrich_rows` 的计算本身，而不是继续盲目加 worker；继续加并发已经出现收益递减。
## 2026-05-30 02:08 +08:00 - 回测计算核心优化完成
- 阶段目标：优化 `run_backtest/enrich_rows` 自身计算，进一步提升全市场三年区间验证吞吐。
- 实际操作：
  - 修改 `backend/app/backtest_engine.py`，给 `run_backtest` 增加 `include_details` 参数；全市场验证只返回紧凑指标，不再跨进程传回每只股票的完整 `rows/equity/trades` 明细。
  - 修改 `backend/app/main.py`，全市场 worker 调用 `run_backtest(..., include_details=False)`，改用 `tradeCount/completedTradeCount`。
  - 修改 `backend/app/backtest_engine.py`，把 `enrich_rows` 中重复切片求 SMA、BOLL 标准差、RSI、ATR 的逻辑改成滚动窗口累计；保留原策略语义。
  - 新增 `RollingStats` 与 `RollingRsi` 小工具类，用于 O(1) 更新均值、标准差和 RSI。
  - 执行 `docker compose restart api`，加载优化后的后端。
- 验证结果：
  - 随机行情对比测试通过：新旧 `enrich_rows` 的 MA/BOLL/RSI/ATR/KDJ/MACD 指标逐项一致；full/compact 两种 `run_backtest` 摘要一致。
  - `check-file.ps1 backend\app\backtest_engine.py backend\app\main.py 操作日志.md` 通过。
  - 三年区间 `max_stocks=1000`：优化后约 `3733只/分钟`，运行期 API 内存约 `1.13GiB`。
  - 三年区间全市场 `5525` 只：耗时约 `65.3s`，吞吐约 `5079只/分钟`，`tested=5442, skipped=83, failed=0`，运行期 API 内存约 `1.1-1.22GiB`。
- 后续事项：当前已经远高于 `500只/分钟` 目标；后续再优化应优先检查数据库读取、结果排序/返回体大小和前端大表渲染，而不是继续增加并发。
## 2026-05-30 02:10 +08:00 - 全市场日线覆盖进度口径修正启动
- 阶段目标：修复全市场日线覆盖停在约 `93%` 的误导性进度，让已完整同步的真实交易日显示为完成。
- 实际操作：
  - 查询 `/api/tushare/sync-progress`，确认当前 `observedDates=727,completeDates=727,sparseDates=0`，但 `totalDates=785,emptyDates=58`。
  - 判断原因是进度分母使用工作日估算，把春节、国庆等 A 股休市工作日计入空缺；最近同步记录显示 `trade_dates=5, skipped_dates=722, failed_dates=0`，真实交易日应为 `727`。
- 验证结果：尚未修改代码；下一步让进度接口优先使用最近一次覆盖该区间的同步记录作为真实交易日分母。
- 后续事项：如果没有可用同步记录，仍需保留工作日估算作为兜底。

## 2026-05-30 02:15 +08:00 - 全市场日线覆盖进度口径修正完成
- 阶段目标：让全市场日线覆盖进度以真实 A 股交易日为分母，避免周末和休市日把已完成覆盖误显示为 `93%`。
- 实际操作：
  - 修改 `backend/app/main.py`，新增 Tushare 交易日历查询与 1 小时内存缓存；`/api/tushare/sync-progress` 优先使用真实交易日历，失败时再退回最近同步记录和工作日估算。
  - 复用同一交易日查询函数给全市场日线/估值同步入口，保持同步日期口径与进度口径一致。
  - 执行 `docker compose restart api`，让后端加载修正后的进度口径。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\main.py` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py` 通过。
  - `GET /api/tushare/sync-progress?target=daily&start_date=2023-05-30&end_date=2026-05-30&min_existing_rows=5000` 返回 `totalDates=726, observedDates=726, completeDates=726, emptyDates=0, progressPct=1, basis=tushare_trade_calendar`。
  - Chrome headless 截图 `artifacts/sync-progress-calendar-check.png` 确认页面顶部显示“全市场日线覆盖 100% / 完整 726/726 日 / 空缺 0 日”。
- 后续事项：全量日线首次补齐仍受 Tushare 按交易日拉取接口和限流影响；后续若继续优化同步速度，应优先做后台同步任务队列与限速策略，而不是盲目提高并发。

## 2026-05-30 02:28 +08:00 - 自选标的池与池内验证完成
- 阶段目标：在单票回测和全市场验证之间新增“自选标的池”范围，让策略可以针对用户指定的一组股票做批量验证。
- 实际操作：
  - 修改 `backend/app/models.py`，新增 `stock_pools` 与 `stock_pool_members`，用于持久化研究池和池内标的。
  - 修改 `backend/app/schemas.py` 与 `backend/app/main.py`，新增标的池 CRUD、池内成员增删接口，并给 `MarketBacktestRequest` 增加可选 `pool_id`；池内验证复用原后台回测引擎，只把候选范围收窄到池内股票。
  - 修改 `frontend/src/main.jsx` 与 `frontend/src/styles.css`，在选股池页加入“自选标的池”控制台，支持建池、切换池、手动加标的、候选批量入池、单卡入池、移除成员和“验证池子”。
  - 执行 `docker compose restart api frontend`，让新表和前端界面加载生效。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\models.py backend\app\schemas.py backend\app\main.py frontend\src\main.jsx frontend\src\styles.css` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py` 通过。
  - 临时创建 `CodexPoolTest`，加入 `600703.SH` 与 `002594.SZ` 后调用 `/api/backtests/market`，返回 `candidates=2,tested=2,scopePool=CodexPoolTest`；随后已删除该测试池，未保留测试数据。
  - Chrome headless 截图 `artifacts/stock-pool-ui-check.png` 确认选股池页新增自选池控制台，布局未压坏现有三栏工作台。
- 后续事项：如后续需要多个长期组合，可继续补“复制池子”“从回测结果生成池子”“导入/导出代码列表”等批量操作；当前版本先保持小而可控。

## 2026-05-30 02:37 +08:00 - 基本面质量评分与排行榜完成
- 阶段目标：把基本面数据转成可解释的质量评分，并在选股池中作为排行榜口径，帮助用户从候选集中挑选优质资产。
- 实际操作：
  - 修改 `backend/app/schemas.py`，在 `StockScreenOut` 中新增 `fundamental_grade` 与 `fundamental_breakdown`。
  - 修改 `backend/app/main.py`，把原粗粒度基本面评分升级为 100 分制：盈利质量 30、成长性 20、资产负债 18、估值 20、流动性分红 12；返回 A/B/C/D/待同步等级和分项明细。
  - 修改 `backend/app/main.py`，给 `/api/stocks/screen` 新增 `rank_by` 查询参数，支持 `fundamental/composite/technical/valuation` 排行；基本面榜会优先排序财务与估值均完整的标的，避免只有估值数据的股票误排到优质资产前列。
  - 修改 `frontend/src/main.jsx` 与 `frontend/src/styles.css`，新增“排行口径”筛选项、“基本面质量榜”、候选卡片分项评分、左侧基本面等级展示。
  - 执行 `docker compose restart api frontend`，加载最新后端评分逻辑和前端界面。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\schemas.py backend\app\main.py frontend\src\main.jsx frontend\src\styles.css` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\main.py` 通过。
  - `GET /api/stocks/screen?q=002594&technical=all&rank_by=fundamental&limit=5&start_date=2023-05-30&end_date=2026-05-30` 返回 `fundamental_score=27,fundamental_grade=D`，并包含 `盈利质量/成长性/资产负债/估值/流动性分红` 分项。
  - Chrome headless 截图 `artifacts/fundamental-quality-board-check.png` 确认选股池页新增“排行口径”入口，左侧基本面等级展示正常。
- 后续事项：当前评分是规则化研究评分，不构成投资建议；如果要做真正全市场“优质资产榜”，还应先补齐全市场财务指标，否则缺财务的标的只能显示“待同步”并排在完整样本之后。

## 2026-05-30 02:47 +08:00 - 多Agent质量诊断页完成
- 阶段目标：新增一个按技术面、消息面、基本面综合验证单个股票质量的页面，并用多Agent概念输出研究评级。
- 实际操作：
  - 查阅 `TauricResearch/TradingAgents` 公开说明，参考其基本面、情绪、新闻、技术分析师角色拆分，但不引入 LangGraph 或真实交易链路。
  - 修改 `backend/app/ai_client.py`，新增 `analyze_stock_quality_with_deepseek`，在配置 `DEEPSEEK_TOKEN` 时可让 DeepSeek 综合本地多Agent证据；未配置时返回本地规则诊断。
  - 修改 `backend/app/main.py`，新增 `GET /api/stocks/{ts_code}/quality-analysis`，生成基本面分析师、技术分析师、情绪分析师、新闻分析师四个 agent 的评分、证据、风险、置信度，并汇总为 `买入/持有/中性/卖出` 研究评级。
  - 修改 `frontend/src/main.jsx` 与 `frontend/src/styles.css`，新增“质量诊断”页签、顶部“质量诊断”操作按钮、最终评级卡、AI综合状态、四个 agent 卡片、支撑/风险/观察清单。
  - 执行 `docker compose restart api frontend`，加载最新后端和前端。
- 验证结果：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\ai_client.py backend\app\main.py frontend\src\main.jsx frontend\src\styles.css` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py` 通过。
  - `GET /api/stocks/002594.SZ/quality-analysis?start_date=2023-05-30&end_date=2026-05-30&use_ai=false` 返回 `rating=卖出, score=37, confidence=78`，并包含四个 agent 的分数和评级。
  - Chrome headless 截图 `artifacts/quality-analysis-page-check.png` 确认“质量诊断”页签和空态运行按钮渲染正常。
- 后续事项：当前情绪分析师未接入 StockTwits/Reddit，使用财经新闻标题代理短线情绪；后续如要更接近 TradingAgents，可继续接入社媒数据源、宏观指标和研究员辩论/风险管理二级 agent。

## 2026-05-30 02:50 +08:00 - Codex 代码 Hook 核查与补强完成
- 阶段目标：确认本仓库 Codex 改码后检查 hook 是否已配置、已信任并可执行，同时补强对非 patch 型编辑工具的文件识别。
- 实际操作：
  - 核查 `.codex/hooks.json`，确认项目级 `PostToolUse` hook 监听 `apply_patch/Edit/Write`，Windows 下执行 `.codex/hooks/post_tool_use_check.py`。
  - 核查 `C:\Users\Windows\.codex\config.toml`，确认该仓库 hook 已存在 `trusted_hash` 记录。
  - 修改 `.codex/hooks/post_tool_use_check.py`，让变更文件提取同时支持 patch 文本、`command` 数组以及 `file_path/path/filename/target_file/files` 等显式路径字段。
- 验证结果：
  - 使用 Context7 查询当前 Codex hooks 文档，确认 `PostToolUse`、`matcher`、`commandWindows` 和 trust 状态模型与本仓库配置一致。
  - `python -m py_compile .codex\hooks\post_tool_use_check.py` 通过。
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 .codex\hooks\post_tool_use_check.py .codex\hooks.json scripts\check-file.ps1` 通过。
  - 模拟 `PostToolUse` 的 `Write + file_path` 与 `apply_patch + command` 两类事件，hook 脚本均正常返回。
- 后续事项：如后续修改 `.codex/hooks.json`，需要在 Codex 的 `/hooks` 中重新 review/trust；仅修改 hook 脚本本身不改变当前 hook 配置哈希。

## 2026-05-30 02:54 +08:00 - Agent 主管规则与代码导航更新完成
- 阶段目标：根据项目未来要同时服务人类 Web 工作台和 AI 自动研究流程的目标，更新 `AGENTS.md`，并新增面向后续 Agent 的代码导航文档。
- 实际操作：
  - 重写 `AGENTS.md`，把它收敛为 300 行以内的项目主管规则，补充“给 AI 看”的项目目标、AI 科研闭环、自动策略研究边界、DeepSeek 降级、本地研究安全红线和验证要求。
  - 新增 `docs/agent-code-map.md`，梳理启动入口、后端模块、主要 API、前端入口、AI 科研闭环落点、常见改动路线和验证命令。
  - 保持本阶段为文档治理变更，未修改业务代码、端口、依赖、数据库 volume 或核心回测语义。
- 验证结果：
  - `AGENTS.md` 行数检查为 `118` 行，低于 300 行约束。
  - `git diff --check -- AGENTS.md docs/agent-code-map.md` 未发现空白错误；Git 仅提示 `AGENTS.md` 下次触碰时会按当前换行设置转换行尾。
  - 使用 UTF-8 读取并搜索 `AI 研究闭环`、`agent-code-map`、`不要自动下单`、`DeepSeek`、`PostgreSQL`，确认新增规则和导航关键点可读。
- 后续事项：后续若实现真正无人策略进化调度器，应另起阶段，把研究产物保存位置、任务状态表、实验比较口径和回滚机制先设计清楚，再进入代码实现。

## 2026-05-30 03:13 +08:00 - AI 策略研究闭环首轮执行完成
- 阶段目标：把“收益率 20%、最大回撤 15% 以内”的目标固化为可复用研究上下文，并开始按“假设 -> 写代码/配置 -> 回测验证 -> 复盘 -> 下一轮输入”的方式执行策略研究。
- 实际操作：
  - 新增 `docs/research/README.md`，定义默认研究目标、单次研究文件夹协议和执行方式。
  - 新增 `docs/research/context.default.json`，固化三年窗口、资金假设、交易成本、风控参数、收益/回撤目标、最小交易数和新闻使用边界。
  - 新增 `docs/research/next-strategy-brief.md`，作为上一轮复盘传递给下一轮策略制定的固定入口。
  - 新增 `scripts/research/run_research_round.py`，读取研究上下文，调用本地 `/api/backtests/market/jobs`，并把 `hypothesis.md`、`context.json`、`strategies.json`、`results.json`、`review.md`、`next-input.md` 写入独立研究目录。
  - 执行 5 轮全市场逐标的扫描：`001-baseline-boll-rebound`、`002-baseline-macd-cross`、`003-baseline-trend-follow`、`004-trend-follow-wide-profit`、`005-trend-wide-no-macd`。
  - 根据前三轮复盘，将硬目标校正为用户指定的收益/回撤/最小交易数；纪律评分保留为诊断指标，不作为默认硬门槛。
- 验证结果：
  - `python -m py_compile scripts\research\run_research_round.py` 通过。
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 scripts\research\run_research_round.py docs\research\context.default.json` 通过。
  - `git diff --check -- docs/research scripts/research` 未发现空白错误。
  - `boll-rebound`、`macd-cross`、`trend-follow` 三个基线均未达到三年收益 20% 目标。
  - `trend-follow-wide-profit` 达标：全市场测试 `5445` 只，合格样本 `10` 个，最高 `002384.SZ` 收益约 `24.70%`、最大回撤约 `-4.05%`。
  - `trend-follow-wide-profit-no-macd` 达标且略优：合格样本 `12` 个，最高 `688307.SH` 收益约 `25.66%`、最大回撤约 `-2.10%`。
- 后续事项：当前结果仍是逐标的单票扫描，不是共享资金组合回测；下一阶段应先加入 ST/风险标的排除、流动性或基本面质量过滤，再补组合级回测器，避免把单票适配结果误当作可执行组合策略。

## 2026-05-30 03:26 +08:00 - AGENTS 后续优化方向补充完成
- 阶段目标：把当前策略研究后的后续优化方向固化到 `AGENTS.md`，让后续 Agent 在继续开发时按稳定顺序推进。
- 实际操作：
  - 修改 `AGENTS.md`，新增“默认优化方向”小节。
  - 将优化顺序固定为：研究上下文、数据质量与风险过滤、组合级回测、稳健性验证、自动研究循环、展示与操作。
  - 明确新闻只作为风险标签和复盘解释，不作为核心买入信号；同时强调不要跳过基础设施直接盲目调参。
- 验证结果：
  - 待运行 `AGENTS.md` 行数检查和 `git diff --check`。
- 后续事项：下一阶段如继续实现，应优先做 ST/风险标的排除、流动性/基本面质量过滤和组合级回测器。

## 2026-05-30 03:36 +08:00 - 策略进化后全市场复核目标调整与执行
- 阶段目标：按用户澄清，将目标调整为“每完成一个策略进化后，跑三年全市场回测复核”，并验证新目标线：收益率不低于 30%、盈亏比不低于 2:1、最大回撤不高于 10%。
- 实际操作：
  - 修改 `AGENTS.md`，把“全市场复核”加入默认优化方向：每次策略进化完成后必须跑三年全市场回测复核。
  - 修改 `docs/research/README.md` 与 `docs/research/context.default.json`，将第二阶段目标从旧的 20/15 单票目标调整为 30/2:1/10 的策略进化后全市场复核目标。
  - 修改 `backend/app/backtest_engine.py`，新增 `averageWin`、`averageLoss`、`profitLossRatio`、`profitFactor` 指标。
  - 修改 `backend/app/main.py`，让全市场逐标的回测结果返回 `profitLossRatio` 与 `profitFactor`。
  - 修改 `scripts/research/run_research_round.py`，让研究脚本按收益、盈亏比、回撤和最小交易数筛选合格样本，并新增 `trend-follow-wider-profit-no-macd` 进化策略。
  - 重启 `api` 容器，让新增指标生效。
  - 执行 `006-full-market-trend-wide-no-macd-target30`：原 6%/12% 止盈策略盈亏比和回撤达标，但最高收益 `25.66%`，未达 30%。
  - 执行 `007-full-market-trend-wider-profit-target30`：8%/16% 止盈策略出现 1 个达标样本，`688313.SH` 收益约 `31.02%`、盈亏比约 `3.20:1`、最大回撤约 `-3.91%`。
- 验证结果：
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py scripts\research\run_research_round.py` 通过。
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\backtest_engine.py backend\app\main.py scripts\research\run_research_round.py docs\research\context.default.json AGENTS.md` 通过。
  - `GET /api/health` 在 API 重启后返回 `ok`。
  - `007` 轮全市场复核测试 `5445` 只、跳过 `80` 只、失败 `0` 只，生成完整 `hypothesis/context/strategies/results/review/next-input` 研究目录。
- 后续事项：硬目标已有样本达标，但覆盖太窄；下一步应做稳健性验证和覆盖率提升，包括不同时间窗口、排除 ST/低流动性、加入基本面质量过滤，并尝试让达标样本数扩大。

## 2026-05-30 03:39 +08:00 - 高弹性止盈策略全市场复核达标
- 阶段目标：继续推进第二阶段全市场复核目标，在 8%/16% 止盈只有 1 个达标样本的基础上，提高收益弹性并验证是否能扩大达标覆盖。
- 实际操作：
  - 修改 `scripts/research/run_research_round.py`，新增 `trend-follow-maximum-profit-no-macd` 策略：MA 趋势跟随、移除 MACD 过滤、止盈提高到 `10%/20%`。
  - 执行 `008-full-market-trend-maximum-profit-target30` 三年全市场复核，生成独立研究目录 `docs/research/runs/008-full-market-trend-maximum-profit-target30/`，包含 `hypothesis.md`、`context.json`、`strategies.json`、`results.json`、`review.md`、`next-input.md`。
  - 更新 `docs/research/next-strategy-brief.md`，将 008 轮复盘传递为下一轮输入。
- 验证结果：
  - `python -m py_compile scripts\research\run_research_round.py` 通过。
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 scripts\research\run_research_round.py` 通过。
  - 008 轮全市场复核测试 `5445` 只、跳过 `80` 只、失败 `0` 只。
  - 008 轮达标样本数从 007 轮的 `1` 个提高到 `6` 个；最佳样本 `300135.SZ` 收益约 `36.05%`、盈亏比约 `4.00:1`、最大回撤约 `-2.34%`，满足收益 `>=30%`、盈亏比 `>=2:1`、最大回撤 `<=10%`。
- 后续事项：目标口径已经找到达标策略，但仍需做稳健性验证：不同年份窗口、排除 ST/低流动性、加入基本面质量过滤，确认不是少数标的或单一阶段行情导致的偶然结果。

## 2026-05-30 02:52 +08:00 - README 质量诊断说明补齐

- 阶段目标：在多Agent质量诊断页完成后，同步更新项目 README，方便后续启动和使用时理解新能力。
- 实际操作：
  - 修改 `README.md`，补充自选标的池、基本面质量榜、标的池/全市场验证和“质量诊断”能力说明。
  - 新增“质量诊断”小节，记录 `/api/stocks/{ts_code}/quality-analysis` 接口、四个分析师视角、DeepSeek 可选汇总和本地规则兜底。
  - 更新环境变量和数据表说明，补充 `DEEPSEEK_API_KEY`、`DEEPSEEK_TIMEOUT_SECONDS`、`stock_pools`、`stock_pool_members`。
- 验证结果：`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 README.md 操作日志.md` 通过。
- 后续事项：如后续接入 StockTwits、Reddit 或宏观指标，需要继续更新质量诊断的数据源说明。

## 2026-05-30 03:51 +08:00 - 全市场尾部审计口径补齐
- 阶段目标：纠正只看收益前 10 的全市场复核偏差，把收益后 10 的亏损、回撤和盈亏比纳入硬约束，为后续组合策略落地建立更严格门槛。
- 实际操作：
  - 修改 `scripts/research/run_research_round.py`，新增 `bottom10`、`tailRisk`、`baseTargetMet`，并让 `targetMet` 同时要求头部基础目标和收益后 10 尾部审计通过。
  - 修改 `docs/research/context.default.json`，新增最少合格样本数、收益后 10 数量、尾部最大亏损、尾部最大回撤、尾部最低盈亏比和组合策略目标占位参数。
  - 修改 `docs/research/README.md` 与 `AGENTS.md`，明确全市场复核必须同时输出收益前 10 和收益后 10，尾部审计是硬约束。
  - 新增 `docs/research/portfolio-strategy-gate.md`，记录候选策略进入组合级共享资金回测前必须满足的硬门槛。
  - 复跑 `009-full-market-tail-audit-maximum-profit`，使用 `trend-follow-maximum-profit-no-macd` 重新生成研究目录和下一轮输入。
- 验证结果：
  - `009` 全市场复核测试 `5445` 只、跳过 `80` 只、失败 `0` 只。
  - 头部基础目标达标：合格样本 `6` 个，最佳 `300135.SZ` 收益约 `36.05%`、盈亏比约 `4.00:1`、最大回撤约 `-2.34%`。
  - 收益后 10 尾部审计未通过：尾部最差收益约 `-19.97%`、最深回撤约 `-21.76%`、最低盈亏比 `0.00:1`，亏损和回撤各 `10` 个违规。
  - `AGENTS.md` 行数检查为 `206` 行，仍低于 300 行约束。
- 后续事项：当前候选策略不能进入组合落地；下一轮优先做 ST/退市风险、低流动性、上市不足、市场状态和基本面质量过滤，待尾部审计通过后再实现共享资金组合回测器。

## 2026-05-30 04:05 +08:00 - 入池过滤与组合诊断推进
- 阶段目标：把“尾部风险控制”从复盘建议推进成可执行过滤口径，并补齐共享资金组合回测诊断，判断当前候选策略是否能落地为组合策略。
- 实际操作：
  - 修改 `backend/app/schemas.py` 与 `backend/app/main.py`，为全市场回测请求新增 `exclude_st`、`exclude_bj`、`min_list_days`、`min_avg_amount`、`min_avg_circ_mv`、`min_avg_turnover_rate_f`，并在候选集查询阶段执行过滤。
  - 修改 `docs/research/context.default.json`，默认排除 ST/北交所、上市不足一年、日均成交额低于约 1 亿元、平均流通市值低于约 30 亿元、平均自由流通换手率低于 0.5% 的标的。
  - 修改 `scripts/research/run_research_round.py`，把过滤参数写入研究 payload，并新增 `trend-follow-maximum-profit-macd`、`trend-follow-maximum-profit-strong-volume`、`trend-follow-tight-stop-maximum-profit` 三个敏感性策略。
  - 新增 `scripts/research/run_portfolio_backtest.py`，在 `api` 容器内使用共享资金、最大持仓、单票仓位、行业集中度和每周新开仓限制进行组合诊断。
  - 执行 `010` 至 `013` 四轮过滤后逐标的尾部审计，并执行 `014`、`015` 两轮组合诊断。
  - 更新 `docs/research/README.md`、`docs/research/portfolio-strategy-gate.md` 与 `docs/research/next-strategy-brief.md`，记录组合诊断命令、失败证据和下一轮研究方向。
- 验证结果：
  - 数据覆盖检查：`stocks` 共 `5525` 只且都有上市日期；`stock_daily_bars` 和 `stock_daily_basic` 均覆盖约 `3901653` 行，关键过滤字段可用。
  - `010` 固定入池过滤后候选数降至 `2720`，但合格样本只有 `4` 个，收益后 10 最差收益约 `-17.23%`、最深回撤约 `-19.02%`，未通过。
  - `011` 恢复 MACD 动量确认、`012` 提高放量阈值、`013` 收紧止损到 3% 均未通过收益后 10 审计。
  - `014` 紧止损组合诊断总收益约 `-35.35%`、年化约 `-13.54%`、最大回撤约 `-37.20%`，未达标。
  - `015` 原 10%/20% 止盈组合诊断总收益约 `-38.07%`、年化约 `-14.77%`、最大回撤约 `-39.41%`，未达标。
- 后续事项：当前 MA 趋势跟随高弹性止盈路线应暂停继续调止盈/止损；下一轮优先研究市场状态过滤、指数趋势/市场宽度和买入排序，先减少无效入场，再尝试新的组合回测。

## 2026-05-30 04:21 +08:00 - 市场宽度与信号质量前沿验证
- 阶段目标：继续推进组合策略落地目标，把“市场状态过滤”和“买入排序质量”从建议变成可运行组合参数，并用实际三年组合数据验证收益/回撤前沿。
- 实际操作：
  - 修改 `docs/research/context.default.json`，在组合目标中加入市场宽度过滤、信号分数上限、最大持仓数和单票集中度参数。
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增市场宽度状态计算、前一交易日 Risk-On/Risk-Off 新开仓过滤、信号分数上下限过滤、组合层集中度修剪和对应指标输出。
  - 执行 `016` 至 `025` 十轮组合诊断，覆盖市场宽度过滤、紧止损、信号分数上限、持仓数、单票仓位和集中度修剪。
  - 更新 `docs/research/portfolio-strategy-gate.md`、`docs/research/README.md` 与 `docs/research/next-strategy-brief.md`，记录参数前沿和下一轮转向。
- 验证结果：
  - `016` 市场宽度过滤后，总收益约 `-20.21%`、最大回撤约 `-22.72%`，相比 `015` 明显改善但未达标。
  - `020` 市场宽度 + 信号分数上限 `3.2` 后，总收益约 `29.47%`、最大回撤约 `-17.83%`，接近收益目标但回撤过深。
  - `022` 持仓降至 `4`、单票 `18%` 后，总收益约 `22.08%`、最大回撤约 `-14.04%`。
  - `024` 持仓降至 `3`、单票 `15%` 后，总收益约 `15.71%`、最大回撤约 `-10.04%`。
  - `025` 加入单票集中度修剪后，总收益约 `15.75%`、最大回撤约 `-10.01%`，仍未同时满足收益 `>=30%` 和回撤 `<=10%`。
- 后续事项：当前路线的收益/回撤前沿已经清晰，不能判定完成；下一轮应把动态市场状态同步进入逐标的尾部审计，并研究新的信号质量因子，尤其是“市场 Risk-On + 个股未过度偏离 MA20 + 趋势刚启动”的入场点。

## 2026-05-30 04:29 +08:00 - 逐标的市场状态同步审计
- 阶段目标：补齐组合层市场宽度过滤与逐标的全市场尾部审计之间的口径差异，验证同一批 Risk-On 日期和信号分数阈值下，当前候选策略是否能通过收益前 10/后 10 审计。
- 实际操作：
  - 修改 `backend/app/backtest_engine.py`，让 `should_enter()` 支持 `allowedEntryDates`、`entryScoreMode`、`minEntrySignalScore`、`maxEntrySignalScore`，从而把市场宽度和过热过滤同步到逐标的回测。
  - 修改 `scripts/research/run_research_round.py`，支持 `context.strategy_overrides` 覆盖策略配置。
  - 新增 `docs/research/context.market-breadth-score32.json`，从 `025` 组合诊断的 Risk-On 交易日提取 `178` 个允许开仓日期，并写入 `maxEntrySignalScore=3.2`。
  - 执行 `026-single-tail-market-breadth-score32`、`027-portfolio-market-breadth-score32-synced-source`、`028-single-tail-market-breadth-score32-tight-stop`、`029-portfolio-market-breadth-score32-tight-stop-position-3x15`、`030-single-tail-market-breadth-score32-profit-12-24`。
  - 更新 `docs/research/README.md`、`docs/research/portfolio-strategy-gate.md` 与 `docs/research/next-strategy-brief.md`，记录同步口径和下一轮入场假设。
- 验证结果：
  - `026` 逐标的同步审计：正收益占比约 `74.93%`、平均回撤约 `-4.52%`，但最高收益约 `29.84%`，尾部最差收益约 `-13.46%`，仍未达标。
  - `027` 对应组合诊断：总收益约 `15.75%`、最大回撤约 `-10.01%`，未达标。
  - `028` 紧止损同步审计：尾部最差收益约 `-10.22%`，只剩 `1` 个亏损违规，但头部最高收益降至约 `20.12%`。
  - `029` 紧止损组合诊断：总收益约 `9.22%`、最大回撤约 `-10.34%`，未达标。
  - `030` 止盈 12%/24% 审计：头部最高收益约 `26.46%`、尾部最差收益约 `-12.91%`，未达标。
- 后续事项：当前 MA 趋势跟随路线已经证明不能同时满足收益、回撤和尾部要求；下一轮应新建“Risk-On + 未过度偏离 MA20 + 趋势刚启动/回踩确认”的入场假设，而不是继续微调当前路线。

## 2026-05-30 04:43 +08:00 - 入场形态与高流动性池复核
- 阶段目标：把一个候选策略推进到可执行组合策略前，继续验证新的入场形态、候选池质量和组合诊断是否能同时满足三年收益、盈亏比、最大回撤和尾部审计要求。
- 实际操作：
  - 修改 `backend/app/backtest_engine.py`，新增 `trend-pullback-confirm` 入场模式、MA20 乖离过滤和 MA20/MA60 结构过滤。
  - 修改 `scripts/research/run_research_round.py`，新增 `trend-pullback-confirm-market-breadth`、`trend-follow-market-breadth-unextended`、`trend-follow-market-breadth-ma60` 三个研究预设，并让 `read_json()` 兼容 Windows 生成的 UTF-8 BOM JSON。
  - 新增 `docs/research/context.market-breadth-score32-liquid.json`，在同一 Risk-On 口径下把候选池提高到日均成交额约 2 亿元、流通市值约 50 亿元、自由流通换手率 1%。
  - 执行 `031` 至 `041` 共 11 轮研究，覆盖回踩确认、MA20 未过热、MA60 结构、高流动性池、12%/24% 止盈、3% 紧止损及对应组合诊断。
  - 更新 `docs/research/README.md`、`docs/research/portfolio-strategy-gate.md`、`docs/research/next-strategy-brief.md`，把失败证据和下一轮方向传递给后续研究。
- 验证结果：
  - `python -m py_compile backend\app\backtest_engine.py scripts\research\run_research_round.py` 通过。
  - `031` 回踩确认逐标的审计：尾部最差收益约 `-9.06%`、最深回撤约 `-9.72%`，但头部最高只有 `16.24%`。
  - `037` 高流动性池原止损：测试 `1430` 只，头部最高约 `29.84%`，尾部盈亏比过线，但尾部最差收益和最深回撤均约 `-13.46%`。
  - `039` 高流动性池紧止损：尾部最差收益约 `-8.78%`，最深回撤约 `-10.32%`，头部最高约 `20.12%`。
  - `040` 高流动性原止损组合：总收益约 `0.90%`、最大回撤约 `-13.96%`。
  - `041` 高流动性紧止损组合：总收益约 `-7.00%`、最大回撤约 `-17.50%`。
- 后续事项：当前“日线趋势跟随 + 固定止盈止损 + Risk-On”路线不能判定为可执行落地组合策略；下一轮应转向横截面择强和失败节流，例如相对强度/行业排名买入排序、止损后冷却、连续失败降权和行业级暂停新增。

## 2026-05-30 04:53 +08:00 - 横截面择强组合前沿突破
- 阶段目标：在不继续微调止盈止损的前提下，实现并验证“横截面择强 + 失败节流”，判断候选策略能否从逐标的扫描推进到组合级共享资金回测。
- 实际操作：
  - 修改 `backend/app/backtest_engine.py`，为日线增强字段新增 `return20`、`return60`、`distanceFromHigh60Pct`、`recoveryFromLow20Pct`，供横截面排序使用。
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增 `cross_section_strength` 买入排序、失败标的冷却、行业周亏损冷却、冷却拦截统计、成交标的收益前 10/后 10 审计。
  - 新增 `docs/research/context.cross-section-strength-liquid.json`、`docs/research/context.cross-section-strength-liquid-14pct.json`、`docs/research/context.cross-section-strength-liquid-14pct-cool5.json`，分别记录横截面择强、单票建仓 14%/暴露 15%、短冷却敏感性上下文。
  - 执行 `042` 至 `046` 五轮组合诊断，并更新 `docs/research/README.md`、`docs/research/portfolio-strategy-gate.md`、`docs/research/next-strategy-brief.md`。
- 验证结果：
  - `python -m py_compile backend\app\backtest_engine.py scripts\research\run_portfolio_backtest.py` 通过。
  - `042` 横截面择强组合总收益约 `34.62%`、最大回撤约 `-10.68%`、盈亏比约 `4.00:1`，收益过线但回撤和单票暴露略超。
  - `044` 单票建仓上限 14%、观测暴露上限 15% 后，组合总收益约 `32.68%`、年化约 `9.89%`、最大回撤约 `-9.95%`、盈亏比约 `4.00:1`、完成交易 `136` 笔、最大单票暴露约 `14.93%`、最大行业暴露约 `34.01%`，组合核心指标全部过线。
  - `045` 新增成交标的尾部审计后，成交标的 `131` 只，收益后 10 最差收益约 `-9.75%`、最深回撤约 `-9.75%`，但最低盈亏比 `0.00:1`，尾部盈亏比未过。
  - `046` 把标的冷却从 20 天缩短到 5 天，组合指标和成交标的尾部审计结果基本不变，说明当前瓶颈不是标的冷却长度。
- 后续事项：目标仍未完成。下一轮应只研究尾部盈亏比修复，例如首亏后小仓二次确认、行业冷却恢复规则、成交标的尾部 PnL 贡献口径；不要回到单纯调止盈、止损或仓位。

## 2026-05-30 05:00 +08:00 - 尾部盈亏比修复负证据
- 阶段目标：继续推进组合策略落地目标，围绕 `044/046` 已过线的横截面择强组合，尝试修复成交标的后 10 单标的盈亏比为 `0.00:1` 的问题。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增活跃标的名单上限 `maxDistinctSymbols` 和老标的加分 `knownSymbolScoreBonus`，用于验证“有限标的池复用”是否能让单标的盈亏比可验证。
  - 新增 `docs/research/context.cross-section-strength-liquid-14pct-weekly1.json`、`docs/research/context.cross-section-strength-liquid-14pct-recovery.json`、`docs/research/context.cross-section-strength-liquid-14pct-roster40.json` 三个敏感性上下文。
  - 执行 `047` 至 `049` 三轮组合诊断，并更新 `docs/research/portfolio-strategy-gate.md`、`docs/research/README.md`、`docs/research/next-strategy-brief.md`。
- 验证结果：
  - `047` 每周新开仓降到 1：组合总收益约 `12.51%`、最大回撤约 `-6.72%`，收益不达标，后 10 盈亏比仍为 `0.00:1`。
  - `048` 首亏后恢复机会：组合总收益约 `21.05%`、最大回撤约 `-14.76%`，尾部最差收益约 `-30.17%`，证明亏损后追打会放大尾部。
  - `049` 活跃名单 40 只并复用老标的：组合总收益约 `5.29%`、最大回撤约 `-13.02%`，尾部最差收益约 `-18.55%`，证明强制复用会放大弱标的连续止损。
- 后续事项：当前最佳仍是 `044/046`；下一轮应在买入前过滤“一笔止损型尾部”，例如加入开仓当日波动、缺口/追高、长上影和止损距离质量过滤，而不是减少交易、亏损后恢复或强制复用标的。

## 2026-05-30 05:06 +08:00 - 买入前风险过滤前沿
- 阶段目标：围绕 `044/046` 横截面择强组合，在买入前过滤一笔止损型尾部，验证开仓当日高波动/追高过滤能否继续改善组合收益、回撤和尾部亏损。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增 `entryRiskFilter`，支持买入日振幅、日内涨幅、跳空和下影线过滤，并输出 `blockedRiskSignals` 与买入风险指标。
  - 新增 `docs/research/context.cross-section-strength-liquid-14pct-risk10.json`、`docs/research/context.cross-section-strength-liquid-14pct-risk10-lower2.json`、`docs/research/context.cross-section-strength-liquid-14pct-risk8.json`。
  - 执行 `050` 至 `052` 三轮组合诊断，并更新 `docs/research/portfolio-strategy-gate.md`、`docs/research/README.md`、`docs/research/next-strategy-brief.md`。
- 验证结果：
  - `050` 当日振幅不超过 10%、日内涨幅不超过 10%：组合总收益约 `65.70%`、年化约 `18.35%`、最大回撤约 `-4.71%`、组合盈亏比约 `4.00:1`，成交标的后 10 最差收益/回撤约 `-5%`。
  - `051` 加入下影线不超过 2%：组合总收益约 `56.75%`、最大回撤约 `-5.19%`，仍强但不改善后 10 单标的盈亏比。
  - `052` 当日振幅收紧到 8%、日内涨幅不超过 10%：组合总收益约 `72.68%`、年化约 `19.99%`、最大回撤约 `-3.86%`、组合盈亏比约 `4.00:1`、Profit factor 约 `2.29`、完成交易 `136` 笔、最大单票暴露约 `14.90%`、最大行业暴露约 `27.30%`，为当前最强组合前沿。
- 后续事项：目标仍未完成。`052` 的组合核心指标和尾部亏损/回撤已非常强，但成交标的后 10 的单标的盈亏比仍为 `0.00:1`；下一轮需要固化尾部审计口径，或研究受控复用且不放大亏损的机制。

## 2026-05-30 05:10 +08:00 - 尾部审计口径拆分
- 阶段目标：在不改变 `052` 策略参数的前提下，拆分成交标的后 10 审计，判断当前失败来自真实尾部亏损/回撤，还是来自单标的盈亏比样本稀疏。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增 `tailLossRisk` 与 `tailRatioEvidence`，保留原有严格 `tailRisk`，并输出 `targetCoreMetWithoutSparseTailRatio`。
  - 执行 `053-portfolio-cross-section-strength-liquid-14pct-risk8-tail-split`，沿用 `docs/research/context.cross-section-strength-liquid-14pct-risk8.json` 与 `037-single-tail-risk-on-trend-liquid` 前置源审计。
  - 更新 `docs/research/portfolio-strategy-gate.md`、`docs/research/README.md` 与 `docs/research/next-strategy-brief.md`，把拆分审计结论传给下一轮研究。
- 验证结果：
  - `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_research_round.py` 通过。
  - `053` 组合总收益约 `72.68%`、年化约 `19.99%`、最大回撤约 `-3.86%`、组合盈亏比约 `4.00:1`、Profit factor 约 `2.29`、完成交易 `136` 笔。
  - `053` 的组合核心门槛和成交标的后 10 亏损/回撤审计已通过，尾部最差收益/回撤约 `-5.00%`；但严格尾部审计仍未通过，因为后 10 盈亏比证据样本合格数为 `0/10`。
- 后续事项：目标仍未完成。下一轮不要为了制造单标的盈亏比而强制复用弱标的，应研究“受控复用且不放大亏损”的证据机制，或明确把组合成交尾部审计调整为亏损/回撤优先、盈亏比按样本充分性单独报告。

## 2026-05-30 05:21 +08:00 - 尾部盈亏比证据与源审计同步
- 阶段目标：继续推进组合策略落地目标，验证缺口/上影线过滤、更高质量小池子和逐标的源审计同步是否能修复成交标的后 10 盈亏比证据不足。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，让 `entryRiskFilter` 支持 `maxUpperShadowPct`。
  - 新增 `docs/research/context.cross-section-strength-liquid-14pct-risk8-gap08.json`、`docs/research/context.cross-section-strength-liquid-14pct-risk8-upper4.json`、`docs/research/context.cross-section-strength-liquid-14pct-risk8-gap08-upper4.json`、`docs/research/context.cross-section-strength-liquid-14pct-risk8-quality-plus.json`、`docs/research/context.cross-section-strength-liquid-14pct-risk8-ultra-liquid.json`、`docs/research/context.cross-section-strength-liquid-14pct-risk8-quality-plus-ms300.json`、`docs/research/context.cross-section-strength-liquid-14pct-risk8-ultra-liquid-ms100.json`。
  - 执行 `054` 至 `059` 六轮组合诊断，覆盖缺口过滤、上影线过滤、组合过滤、更高质量小池子和市场宽度 `minSamples` 适配。
  - 修改 `backend/app/backtest_engine.py` 和 `scripts/research/run_research_round.py`，把 `portfolio_target.entryRiskFilter` 同步进逐标的全市场源审计。
  - 执行 `060-single-tail-risk8-synced-source`，验证同步 `risk8` 后的逐标的全市场扫描。
  - 更新 `docs/research/portfolio-strategy-gate.md`、`docs/research/README.md` 与 `docs/research/next-strategy-brief.md`，记录负证据和下一轮方向。
- 验证结果：
  - `python -m py_compile backend\app\backtest_engine.py scripts\research\run_research_round.py scripts\research\run_portfolio_backtest.py` 通过。
  - `054` 缺口不超过 8%：组合总收益约 `59.38%`，后 10 盈亏比证据仍为 `0/10`。
  - `055` 上影线不超过 4%：组合总收益约 `66.50%`，后 10 盈亏比证据仍为 `0/10`。
  - `056` 缺口和上影线同时过滤：组合总收益约 `61.40%`，后 10 盈亏比证据仍为 `0/10`。
  - `057` 小池子未降低市场宽度样本阈值，组合没有开仓，证明 `minSamples=1000` 不适配高质量小池子。
  - `058` 高质量池并把 `minSamples` 降到 300：组合总收益约 `32.03%`，但单票暴露约 `15.27%` 略超，后 10 盈亏比证据仍为 `0/10`。
  - `059` 超高流动性池并把 `minSamples` 降到 100：组合总收益约 `57.23%`、最大回撤约 `-5.22%`、盈亏比约 `4.00:1`，组合核心门槛仍过，但后 10 盈亏比证据仍为 `0/10`。
  - `060` 同步 `risk8` 后的逐标的源审计测试 `1430` 只，最高单票收益约 `22.26%`，收益后 10 最差收益约 `-11.69%`、最深回撤约 `-12.21%`，逐标的源审计未通过。
- 后续事项：目标仍未完成。当前最强组合仍是 `053`；后续应把逐标的源审计降级为信号分布诊断，并把最终落地门槛聚焦到共享资金组合、成交标的后 10 亏损/回撤和尾部样本充分性。

## 2026-05-30 05:26 +08:00 - 可执行组合策略固化
- 阶段目标：把当前最佳候选策略从诊断状态固化为可执行组合策略基线，明确目标硬门槛和诊断项的边界。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，把目标硬门槛拆为 `objectiveGates`，把逐标的源审计和尾部单票盈亏比样本保留为 `diagnosticGates`，并修正 `should_enter()` 内风险过滤造成的 `blockedRiskSignals` 漏计。
  - 执行 `061-portfolio-cross-section-strength-liquid-14pct-risk8-executable-gate`，沿用 `docs/research/context.cross-section-strength-liquid-14pct-risk8.json`。
  - 新增 `docs/research/executable-strategy-cross-section-risk8.md` 和 `docs/research/executable-strategy-cross-section-risk8.json`，固化核心假设、标的池、资金/仓位、交易成本、入场点、出场点、风控参数和实际回测证据。
  - 更新 `docs/research/README.md`、`docs/research/portfolio-strategy-gate.md` 与 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `061` 目标硬门槛全部通过：三年组合总收益约 `72.68%`，年化约 `19.99%`，最大回撤约 `-3.86%`，组合盈亏比约 `4.00:1`，Profit factor 约 `2.29`，完成交易 `136` 笔。
  - `061` 最大单票暴露约 `14.90%`，最大行业暴露约 `27.30%`。
  - `061` 成交标的后 10 最差收益/回撤约 `-5.00%`，尾部亏损/回撤门槛通过。
  - 严格诊断项仍未通过：逐标的源审计未通过，成交标的后 10 单票盈亏比样本合格数仍为 `0/10`。
- 后续事项：当前目标硬门槛已完成。后续进化方向是滚动窗口验证、成本/滑点敏感性、尾部样本充分性和更现实的成交约束。

## 2026-05-30 09:39 +08:00 - Web 装载可执行组合基线
- 阶段目标：把 `cross-section-strength-risk8` 可执行组合策略装载到 Web 工作台，让用户直接查看收益曲线和 AI 策略审查。
- 实际操作：
  - 修改 `backend/app/main.py`，新增 `/api/strategies/executable/{strategy_id}`，从固化策略规格和 `061` 回测结果读取指标、组合权益曲线、门槛、收益前 10/后 10、复盘文本和本地 AI 审查摘要。
  - 修改 `frontend/src/main.jsx`，新增“组合基线”工作区和快捷按钮，展示总收益、年化、最大回撤、盈亏比、完成交易、尾部亏损、收益/回撤曲线、AI 审查、执行参数、门槛审计和收益前后 10 表。
  - 修改 `frontend/src/styles.css`，补齐组合基线的工业化风控终端布局、曲线读数、门槛胶囊、参数网格和响应式样式。
  - 重启 `frontend` 容器刷新 Vite dev server 缓存；未重建镜像，未触碰数据库 volume。
- 验证结果：
  - `python -m py_compile backend\app\main.py` 通过。
  - `npm run typecheck` 通过。
  - `git diff --check -- backend/app/main.py frontend/src/main.jsx frontend/src/styles.css` 通过，仅有 Git 换行提示。
  - 本机 `npm run build` 因 `rolldown` 可选原生包缺失失败；使用容器环境执行 `docker compose run --rm frontend npm run build` 通过。
  - `GET http://localhost:18000/api/strategies/executable/cross-section-strength-risk8` 返回 `726` 个权益点、总收益 `0.7267795567`、最大回撤 `-0.0385645861`、AI 分数 `83`、`targetMet=True`。
  - Chrome headless 打开 `http://localhost:15173/?view=baseline`，确认 `.baseline-layout`、组合收益曲线 canvas、AI 策略审查、收益前 10/后 10 均可渲染。
- 后续事项：下一步可以把 AI 审查从本地规则摘要升级为可配置 LLM 审查，并把滚动窗口/成本敏感性验证结果也接入同一 Web 面板。

## 2026-05-30 12:47 +08:00 - 二阶段稳健性闸门装载
- 阶段目标：把过拟合风险纳入 Web 策略基线，并明确只有三年硬门槛通过后才进入长周期/样本外稳健性验证。
- 实际操作：
  - 修改 `backend/app/main.py`，在 `/api/strategies/executable/{strategy_id}` 返回中新增 `robustness`，包含三年准入状态、本地日线覆盖、5 年/8 年数据覆盖、滚动样本外、参数稳定性、成本压力和当前三年内部年度切片。
  - 修改本地 AI 审查逻辑：三年硬门槛已过但稳健性未完成时，结论从“可作为研究基线”调整为“三年硬门槛已过，稳健性闸门待补数据”，AI 审查分从 `83` 下调到 `74`。
  - 修改 `frontend/src/main.jsx`，在“组合基线”页新增“二阶段稳健性闸门”面板，展示本地数据起止、覆盖年限、6 个稳健性门槛、三年内部切片和下一步动作。
  - 修改 `frontend/src/styles.css`，补齐稳健性面板、状态胶囊、门槛卡片、内部切片表格和响应式布局。
  - 重启 `frontend` 容器刷新 Vite dev server 缓存；未重建镜像，未触碰数据库 volume。
- 验证结果：
  - `docker compose exec db psql -U quant -d quant_trading -t -A -c "select min(trade_date), max(trade_date), count(*) from stock_daily_bars;"` 返回 `2023-05-29|2026-05-29|3901653`，说明当前本地历史只有约 3 年。
  - `python -m py_compile backend\app\main.py` 通过。
  - `npm run typecheck` 通过。
  - `git diff --check -- backend/app/main.py frontend/src/main.jsx frontend/src/styles.css` 通过，仅有 Git 换行提示。
  - `scripts\check-file.ps1 backend\app\main.py frontend\src\main.jsx frontend\src\styles.css` 通过，包含后端编译、ESLint、TypeScript 和 Vite build。
  - 单独本机 `npm run build` 仍因 `rolldown` 可选原生包 `@rolldown/binding-win32-x64-msvc` 缺失失败；项目检查脚本使用可用环境构建通过。
  - `GET http://localhost:18000/api/strategies/executable/cross-section-strength-risk8` 返回 `robustness.status=needs_longer_history`、`stage2Enabled=True`、覆盖 `2023-05-29..2026-05-29`、`gates=6`、`aiScore=74`。
  - Chrome headless 打开 `http://localhost:15173/?view=baseline`，确认 `.robustness-panel` 可渲染，页面包含“二阶段稳健性闸门”“缺长周期数据”“三年硬门槛”“5年数据覆盖”等内容。
- 后续事项：补齐至少从 `2021-05-30` 起的全市场日线和必要估值数据后，才能执行 5 年回测；补齐至 `2018-05-30` 后才能执行 8 年回测。之后再接入滚动样本外、参数扰动和成本/滑点压力测试的实际运行结果。

## 2026-05-30 20:23 +08:00 - 风险调整指标与成本压力优化
- 阶段目标：把合格量化策略标准中的风险调整指标和交易成本敏感性纳入当前可执行组合基线，并在不改变入场/出场语义的前提下优化集中度余量。
- 实际操作：
  - 修改 `backend/app/backtest_engine.py`，新增年化收益、年化波动、Sharpe、Sortino、Calmar 和最长回撤持续天数计算；单票和全市场逐标的回测都会输出这些指标。
  - 修改 `backend/app/main.py`，让全市场逐标的摘要和可执行策略 API 暴露新增风险调整指标。
  - 修改 `scripts/research/run_research_round.py`，在逐标的研究快照和复盘中输出 Sharpe。
  - 修改 `scripts/research/run_portfolio_backtest.py`，组合回测输出新增风险调整指标，并新增 `--cost-multiplier` 与 `--max-single-position-pct`，用于可复现的成本压力和仓位上限敏感性测试。
  - 执行 `062-portfolio-cross-section-strength-risk8-risk-adjusted`，复跑原 14% 建仓上限基线。
  - 执行 `063-portfolio-cross-section-strength-risk8-cost-2x`，测试 2 倍佣金和 2 倍印花税。
  - 执行 `064-portfolio-cross-section-strength-risk8-cost-2x-pos135`，测试 2 倍成本下把单票建仓上限降到 13.5%。
  - 执行 `065-portfolio-cross-section-strength-risk8-pos135-baseline`，验证正常成本下 13.5% 建仓上限。
  - 执行 `066-portfolio-cross-section-strength-risk8-cost-3x-pos135`，继续验证 3 倍成本压力。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`，把当前基线从 14% 建仓上限调整为 13.5%。
- 验证结果：
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py scripts\research\run_research_round.py scripts\research\run_portfolio_backtest.py` 通过。
  - `scripts\check-file.ps1` 针对 `backend\app\backtest_engine.py`、`backend\app\main.py`、`scripts\research\run_research_round.py`、`scripts\research\run_portfolio_backtest.py` 均通过。
  - 合成样本调用 `run_backtest()` 能输出新增风险指标。
  - `062` 原基线：总收益约 `72.68%`，年化约 `19.99%`，年化波动约 `8.26%`，Sharpe 约 `2.42`，Sortino 约 `5.62`，Calmar 约 `5.18`，最长回撤 `138` 天。
  - `063` 2 倍成本仍保持收益/回撤/盈亏比过线，但最大单票暴露约 `15.10%`，集中度门槛失败。
  - `064` 2 倍成本 + 13.5% 建仓上限通过目标硬门槛：总收益约 `65.63%`，最大回撤约 `-3.81%`，Sharpe 约 `2.32`，最大单票暴露约 `14.56%`。
  - `065` 正常成本 + 13.5% 建仓上限通过目标硬门槛：总收益约 `70.12%`，最大回撤约 `-3.75%`，Sharpe 约 `2.44`，最大单票暴露约 `14.76%`。
  - `066` 3 倍成本 + 13.5% 建仓上限仍通过目标硬门槛：总收益约 `62.05%`，最大回撤约 `-3.86%`，Sharpe 约 `2.22`，最大单票暴露约 `14.67%`。
- 后续事项：下一轮继续做滚动窗口验证、3 倍成本或滑点压力、涨跌停/跳空成交约束和尾部样本充分性；不要为了提高尾部单票盈亏比而强制复用弱标的。

## 2026-05-30 20:42 +08:00 - 滚动窗口验证与市场宽度诊断
- 阶段目标：把合格量化策略标准中的滚动/分段稳健性验证纳入当前 `cross-section-strength-risk8` 组合基线，确认三年完整窗口收益是否集中在少数阶段。
- 实际操作：
  - 新增 `scripts/research/run_window_validation.py`，支持年度和 18 个月滚动窗口验证，并输出每个窗口的年化收益、Sharpe、Sortino、最大回撤、盈亏比、交易数、集中度和市场宽度诊断。
  - 修改 `scripts/research/run_portfolio_backtest.py`，为组合回测增加 `evaluation_start_date`，允许读取窗口开始日前的历史数据预热指标，但只在验证窗口内交易和计算绩效。
  - 执行 `067-portfolio-cross-section-risk8-pos135-window-validation`，发现初版窗口验证缺少公平预热。
  - 执行 `068-portfolio-cross-section-risk8-pos135-window-validation-warmup` 和 `069-portfolio-cross-section-risk8-pos135-window-validation-warmup-prehistory`，确认早期窗口失败不是指标预热造成。
  - 执行 `070-portfolio-cross-section-risk8-pos135-window-validation-breadth-diagnostics`，补充 Risk-On 天数、候选数和阻断信号诊断。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `python -m py_compile scripts\research\run_window_validation.py scripts\research\run_portfolio_backtest.py scripts\research\run_research_round.py` 通过。
  - `scripts\check-file.ps1` 针对 `scripts\research\run_window_validation.py` 和 `scripts\research\run_portfolio_backtest.py` 通过。
  - `070` 滚动窗口验证未通过：7 个窗口通过 5 个、失败 2 个；最低年化收益 `0.00%`，最低 Sharpe `0.00`，最深窗口回撤约 `-4.98%`。
  - 失败窗口为 `Y1` 和 `R18-1`：两者 Risk-On 天数均为 `0`，候选数分别为 `739` 和 `846`，低于市场宽度过滤 `minSamples=1000`，导致完成交易数为 `0`。
- 后续事项：下一轮先把市场宽度样本从交易候选池中解耦，或建立独立宽度宇宙；不要先调止盈止损来掩盖滚动窗口失败。

## 2026-05-30 21:00 +08:00 - 独立市场宽度样本与滚动窗口修复
- 阶段目标：修复市场宽度样本与交易候选池耦合导致的假 Risk-Off，重新验证当前组合基线的滚动窗口、完整三年和成本压力表现。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增 `market_state_payload` 和 `market_breadth_scope` 支持；默认仍沿用交易候选池，只有上下文显式配置时才使用独立市场宽度样本。
  - 修改 `scripts/research/run_window_validation.py`，在滚动窗口验证中同步传入独立市场宽度样本，并把对应 payload 写入结果。
  - 修改 `docs/research/context.cross-section-strength-liquid-14pct-risk8.json`，新增 `market_breadth_scope`：排除 ST、北交所、上市不足一年，并要求足够 K 线；不套用交易候选池的高流动性、市值和换手过滤。
  - 执行 `071-portfolio-cross-section-risk8-pos135-window-validation-independent-breadth`。
  - 执行 `072-portfolio-cross-section-risk8-pos135-independent-breadth-baseline`。
  - 执行 `073-portfolio-cross-section-risk8-pos135-independent-breadth-cost-2x`。
  - 执行 `074-portfolio-cross-section-risk8-pos135-independent-breadth-cost-3x`。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `071` 滚动窗口验证全部通过：7 个窗口通过 7 个，最低年化收益约 `9.31%`，最低 Sharpe 约 `1.52`，最深窗口回撤约 `-6.97%`，最低窗口 Risk-On 天数 `38`。
  - `072` 正常成本三年完整窗口通过：总收益约 `68.92%`，年化约 `19.11%`，最大回撤约 `-4.16%`，Sharpe 约 `2.40`，最大单票暴露约 `14.36%`。
  - `073` 2 倍成本压力通过：总收益约 `64.97%`，最大回撤约 `-4.34%`，Sharpe 约 `2.28`。
  - `074` 3 倍成本压力通过：总收益约 `60.74%`，最大回撤约 `-4.43%`，Sharpe 约 `2.16`。
- 后续事项：当前三年完整窗口、成本压力和滚动窗口门槛均已通过。下一轮优先做涨跌停/跳空/滑点等成交现实性压力测试，以及市场宽度阈值和排序权重的小范围参数敏感性。

## 2026-05-30 21:15 +08:00 - 成交现实性压力测试
- 阶段目标：把合格量化策略标准中的成交可实现性纳入当前组合基线，验证滑点、跳空止损和涨停附近买入约束对策略的影响。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增成交压力参数：`--slippage-pct`、`--buy-slippage-pct`、`--sell-slippage-pct`、`--stop-gap-fill-at-open`、`--limit-up-entry-block-pct`。
  - 修改 `scripts/research/run_research_round.py`，让研究上下文中的 `execution_stress` 可以进入策略配置。
  - 组合结果新增 `blockedLimitUpSignals`、`stopGapFillEvents` 和 `estimatedSlippageCost`，并在复盘和下一轮输入中输出。
  - 执行 `075-portfolio-cross-section-risk8-pos135-execution-stress-slip10bp-gap-limitup`。
  - 执行 `076-portfolio-cross-section-risk8-pos135-execution-stress-slip10bp-gap`。
  - 执行 `077-portfolio-cross-section-risk8-pos135-execution-stress-slip10bp-only`。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `077` 单纯 0.1% 买卖滑点压力通过目标硬门槛：总收益约 `70.92%`，年化约 `19.58%`，最大回撤约 `-5.14%`，Sharpe 约 `2.42`，估算滑点成本约 `4629`，尾部亏损/回撤仍通过。
  - `076` 0.1% 滑点 + 止损跳空按开盘价成交时，组合核心收益/回撤仍过线：总收益约 `60.36%`，最大回撤约 `-6.02%`，Sharpe 约 `2.07`；但成交标的后 10 最差收益约 `-11.63%`，尾部亏损/回撤门槛失败。
  - `075` 再加入 9.5% 涨停附近禁止买入后，拦截 `13156` 个信号，总收益约 `-1.51%`，最大回撤约 `-11.93%`，说明一刀切涨停禁买过强。
- 后续事项：下一轮优先分析 `076` 的跳空尾部样本，针对隔夜跳空风险做细粒度过滤或保护；不要直接扩大仓位，也不要粗暴禁止所有强势涨停附近信号。

## 2026-05-30 21:29 +08:00 - 跳空尾部样本审计与历史跳空过滤负证据
- 阶段目标：审计 `076` 中导致尾部亏损/回撤破线的跳空止损样本，并验证更细的历史跳空过滤是否能修复尾部而不破坏收益。
- 实际操作：
  - 分析 `076-portfolio-cross-section-risk8-pos135-execution-stress-slip10bp-gap` 的成交标的后 10 和所有跳空止损样本。
  - 修改 `backend/app/backtest_engine.py`，在 `enrich_rows()` 中新增 `minPriorGap60Pct`、`priorGapDown3Count60`、`priorGapDown5Count60`。
  - 修改 `scripts/research/run_portfolio_backtest.py`，让组合回测可在 `entryRiskFilter` 中使用历史隔夜低开指标，并新增 CLI：`--max-prior-gap-down-60-pct`、`--max-prior-gap-down-3-count-60`。
  - 执行 `078-portfolio-cross-section-risk8-pos135-gap05-execution-stress-slip10bp-gap`，测试买入日高开不超过 5%。
  - 执行 `079-portfolio-cross-section-risk8-pos135-prior-gap3-execution-stress-slip10bp-gap`，测试近 60 日最差隔夜低开不超过 3%。
  - 执行 `080-portfolio-cross-section-risk8-pos135-prior-gap4-execution-stress-slip10bp-gap`，测试近 60 日最差隔夜低开不超过 4%。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `076` 后 10 中 5 个样本亏损超过 10%，均来自跳空止损按开盘成交；破线样本的近 60 日历史隔夜低开风险高于总体样本，但并非充分条件。
  - `078` 买入日高开限制到 5% 后仍未通过：总收益约 `25.05%`，尾部最差约 `-10.66%`。
  - `079` 近 60 日最差隔夜低开不超过 3% 后仍未通过：总收益约 `15.55%`，尾部违规降到 1 个但收益硬门槛失败。
  - `080` 近 60 日最差隔夜低开不超过 4% 后仍未通过：总收益约 `27.42%`，且出现约 `-31.28%` 的新尾部样本。
- 后续事项：历史跳空硬过滤不进入当前基线。下一轮转向入场后保护或组合层隔夜风险预算，并继续用 `076` 的跳空开盘止损口径作为成交现实性压力门槛。

## 2026-05-30 21:40 +08:00 - 次日开盘确认入场负证据
- 阶段目标：验证“收盘信号、次日开盘确认入场”是否能在 `076` 成交现实性压力口径下修复跳空止损尾部。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增 `--next-open-entry` 和 `--next-open-cancel-gap-down-pct`，支持把收盘信号排队到次日开盘执行，并在次日开盘低于信号收盘价一定比例或低于止损价时取消。
  - 组合结果新增 `nextOpenEntryOrders` 和 `nextOpenEntryCancels`。
  - 执行 `081-portfolio-cross-section-risk8-pos135-next-open-entry-slip10bp-gap`。
  - 执行 `082-portfolio-cross-section-risk8-pos135-next-open-entry-no-down-open-slip10bp-gap`。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `081` 次日开盘执行，若开盘低于信号收盘 `3%` 或低于止损则取消：总收益约 `22.73%`，最大回撤约 `-10.72%`，最大单票暴露约 `15.13%`，尾部最差约 `-20.99%`，未通过。
  - `082` 次日开盘必须不低于信号收盘价：候选订单 `648`、取消 `192`，总收益约 `5.57%`，最大回撤约 `-12.08%`，尾部最差约 `-21.50%`，未通过。
- 后续事项：次日开盘确认入场不进入当前基线。下一轮应测试组合层隔夜风险预算或开仓后动态减仓，不再优先尝试更粗的入场硬过滤。

## 2026-05-30 22:04 +08:00 - 新增 Tab2 量化全景页面
- 阶段目标：把合格量化策略标准、策略侧重点、组合基线证据、全量回测结果和图像集中到前端第二个主标签，方便直接审计策略是否达标。
- 实际操作：
  - 修改 `backend/app/main.py`，在可执行策略接口中补充 `allTrades` 和 `finalPositions`，让前端能展示组合基线的全量交易动作。
  - 修改 `frontend/src/main.jsx`，新增 `量化全景` Tab2，展示策略状态、三年收益、风险调整、最大回撤、尾部风险、交易样本、策略侧重点、合格门槛、组合权益图、全量收益分布、收益-回撤散点、全量收益排名曲线、全量回测结果表、全量交易动作和完成交易表。
  - 修改 `frontend/src/styles.css`，新增 Tab2 的工业化量化终端布局、图表 SVG 样式、全量表格和响应式规则。
  - 重启 `frontend` 容器，刷新 Vite 开发服务缓存。
- 验证结果：
  - `python -m py_compile backend\app\main.py` 通过。
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py` 通过。
  - `scripts\check-file.ps1` 针对 `backend\app\main.py`、`frontend\src\main.jsx`、`frontend\src\styles.css` 通过。
  - `docker compose run --rm frontend npm run build` 通过。
  - 访问 `http://localhost:15173/?view=quant` 截图检查通过：Tab2 可打开，组合权益图渲染正常，全量结果图表在未运行全市场验证时显示等待状态，不用单票行情冒充全量结果。
  - `http://localhost:18000/api/strategies/executable/cross-section-strength-risk8` 返回 `allTrades=323`、`completedTrades=136`。
- 后续事项：Tab2 的全量标的表和分布/散点图依赖前端发起“全市场验证”后的 `marketResult.results`；下一轮可继续把研究运行目录中的历史组合压力实验结果做成可切换证据列表。

## 2026-05-30 22:15 +08:00 - 开仓后保护与买入日复合风险过滤
- 阶段目标：在 `076` 成交现实性压力口径下，验证开仓后动态保护和买入日高开/振幅/日内涨幅组合过滤是否能修复跳空止损尾部。
- 实际操作：
  - 复核 `083-portfolio-cross-section-risk8-pos135-early-exit-lowbreak-slip10bp-gap`，确认该运行完整生成结果。
  - 分析 `076` 尾部成交标的的买入日 `riskMetrics` 和横截面 `scoreParts`，确认破线样本多集中在强势高分、高开、高振幅或强日内涨幅买点。
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增 CLI 覆盖参数：`--max-entry-range-pct`、`--max-intraday-return-pct`，用于实验性覆盖已有 `entryRiskFilter`，默认不改变基线。
  - 执行 `084-portfolio-cross-section-risk8-pos135-entry-risk-combo-slip10bp-gap`：0.1% 买卖滑点、跳空止损按开盘成交、单票建仓上限 13.5%、买入日高开 <=5%、振幅 <=7%、日内涨幅 <=4.8%。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `083` 早期跌破开仓日低点退出触发 `7` 次，组合总收益约 `51.64%`，最大回撤约 `-5.84%`，尾部最差仍约 `-11.63%`，未通过。
  - `084` 买入风险过滤拦截 `13515` 个信号，组合总收益约 `7.73%`，年化约 `2.51%`，最大回撤约 `-7.61%`，尾部最差约 `-23.69%`，未通过。
  - `python -m py_compile scripts\research\run_portfolio_backtest.py` 通过。
  - `scripts\check-file.ps1 .\scripts\research\run_portfolio_backtest.py` 通过。
- 后续事项：开仓后收盘级保护和买入日形态硬过滤均不进入当前基线。下一轮应转向事件/行业隔夜风险标签、跳空后再入场冷却或同类行业熔断，继续使用 `076` 作为成交现实性压力门槛。

## 2026-05-30 22:27 +08:00 - 跳空止损后冷却与行业熔断负证据
- 阶段目标：验证跳空止损后的再入场冷却和同类行业熔断，是否能在 `076` 成交现实性压力口径下修复成交标的后 10 尾部亏损/回撤。
- 实际操作：
  - 分析 `076` 中跳空止损尾部的时间和行业分布，确认极端尾部多为单次首次隔夜跳空，而非同一标的连续重复亏损。
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增默认关闭的实验参数：`--gap-stop-market-cooldown-days`、`--gap-stop-industry-cooldown-days`、`--gap-stop-symbol-cooldown-days`。
  - 回测结果新增跳空止损冷却事件数和拦截信号数，并写入 `review.md` / `next-input.md`。
  - 执行 `085-portfolio-cross-section-risk8-pos135-gap-stop-cooldown-slip10bp-gap`：0.1% 买卖滑点、跳空止损按开盘成交、单票建仓上限 13.5%、跳空止损后全市场冷却 3 天、同行业冷却 20 天、同标的冷却 60 天。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `085` 未通过：总收益约 `44.34%`，年化约 `13.02%`，最大回撤约 `-5.42%`，Sharpe 约 `1.69`，尾部最差仍约 `-11.63%`，尾部亏损/回撤违规 `4` 个。
  - 跳空冷却触发：全市场/行业/标的冷却各 `19` 次；分别拦截信号 `945`、`7303`、`493` 个。
  - 结论：事后冷却可以减少后续同类风险，但无法拦住单次首次隔夜跳空尾部；继续加长冷却天数预计主要压低收益。
- 后续事项：跳空止损后的事后冷却不进入当前基线。下一轮应转向开仓前或持仓过夜前可生效的事件/行业隔夜风险标签、隔夜仓位预算，或更真实的涨跌停/停牌成交模型。

## 2026-05-30 22:20 +08:00 - 选股池横幅与量化全景策略选择
- 阶段目标：按浏览器批注，把选股池里的自选标的池控制台改成横向横幅，并让 `量化全景` 支持切换到现有策略预设。
- 实际操作：
  - 修改 `frontend/src/main.jsx`，新增量化全景策略视角状态和下拉选项；选择组合基线时载入固化基线，选择其他策略时复用 `ENTRY_PRESETS` 更新当前回测参数。
  - 修改 `frontend/src/main.jsx`，让量化全景在组合基线和策略预设两种视角下显示不同的标题、指标卡、策略侧重点和验证快照。
  - 修改 `frontend/src/styles.css`，把自选标的池控制台改为双列横幅，池内标的改为横向滚动条，并补充量化全景策略下拉样式和移动端回退。
  - 重启 `frontend` 容器，刷新 Vite 开发服务缓存。
- 验证结果：
  - `.\scripts\check-file.ps1 frontend\src\main.jsx` 通过。
  - `.\scripts\check-file.ps1 frontend\src\styles.css` 通过。
  - `docker compose run --rm frontend npm run build` 通过。
  - 浏览器刷新 `http://localhost:15173/` 后，选股池中的自选标的池已按横幅渲染。
  - 浏览器打开 `http://localhost:15173/?view=quant` 后，策略视角下拉存在 `8` 个选项；切换到 `RSI超卖反转` 后，量化全景标题和指标卡切换为该策略视角。
- 后续事项：当前“其他策略”复用已有策略预设和全市场验证入口；若后续要切换多个已固化组合策略，需要后端新增策略列表/证据接口。

## 2026-05-30 22:53 +08:00 - 前端整体研究台视觉重构
- 阶段目标：回应用户对当前界面吸引力的质疑，按 `frontend-design` 技能把页面从松散卡片堆重构为更紧凑、可信的工业化量化研究终端。
- 实际操作：
  - 修改 `frontend/src/main.jsx`，在顶部品牌区加入当前工作区、日线数量和数据来源状态，并给量化/基线视图的工作区添加宽屏布局标记。
  - 修改 `frontend/src/main.jsx`，修复 `GatePill` 与 `RobustnessGateCard` 列表渲染中把 React `key` 当普通组件 prop 传入导致的类型检查问题。
  - 修改 `frontend/src/styles.css`，重做全局色板、顶部横向 tab 横幅、命令控制台、工作区网格、量化全景 hero、面板质感和响应式规则。
  - 重启 `frontend` 容器，刷新 Vite 开发服务缓存。
- 验证结果：
  - `.\scripts\check-file.ps1 frontend\src\main.jsx` 通过。
  - `.\scripts\check-file.ps1 frontend\src\styles.css` 通过。
  - `docker compose run --rm frontend npm run build` 通过。
  - 浏览器打开 `http://localhost:15173/?view=quant`，确认顶部 `6` 个 tab 横向铺开，header 高度约 `93px`，命令区约 `130px`，工作区从约 `256px` 开始，第一屏大面积空白已消失。
  - 浏览器打开 `http://localhost:15173/?view=screen`，确认自选标的池横幅为双列布局。
  - 浏览器在 `量化全景` 中切换策略到 `MACD金叉`，标题变为 `MACD金叉 全景`，策略选择仍可用。
- 后续事项：浏览器截图接口本轮多次超时，但 DOM 布局和交互检查已通过；若继续打磨，可下一轮专门做图表/表格的信息密度和移动端截图复核。

## 2026-05-30 22:41 +08:00 - Tab2 全量回测结果与资本尾部审计
- 阶段目标：按用户要求，让 `量化全景` 明确展示全量回测结果和图像；同时补齐组合回测的资本加权尾部诊断字段，避免只看单票百分比亏损。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，修复 `execute_buy` 已传入但未接收 `portfolio_equity_before_entry` 的问题；新增完成交易的 `entryCost`、`netProceeds`、`netPnl`、`capitalReturnPct`、`pnlPctOfEntryEquity`，并新增 `capitalBottom10` / `tailCapitalRisk` 诊断。
  - 执行 `086-portfolio-cross-section-risk8-pos135-independent-breadth-baseline-capital-audit`，复现 `072` 合格基线，不改变策略规则。
  - 修改 `backend/app/main.py`，可执行策略接口返回 `summary`、`scope`、完整 `analysis`、全量成交标的审计行、资本尾部诊断、全量交易动作、完成交易和最终持仓。
  - 修改 `frontend/src/main.jsx` 和 `frontend/src/styles.css`，Tab2 新增组合基线全量成交标的表、最终持仓表、资本收益/组合影响列、全量交易流水代码/费用/成交规则列、全市场结果导出和组合基线 JSON 导出；收益分布、收益-回撤散点和排名曲线在未运行全市场验证时使用组合基线全量成交标的。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `086` 目标硬门槛继续通过：总收益约 `68.92%`，年化约 `19.11%`，最大回撤约 `-4.16%`，盈亏比约 `4.00:1`。
  - `086` 新增资本尾部：最差单个成交标的对入场前组合权益影响约 `-0.69%`，资本后 10 合计约 `-6.84%`。
  - `python -m py_compile backend\app\main.py scripts\research\run_portfolio_backtest.py` 通过。
  - `scripts\check-file.ps1` 针对 `backend\app\main.py`、`scripts\research\run_portfolio_backtest.py`、`frontend\src\main.jsx`、`frontend\src\styles.css` 通过。
- 后续事项：`086` 是展示和审计口径补强，不代表解决 `076/085` 的跳空开盘成交压力；下一轮优化仍应围绕持仓过夜前的风险标签、隔夜仓位预算或真实涨跌停/停牌成交模型。

## 2026-05-30 23:01 +08:00 - 行业隔夜风险标签负证据
- 阶段目标：在 `076` 成交现实性压力口径下，验证开仓前行业隔夜风险标签是否能修复首次隔夜跳空导致的成交标的后 10 尾部亏损/回撤。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增默认关闭的实验参数：`--industry-overnight-risk-window-days`、`--industry-overnight-risk-gap-down-pct`、`--industry-overnight-risk-min-count`、`--industry-overnight-risk-min-ratio`。
  - 组合回测按行业滚动统计近期隔夜低开事件；当行业低开事件数和比例同时达到阈值时，暂停该行业新开仓，并输出 `industryOvernightRiskActiveDays`、`industryOvernightRiskActiveIndustryDays`、`blockedIndustryOvernightRiskSignals`。
  - 修正集中度减仓时未写入 `realizedGross` / `realizedFees` 的资本审计口径。
  - 执行 `087-portfolio-cross-section-risk8-pos135-industry-overnight-risk20-slip10bp-gap`：0.1% 买卖滑点、跳空止损按开盘成交、近 20 个行业样本日、行业内 `<= -3%` 低开事件不少于 `8` 且占比不低于 `8%`。
  - 执行 `088-portfolio-cross-section-risk8-pos135-industry-overnight-risk20-sensitive-slip10bp-gap`：同一压力口径，阈值加严到不少于 `5` 且占比不低于 `5%`。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `087` 未通过：总收益约 `62.65%`，最大回撤约 `-4.54%`，尾部最差约 `-10.41%`，尾部亏损/回撤违规 `4` 个；行业风险标签活跃 `285` 天，拦截信号 `11054` 个。
  - `088` 未通过：总收益约 `42.35%`，最大回撤约 `-5.73%`，尾部最差仍约 `-10.41%`，尾部亏损/回撤违规 `4` 个；行业风险标签活跃 `522` 天，拦截信号 `48813` 个。
  - `python -m py_compile scripts\research\run_portfolio_backtest.py` 通过。
  - `scripts\check-file.ps1 .\scripts\research\run_portfolio_backtest.py` 通过。
- 后续事项：行业隔夜风险标签有弱信号，但不足以跨过 `10%` 尾部红线；继续加严会明显压低收益。下一轮优先验证隔夜仓位预算，或建立更真实的涨跌停/停牌成交模型。

## 2026-05-30 23:08 +08:00 - Tab2 全量回测结果覆盖说明
- 阶段目标：按用户补充要求，让 `量化全景` 更明确地展示“全量回测结果和图像”的覆盖范围，而不是只像摘要看板。
- 实际操作：
  - 修改 `backend/app/main.py`，在可执行策略接口返回基线证据 `results.json` 与 `review.md` 的仓库相对路径。
  - 修改 `frontend/src/main.jsx`，在 Tab2 新增 `回测结果覆盖` 面板，展示权益图像交易日数、全量交易动作、完成交易、标的审计、最终持仓、验证窗口、证据 run、来源 run 和结果文件路径。
  - 修改 `frontend/src/styles.css`，补充覆盖面板的高密度网格样式和移动端响应式规则。
  - 重启 `frontend` 容器，刷新 Vite 开发服务缓存。
- 验证结果：
  - `python -m py_compile backend\app\main.py` 通过。
  - `.\scripts\check-file.ps1 .\backend\app\main.py` 通过。
  - `.\scripts\check-file.ps1 .\frontend\src\main.jsx` 通过，包含 `eslint`、`tsc --noEmit` 和 `vite build`。
  - `.\scripts\check-file.ps1 .\frontend\src\styles.css` 通过，包含 `tsc --noEmit` 和 `vite build`。
  - `docker compose run --rm frontend npm run build` 通过。
  - 浏览器打开 `http://localhost:15173/?view=quant` 并点击 `载入基线` 后，`回测结果覆盖` 面板显示：权益图像 `726`、交易动作 `323`、完成交易 `136`、标的审计 `132`，结果文件为 `docs/research/runs/086-portfolio-cross-section-risk8-pos135-independent-breadth-baseline-capital-audit/results.json`。
- 后续事项：Tab2 现在会明确告诉用户当前完整基线 run 覆盖了多少图像点和明细行；下一轮可继续把 `087/088` 等压力实验做成可切换历史证据列表，或回到隔夜仓位预算实验。

## 2026-05-30 23:24 +08:00 - 隔夜仓位预算负证据
- 阶段目标：在 `076` 成交现实性压力口径下，验证组合层隔夜持仓预算是否能修复首次隔夜跳空导致的成交标的后 10 尾部亏损/回撤。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增默认关闭的 `--max-overnight-exposure-pct` 实验参数，并在普通收盘买入和次日开盘买入两条路径上按预计隔夜持仓市值缩量或拦截。
  - 回测结果新增 `maxOvernightExposurePct`、`blockedOvernightBudgetSignals`、`overnightBudgetReducedEntries`、`overnightBudgetReductionShares`。
  - 执行 `089-portfolio-cross-section-risk8-pos135-overnight-budget27-slip10bp-gap`：0.1% 买卖滑点、跳空止损按开盘成交、单票建仓上限 13.5%、隔夜预算 27%。
  - 执行 `090-portfolio-cross-section-risk8-pos135-overnight-budget20-slip10bp-gap`：同一压力口径，隔夜预算 20%。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `089` 未通过：总收益约 `59.55%`，最大回撤约 `-5.12%`，尾部最差约 `-11.63%`，尾部亏损/回撤违规 `5` 个；隔夜预算拦截 `6` 个信号、缩量 `22` 次、缩减 `14800` 股。
  - `090` 未通过：总收益约 `49.65%`，最大回撤约 `-3.83%`，尾部最差仍约 `-11.63%`，尾部亏损/回撤违规增至 `6` 个；隔夜预算拦截 `62` 个信号、缩量 `75` 次、缩减 `57700` 股。
  - `090` 的资本后 10 合计组合影响约 `-10.09%`，较 `089` 有改善，但不足以替代单标的百分比尾部失败。
- 后续事项：隔夜仓位预算不进入当前基线；下一轮优先建立更真实的涨跌停/停牌/无法按开盘价成交模型，或明确拆分资本风险与单标的百分比风险的目标口径。

## 2026-05-30 23:32 +08:00 - 量化全景切换为研究运行证据
- 阶段目标：按用户补充要求，让 `量化全景` 呈现 `docs/research/runs/` 下的研究运行结果，而不是原本预设的常规策略视角。
- 实际操作：
  - 修改 `backend/app/main.py`，新增研究运行目录读取能力和 `/api/research/runs`、`/api/research/runs/{run_id}` 接口，聚合 `results.json`、研究假设、运行指标、权益曲线、交易动作、标的审计和证据文件路径。
  - 修改 `frontend/src/main.jsx`，移除量化全景中的策略预设选择，改为研究运行下拉选择、运行证据 hero、研究门槛审计、标的审计、交易动作和完成交易视图。
  - 修改 `frontend/src/main.jsx`，把左侧量化页摘要从“当前策略”改为 `研究运行索引`，默认展示最新 `090` run 的收益、回撤和完成交易样本。
  - 修改 `frontend/src/styles.css`，调整量化全景 hero、运行证据卡片、门槛审计和图像区的高密度布局。
  - 重启 `frontend` 容器，刷新 Vite 开发服务缓存。
- 验证结果：
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py` 通过。
  - `.\scripts\check-file.ps1 backend\app\main.py` 通过。
  - `.\scripts\check-file.ps1 frontend\src\main.jsx` 通过，包含 `eslint`、`tsc --noEmit` 和 `vite build`。
  - `.\scripts\check-file.ps1 frontend\src\styles.css` 通过，包含 `tsc --noEmit` 和 `vite build`。
  - `docker compose run --rm frontend npm run build` 通过。
  - API 验证 `http://localhost:18000/api/research/runs?limit=5` 返回最新 `090`、`089`、`088` 等研究运行。
  - 浏览器验证 `http://localhost:15173/?view=quant`：页面显示 `TAB2 RESEARCH RUN PANORAMA`、`研究运行索引`、`研究运行标的审计` 和 `090-portfolio-cross-section-risk8-pos135-overnight-budget20-slip10bp-gap`；旧文案 `TAB2 QUANT STRATEGY SURFACE`、`当前策略`、`当前标的图像`、`全量回测结果` 均未出现。
- 后续事项：当前量化全景已对齐研究运行目录；后续可继续把 `review.md`、`next-input.md` 的摘要做成运行详情中的独立复盘/下一步面板。

## 2026-05-30 23:36 +08:00 - 涨跌停成交现实性压力负证据
- 阶段目标：在 `076` 成交现实性压力口径下，验证跌停附近止损无法按开盘价及时成交的更保守假设，并判断它是否能作为下一步策略优化方向。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增默认关闭的 `--limit-down-stop-delay` 与 `--limit-band-tolerance-pct`，按 A 股常见 10%/20% 涨跌幅限制推断主板、创业板和科创板样本的跌停附近状态。
  - 止损跳空且开盘价接近跌停时，不再假设立即按开盘价卖出，而是记录 `limitDownStopDelayEvents` 并延迟到后续交易日继续评估。
  - 执行 `091-portfolio-cross-section-risk8-pos135-limitdown-delay-slip10bp-gap`：0.1% 买卖滑点、跳空止损按开盘成交、单票建仓上限 13.5%、启用跌停附近止损延迟。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `091` 未通过：总收益约 `58.84%`，年化约 `16.69%`，最大回撤约 `-6.50%`，盈亏比约 `3.90:1`，完成交易 `136` 笔。
  - 跌停附近止损延迟触发 `4` 次；成交标的尾部最差收益/回撤扩大到约 `-14.70%`，尾部亏损/回撤违规 `4` 个。
  - 资本尾部也未过线：最差单标的组合影响约 `-1.99%`，资本后 10 合计约 `-11.17%`。
- 后续事项：`091` 是更严格成交审计，不进入当前基线。下一轮应把涨跌停、停牌和排队成交模型做成常设压力口径，并分析 091 延迟样本的行业、分数结构和流动性共性。

## 2026-05-31 00:03 +08:00 - 跌停延迟可审计事件与高开过滤边界
- 阶段目标：让跌停附近止损延迟事件进入全量交易流水，并验证高开/振幅入场过滤能否在更严格成交压力下修复尾部。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，买入时把入场分数和风险指标保存在持仓上；遇到 `limit_down_stop_delay` 时追加 `止损延迟` 明细行，不改变现金、持仓和收益计算。
  - 执行 `092-portfolio-cross-section-risk8-pos135-limitdown-delay-audit-slip10bp-gap`，复现 `091` 指标，并让 `4` 次跌停附近止损延迟出现在全量交易动作中。
  - 分析 `092` 延迟样本，确认 4 次事件平均入场分数约 `9.19`，部分样本入场日高开接近或超过 `9.7%`。
  - 执行 `093/094/095`，分别测试 `5%/8%/6%` 入场高开硬过滤；执行 `096/097`，在 `6%` 高开过滤上叠加 `6%/6.3%` 入场日振幅过滤。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `092` 指标与 `091` 一致：总收益约 `58.84%`，最大回撤约 `-6.50%`，尾部最差约 `-14.70%`；交易动作数由 `321` 增至 `325`，新增 `4` 条可审计延迟事件。
  - `093` 高开 `5%` 过滤尾部改善到约 `-10.66%`，但总收益只有约 `26.91%`，未达收益门槛。
  - `094` 高开 `8%` 过滤总收益约 `42.65%`，但尾部最差仍约 `-11.63%`。
  - `095` 高开 `6%` 过滤总收益约 `43.84%`、最大回撤约 `-6.72%`、资本后 10 合计约 `-8.59%`，但尾部仍有 `1` 个样本约 `-10.66%`，未通过。
  - `096/097` 叠加入场日振幅过滤后尾部亏损/回撤通过，最差约 `-9.21%`，但总收益降到约 `18.09%/15.37%`，不进入基线。
- 后续事项：硬过滤已经到边界。下一轮应验证高开/高振幅信号降权或缩仓，而不是继续直接屏蔽样本。

## 2026-05-31 00:19 +08:00 - 入场风险降权与日内涨幅过滤负证据
- 阶段目标：在跌停延迟压力口径下，验证比硬过滤更温和的高开/振幅打分降权，以及更窄的入场日内涨幅过滤，是否能保住收益并修复 `095` 剩余尾部。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增默认关闭的 `entryScorePenalty` 覆盖参数：`--entry-gap-score-penalty-threshold-pct`、`--entry-gap-score-penalty`、`--entry-range-score-penalty-threshold-pct`、`--entry-range-score-penalty`。
  - 横截面择强打分时将入场高开/振幅罚分写入 `scoreParts.entryRiskPenalty`，便于后续审计；默认不启用，不改变当前基线。
  - 执行 `098-portfolio-cross-section-risk8-pos135-entry-gap6-range63-scorepen2-limitdelay-slip10bp-gap`：高开硬过滤 `6%`，振幅超过 `6.3%` 扣 `2` 分。
  - 执行 `099-portfolio-cross-section-risk8-pos135-entry-gap6-intraday6-limitdelay-slip10bp-gap` 与 `100-portfolio-cross-section-risk8-pos135-entry-gap6-intraday65-limitdelay-slip10bp-gap`：高开硬过滤 `6%`，分别限制入场日内涨幅不超过 `6%/6.5%`。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `098` 未通过：总收益约 `16.90%`，最大回撤约 `-9.39%`，尾部最差约 `-28.73%`，并引入 `603773.SH` 新极端尾部。
  - `099` 未通过：尾部亏损/回撤通过，最差约 `-9.21%`，但总收益只有约 `22.70%`。
  - `100` 未通过：尾部亏损/回撤通过，最差约 `-9.21%`，但总收益只有约 `22.42%`。
- 后续事项：排序降权和日内涨幅硬过滤都不进入基线。下一轮应验证仓位缩放/资本风险目标，或引入新的风险信息，而不是继续微调价量形态硬阈值。

## 2026-05-31 00:32 +08:00 - 入场风险缩仓边界证据
- 阶段目标：在跌停延迟压力口径下，验证高风险入场信号缩仓是否能在保留收益的同时改善组合资本风险，并判断它与单标的百分比尾部硬门槛的关系。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增默认关闭的 `entrySizeHaircut` 覆盖参数：`--entry-gap-size-haircut-threshold-pct`、`--entry-gap-size-haircut-pct`、`--entry-range-size-haircut-threshold-pct`、`--entry-range-size-haircut-pct`、`--entry-intraday-size-haircut-threshold-pct`、`--entry-intraday-size-haircut-pct`。
  - 回测统计新增 `entrySizeHaircutReducedEntries` 和 `entrySizeHaircutReductionShares`，并写入 `review.md` / `next-input.md`。
  - 执行 `101-portfolio-cross-section-risk8-pos135-entry-gap6-risk-haircut50-limitdelay-slip10bp-gap`：高开硬过滤 `6%`，入场日振幅超过 `6.3%` 或日内涨幅超过 `6%` 时缩仓 `50%`。
  - 执行 `102-portfolio-cross-section-risk8-pos135-entry-gap6-risk-haircut40-limitdelay-slip10bp-gap`：同口径缩仓 `40%`。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `101` 未通过：总收益约 `30.71%`，最大回撤约 `-4.20%`，资本后 10 合计约 `-8.38%`，但单标的百分比尾部仍约 `-10.66%`；缩仓 `83` 次，缩减 `48900` 股。
  - `102` 未通过：总收益约 `32.82%`，最大回撤约 `-4.28%`，资本后 10 合计约 `-8.37%`，但单标的百分比尾部仍约 `-10.66%`；缩仓 `83` 次，缩减 `40600` 股。
- 后续事项：入场风险缩仓比硬过滤更适合作为资本风险控制，但不能修复单笔百分比尾部。下一轮需要引入新的风险信息，或明确资本加权尾部与单标的百分比尾部的硬门槛层级。

## 2026-05-31 00:45 +08:00 - 完成交易净收益口径修正
- 阶段目标：复核 `102` 剩余尾部是否真实来自策略风险，还是来自完成交易收益指标没有纳入第一止盈减半后的已实现收益。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，把组合完成交易的 `returnPct` 改为整笔交易净盈亏 / 初始成本，并新增 `exitPriceReturnPct` 保存末次卖出价相对入场价的价格跌幅。
  - 完成交易结果补充 `entryScore`、`entryScoreParts` 和 `entryRiskMetrics`，便于后续定位尾部入场特征。
  - 修改 `frontend/src/main.jsx`，在 Tab2 全量完成交易表中把收益列标为“净收益”，并新增“末次价差”列。
  - 执行 `103-portfolio-cross-section-risk8-pos135-entry-gap6-risk-haircut40-netreturn-slip10bp-gap`，交易路径沿用 `102`，只重算完成交易收益和衍生指标。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `103` 目标硬门槛通过：总收益约 `32.82%`，最大回撤约 `-4.28%`，盈亏比约 `2.68:1`，完成交易 `130` 笔。
  - 成交标的后 10 最差净收益约 `-9.30%`，亏损/回撤违规为 `0`；资本后 10 合计组合影响仍约 `-8.37%`。
  - 严格诊断仍未通过：后 10 标的均为单笔样本，尾部盈亏比证据稀疏。
- 后续事项：下一轮优先对 `103` 做滚动窗口/分段复核，验证更严格成交压力和净收益口径在不同时间切片中是否稳健。

## 2026-05-31 01:20 +08:00 - 严格执行口径滚动窗口复核
- 阶段目标：按合格量化策略标准补上滚动窗口稳健性证据，并确认 `103/105` 这类三年全窗口通过结果是否能升级为稳健合格策略。
- 实际操作：
  - 修改 `scripts/research/run_window_validation.py`，让窗口验证支持入场风险过滤、入场缩仓、滑点、跳空止损开盘成交和跌停附近止损延迟等严格执行参数。
  - 窗口验证新增尾部最差收益、尾部亏损/回撤违规数和尾部硬门槛输出，避免只看窗口总收益。
  - 执行 `104-portfolio-cross-section-risk8-pos135-entry-gap6-risk-haircut40-netreturn-window-validation`、`105-portfolio-cross-section-risk8-pos135-entry-gap6-netreturn-limitdelay-slip10bp-gap`、`106-portfolio-cross-section-risk8-pos135-entry-gap6-netreturn-window-validation`、`107-portfolio-cross-section-risk8-pos135-netreturn-limitdelay-slip10bp-gap`、`108-portfolio-cross-section-risk8-pos135-entry-gap8-netreturn-limitdelay-slip10bp-gap`。
  - 修正 `frontend/src/main.jsx` 中 Tab2 “执行压力”卡片的展示字段，改为从 `costs` 和 `exit` 读取滑点、跳空止损和跌停延迟口径。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `105` 三年全窗口目标硬门槛通过：总收益约 `43.84%`，最大回撤约 `-6.72%`，盈亏比约 `2.62:1`，尾部最差净收益约 `-9.30%`。
  - `106` 滚动窗口未通过：7 个窗口只通过 3 个，失败窗口为 `Y1/Y2/R18-1/R18-2`，最低年化约 `0.32%`，最低 Sharpe 约 `0.06`。
  - `104` 风险缩仓滚动窗口也未通过：7 个窗口只通过 3 个，最低年化约 `2.70%`，最低 Sharpe 约 `0.56`。
  - `107/108` 证明放松高开过滤会重新引入尾部问题：去掉高开过滤尾部最差约 `-14.79%`，放宽到 `8%` 尾部最差约 `-11.72%`。
  - `python -m py_compile scripts\research\run_window_validation.py scripts\research\run_portfolio_backtest.py` 通过；`.\scripts\check-file.ps1` 检查相关脚本、文档、研究运行证据和 `frontend/src/main.jsx` 通过；`docker compose run --rm frontend npm run build` 通过。
  - API 验证 `105` 研究运行可返回全量数据：权益曲线 `726` 天、交易动作 `307` 条、完成交易 `130` 笔、成交标的审计 `129` 行；自动截图验证因当前 `node_repl` 环境缺少 Playwright 包未执行。
- 后续事项：`105` 只能作为当前三年全窗口候选，不能升级为稳健合格策略。下一轮应保留 `6%` 高开过滤和严格执行口径，集中修复 `Y1/Y2/R18-1/R18-2` 的早期窗口收益不足。

## 2026-05-31 01:53 +08:00 - 失败窗口风险归因与振幅边界负证据
- 阶段目标：解释 `105/106` 的滚动窗口失败原因，并验证能否通过市场宽度或买入日振幅边界恢复早期窗口收益。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py`，新增默认关闭的市场宽度覆盖参数：`--disable-market-breadth-filter`、`--market-min-samples`、`--market-min-above-ma20-pct`、`--market-min-above-ma60-pct`、`--market-min-up-pct`。
  - 修改 `scripts/research/run_portfolio_backtest.py` 和 `scripts/research/run_window_validation.py`，把 `blockedRiskSignals` 拆分为 `blockedRiskReasons`，让窗口复盘能看到风险拦截的具体原因。
  - 执行 `109-portfolio-cross-section-risk8-pos135-entry-gap6-no-breadth-netreturn-window-validation`：保留 `6%` 高开过滤和严格执行压力，关闭二次市场宽度闸门。
  - 执行 `110-portfolio-cross-section-risk8-pos135-entry-gap6-netreturn-window-riskreason-validation`：复跑 `105` 口径窗口验证，并输出风险拦截原因。
  - 执行 `111-portfolio-cross-section-risk8-pos135-entry-gap6-range9-netreturn-limitdelay-slip10bp-gap`：保留 `6%` 高开过滤，把买入日振幅上限从 `8%` 放宽到 `9%`。
  - 更新 `docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/README.md` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `109` 未通过：滚动窗口只通过 `1/7`，最低年化约 `0.85%`，最深窗口回撤约 `-10.49%`；关闭市场宽度闸门不是简单解。
  - `110` 未通过：滚动窗口仍通过 `3/7`。失败窗口的风险拦截主要来自买入日振幅，例如 `Y2` 振幅拦截 `3344` 次、高开拦截 `666` 次。
  - `111` 未通过：三年总收益约 `24.78%`，最大回撤约 `-7.08%`，盈亏比约 `2.53:1`，尾部最差净收益约 `-10.29%`，尾部亏损/回撤违规 `1` 个。
  - `python -m py_compile scripts\research\run_window_validation.py scripts\research\run_portfolio_backtest.py` 通过；相关脚本 `.\scripts\check-file.ps1` 通过。
- 后续事项：不要通过关闭市场宽度、放宽高开或放宽振幅来换收益。下一轮应分析 `Y1/Y2/R18-1/R18-2` 的行业暴露、分数分布、`entryScoreParts` 和 `entryRiskMetrics`，做排序因子消融或行业质量诊断。
## 2026-05-31 01:36 +08:00 - 当前最优策略筛票 Skill 封装
- 阶段目标：把当前最优 Risk8 横截面择强策略的本地筛票流程封装为 Codex Skill，便于后续按同一口径生成观察票和入场/出场参考。
- 实际操作：
  - 新增 `.codex/skills/a-share-risk8-watchlist/SKILL.md`，说明使用当前固化策略规格生成 A 股观察票，并强调 `105` 三年全窗口通过但 `106` 滚动窗口未通过。
  - 新增 `.codex/skills/a-share-risk8-watchlist/scripts/generate_watchlist.py`，脚本读取 `docs/research/executable-strategy-cross-section-risk8.json` 的 `evidenceRun`，复用项目现有策略、市场宽度、入场风险过滤和横截面排序函数。
  - 新增 `.codex/skills/a-share-risk8-watchlist/agents/openai.yaml`，提供 Skill 展示 metadata。
  - 默认排除 `300/301/688/689`，用于用户无创业板/科创板权限时生成观察清单。
- 验证结果：
  - `python -m py_compile .codex\skills\a-share-risk8-watchlist\scripts\generate_watchlist.py` 通过。
  - `python C:\Users\Windows\.codex\skills\.system\skill-creator\scripts\quick_validate.py .codex\skills\a-share-risk8-watchlist` 通过。
  - `.\scripts\check-file.ps1` 检查 Skill 文件、脚本、metadata 和 `操作日志.md` 通过。
  - 样例运行 `docker compose exec -T api python .codex/skills/a-share-risk8-watchlist/scripts/generate_watchlist.py --date 2026-05-29 --top 8` 通过，输出读取 `105` 证据口径、默认排除创业板/科创板，模式为 `OBSERVE_ONLY_MARKET_RISK_OFF`。
- 后续事项：后续若当前最优策略更新，只需先更新 `docs/research/executable-strategy-cross-section-risk8.json` 的 `evidenceRun` 和相关规格，Skill 脚本会跟随读取最新固化口径。

## 2026-05-31 02:09 +08:00 - 年化 30% 目标与观察级候选降级
- 阶段目标：把合格量化策略收益目标从旧“三年总收益 30%”上调为“年化收益 30%”，并按用户复盘意见把 `105` 降级为观察级候选，补齐状态分级、研究台账和纸面交易准备入口。
- 实际操作：
  - 修改 `docs/research/context.cross-section-strength-liquid-14pct-risk8.json`，新增 `target_annualized_return=0.3`，并把三年折算 `target_total_return` 更新为 `1.197`，保留旧目标为历史可比字段。
  - 修改 `scripts/research/run_portfolio_backtest.py` 和 `scripts/research/run_window_validation.py`，未来组合回测和滚动窗口验证优先使用年化目标判定。
  - 修改 `docs/research/executable-strategy-cross-section-risk8.json`、`docs/research/executable-strategy-cross-section-risk8.md`、`docs/research/README.md`、`docs/research/next-strategy-brief.md` 和 `AGENTS.md`，明确 `105` 为“观察”而非“可执行/合格”。
  - 新增 `docs/research/strategy-status-levels.md`、`docs/research/research-runs.json`、`docs/research/execution-readiness-plan.md`，记录状态分级、105-113 机器研究台账、纸面交易作业单/数据质量/运行时风控计划。
  - 修改 `backend/app/main.py`，让固化策略 API 使用规格文件里的新目标覆盖展示指标。
  - 修改 `frontend/src/main.jsx`，Tab2 量化页按年化目标展示策略状态、收益门槛和观察级候选状态。
- 验证结果：
  - `python -m py_compile backend\app\main.py scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 通过。
  - `docker compose run --rm frontend npm run build` 通过。
  - `.\scripts\check-file.ps1` 检查 `AGENTS.md`、后端、研究脚本、前端、研究文档、JSON 台账和 `操作日志.md` 通过。
  - `ConvertFrom-Json` 解析 `context.cross-section-strength-liquid-14pct-risk8.json`、`executable-strategy-cross-section-risk8.json`、`research-runs.json` 通过。
  - API `GET /api/strategies/executable/cross-section-strength-risk8` 返回 `status=observation_candidate`、`targetMet=false`、`targetAnnualizedReturn=0.3`、`annualizedReturn=0.1289`、`annualizedReturnMet=false`。
  - 已重启 `frontend` 容器；浏览器 `http://localhost:15173/?view=quant` 显示年化收益目标 `30%`，不再出现旧的“三年 >=30%”门槛，并把未过年化目标的研究运行显示为“观察”。
- 后续事项：下一阶段不要继续微调止损、高开、振幅或单一排序权重；优先实现失败窗口归因报告，比较失败/通过窗口的行业暴露、`entryScoreParts`、盈利/亏损评分组成，以及被风险过滤器挡掉票的事后表现。

## 2026-05-31 02:23 +08:00 - 长期目标与阶段推进制度设计
- 阶段目标：按用户新要求，把终极目标调整为年化收益 `100%`、最大回撤 `<10%`、盈亏比 `>=3:1`，并设计每达成一个阶段目标后新建下一阶段文件夹的推进制度。
- 实际操作：
  - 新增 `docs/research/long-term-goal.md`，定义终极目标、当前状态、分阶段门槛和阶段文件夹推进规则。
  - 新增 `docs/research/stages/README.md`，建立阶段索引。
  - 新增 `docs/research/stages/000-goal-system-design/README.md`，记录本次目标体系设计阶段已完成。
  - 新增 `docs/research/stages/001-observation-diagnosis/README.md` 和 `evidence.md`，作为下一步活跃阶段入口，目标是解释 `105/106/110` 失败窗口为什么没有稳定正期望。
  - 更新 `docs/research/README.md`、`docs/research/research-runs.json` 和 `AGENTS.md`，让长期目标和活跃阶段成为后续研究默认入口。
- 验证结果：
  - `ConvertFrom-Json` 解析 `docs/research/research-runs.json` 通过。
  - `.\scripts\check-file.ps1` 检查 `AGENTS.md`、研究 README、长期目标文档、阶段目录文档、机器台账和 `操作日志.md` 通过。
  - `git diff --check` 针对本阶段修改文件通过。
- 后续事项：下一阶段只推进 `001-observation-diagnosis`，先补失败窗口归因和排序因子消融，不直接调参冲年化 100%。

## 2026-05-31 02:31 +08:00 - AGENTS 长期目标主管规则收敛
- 阶段目标：按用户提醒，把 `AGENTS.md` 中残留的静态年化 30% 合格描述改成长期目标 + 阶段推进规则，避免后续 Agent 继续误用旧门槛。
- 实际操作：
  - 修改 `AGENTS.md`，新增“长期目标与阶段推进”章节，明确终极目标为年化 `>=100%`、最大回撤 `<10%`、盈亏比 `>=3:1`。
  - 修改 `AGENTS.md` 的“量化策略最低合格标准”，把收益/回撤/盈亏比硬门槛拆成当前阶段门槛和终极门槛。
  - 修改结论分类，把原来的笼统“通过”改为“阶段通过”，要求说明通过的是哪个阶段、是否能进入下一阶段。
- 验证结果：
  - `.\scripts\check-file.ps1 AGENTS.md` 通过。
  - `.\scripts\check-file.ps1 操作日志.md` 通过。
  - `git diff --check -- AGENTS.md 操作日志.md` 通过。
  - `rg` 确认 `AGENTS.md` 已包含“长期目标与阶段推进”“阶段通过”“年化收益 >=100%”等关键规则，旧的固定“通过=年化30%/2:1”结论口径已移除。
- 后续事项：后续策略研究必须先看 `docs/research/long-term-goal.md` 和 `docs/research/stages/README.md`，当前只推进 `001-observation-diagnosis`。

## 2026-05-31 02:29 +08:00 - 并行 Session 研究协议补充
- 阶段目标：判断现有阶段设计是否支持多个 session 并行研究；若不足，则在 `AGENTS.md` 中补充并行研究边界。
- 实际操作：
  - 检查 `AGENTS.md`、`docs/research/long-term-goal.md`、`docs/research/stages/README.md` 和 `docs/research/stages/001-observation-diagnosis/README.md`。
  - 结论：现有设计有阶段目录，但缺少 session 级隔离、共享文件合并和 run id 命名规则，不能安全并行。
  - 修改 `AGENTS.md`，新增“并行 Session 研究规则”，规定并行 session 必须写入 `docs/research/stages/<stage-id>/sessions/<session-id>/`，全局文件只能由整合 session 统一更新。
- 验证结果：
  - `.\scripts\check-file.ps1 AGENTS.md` 通过。
  - `.\scripts\check-file.ps1 操作日志.md` 通过。
  - `git diff --check -- AGENTS.md 操作日志.md` 通过。
  - `rg` 确认 `AGENTS.md` 已包含“并行 Session 研究规则”、`sessions/<YYYYMMDD-HHMM>-<short-topic>`、`session-log.md` 和“整合 session”规则。
- 后续事项：后续如启动并行研究，必须先创建 session 目录和 `session.md`，并让各 session 只写自己的证据；阶段总证据和机器台账由整合 session 合并。

## 2026-05-31 02:31 +08:00 - 当前仓库内容分批提交与推送
- 阶段目标：按用户要求，把当前仓库未提交内容按主题分批 commit，并推送到远端 `origin/main`。
- 实际操作：
  - 检查 `git status --short --branch`、`git remote -v` 和 `git fetch origin main`，确认当前分支为 `main`，远端为 `https://github.com/Jettlin927/Quantitative_trading.git`，本地提交前未落后远端。
  - 按主题创建提交：`f22291d docs: add research governance and evidence archive`、`6e3edce feat: add portfolio research backend workflows`、`7024748 feat: expand research workbench UI`、`8506ccf chore: add risk8 watchlist codex tooling`。
  - 检查未跟踪文件与 `.gitignore`，确认 `__pycache__` 和 `*.pyc` 已被忽略，没有纳入提交。
  - 对变更范围搜索 token/password/secret 等敏感模式，未发现真实凭据；命中的均为环境变量名、占位说明或研究 run id 文本。
- 验证结果：
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py scripts\research\run_portfolio_backtest.py scripts\research\run_research_round.py scripts\research\run_window_validation.py .codex\hooks\post_tool_use_check.py .codex\skills\a-share-risk8-watchlist\scripts\generate_watchlist.py` 通过。
  - `docker compose run --rm frontend npm run build` 通过。
  - `python C:\Users\Windows\.codex\skills\.system\skill-creator\scripts\quick_validate.py .codex\skills\a-share-risk8-watchlist` 通过。
  - 各批次提交前均执行 `git diff --cached --check`，未发现 whitespace error。
- 后续事项：追加本条日志后单独提交，再执行 `git push origin main`；推送完成后确认工作区状态。

## 2026-05-31 02:35 +08:00 - 001 阶段目标1失败窗口归因
- 阶段目标：完成 `001-observation-diagnosis` 的阶段性目标1，回答“失败窗口里排序选出的票为什么没有正期望”，并留下后续阶段可复用经验。
- 实际操作：
  - 读取 `docs/research/stages/README.md`、`docs/research/stages/001-observation-diagnosis/README.md` 和当前阶段 `evidence.md`，确认目标1对应验收条件 1。
  - 解析 `110-portfolio-cross-section-risk8-pos135-entry-gap6-netreturn-window-riskreason-validation/results.json`，参照 `105/106`，统计失败窗口的窗口指标、评分分层、盈利/亏损评分组成、行业贡献和风险过滤计数。
  - 追加 `docs/research/stages/001-observation-diagnosis/evidence.md`，记录目标1结论：失败窗口主要问题不是尾部破线，而是弱/窄行情下排序辨别力不足，放量强势票经常代表情绪末端，正收益集中在少数行业主题。
- 验证结果：
  - 已完成证据解析，未跑新回测，未修改策略参数、回测语义、数据库 volume 或端口配置。
  - `.\scripts\check-file.ps1 docs\research\stages\001-observation-diagnosis\evidence.md` 通过。
  - `.\scripts\check-file.ps1 操作日志.md` 通过。
  - `git diff --check -- docs/research/stages/001-observation-diagnosis/evidence.md 操作日志.md` 通过；仅提示工作区 LF 未来可能被 Git 转为 CRLF。
- 后续事项：阶段整体仍未验收；下一步应继续做评分组成稳定性、行业暴露归因、风险过滤反事实和排序因子消融。

## 2026-05-31 02:44 +08:00 - 001 阶段目标2评分组成差异
- 阶段目标：完成 `001-observation-diagnosis` 的阶段性目标2，回答“哪些评分组成在盈利交易和亏损交易之间有稳定差异”，并把结论服务于长期年化 `>=100%`、最大回撤 `<10%`、盈亏比 `>=3:1` 的终极目标。
- 实际操作：
  - 按当前活跃阶段约束，只做 `105/110` 已有结果解析，不跑新回测，不调整止损止盈、仓位、成交压力或排序参数。
  - 查看 `scripts/research/run_portfolio_backtest.py` 中 `apply_cross_section_strength_scores`，确认 `entryScoreParts` 的默认评分权重和惩罚项语义。
  - 统计 `110` 的 `Y1/Y2/Y3/R18-*` 窗口以及 `105` 三年全窗口中盈利/亏损交易的 `return20Rank`、`return60Rank`、`high60Rank`、`recovery20Rank`、`volumeRank`、`baseScore` 差异和收益相关性。
  - 追加 `docs/research/stages/001-observation-diagnosis/evidence.md`：结论为 `high60Rank` 最稳定正向，`baseScore` 是辅助正向，`volumeRank` 不是稳定正向；`return20Rank/return60Rank/recovery20Rank` 暂不能作为下一阶段核心变量。
- 验证结果：
  - 已完成证据解析，未修改业务代码、策略语义、数据库 volume 或端口配置。
  - `.\scripts\check-file.ps1 docs\research\stages\001-observation-diagnosis\evidence.md` 通过。
  - `.\scripts\check-file.ps1 操作日志.md` 通过。
  - `git diff --check -- docs/research/stages/001-observation-diagnosis/evidence.md 操作日志.md` 通过；仅提示工作区 LF 未来可能被 Git 转为 CRLF。
- 后续事项：阶段整体仍未验收；下一步应继续做行业暴露归因、风险过滤反事实和排序因子消融。

## 2026-05-31 02:47 +08:00 - 长期阶段路线预设补齐
- 阶段目标：按用户提醒，提前锁定从观察诊断到终极目标、纸面交易准备和实盘前验证的完整阶段性目标，避免后续研究路线漂移。
- 实际操作：
  - 更新 `docs/research/long-term-goal.md`，把阶段路线扩展为 `000` 至 `007`，逐阶段写明核心问题、硬指标、必须产物和进入下一阶段条件。
  - 更新 `docs/research/stages/README.md`，在阶段索引中列出未来 `002` 至 `007` 的预设阶段，并说明未来目录只在上一阶段验收通过后创建。
  - 更新 `AGENTS.md`，明确 `docs/research/long-term-goal.md` 是阶段顺序、阶段名称和硬门槛的唯一来源，session 不得自行改名、插队、重排或跳级。
  - 更新 `docs/research/README.md`，把长期目标描述改为同时指向完整阶段路线。
- 验证结果：
  - `.\scripts\check-file.ps1 docs\research\long-term-goal.md` 通过。
  - `.\scripts\check-file.ps1 docs\research\stages\README.md` 通过。
  - `.\scripts\check-file.ps1 AGENTS.md` 通过。
  - `.\scripts\check-file.ps1 docs\research\README.md` 通过。
  - `.\scripts\check-file.ps1 操作日志.md` 通过。
  - `git diff --check -- docs/research/long-term-goal.md docs/research/stages/README.md AGENTS.md docs/research/README.md 操作日志.md` 通过；仅提示工作区 LF 未来可能被 Git 转为 CRLF。
- 后续事项：继续按 `001-observation-diagnosis` 推进，未来只有在当前阶段验收通过并写完 `next-stage.md` 后才创建 `002-candidate-repair-30/`。

## 2026-05-31 02:53 +08:00 - 001 阶段验收并进入 002
- 阶段目标：完成 `001-observation-diagnosis` 剩余验收问题，判断是否可以进入 `002-candidate-repair-30`，同时保持目标终点 `004-high-return-frontier-75` 的阶段推进边界。
- 实际操作：
  - 解析 `110` 的窗口完成交易，补齐行业暴露归因：失败窗口收益依赖软件服务、通信设备、证券、多元金融等少数主题，同时半导体、汽车整车、化工原料、小金属、航空等行业持续拖累。
  - 解析 `107/108/111` 风险过滤反事实：放宽缺口会引入右尾但重新破尾部硬线；放宽振幅新增样本整体负期望，说明振幅过滤主要保护收益。
  - 解析 `112/113` 成交量权重消融并结合评分组成差异，确认下一阶段应做行业状态、`high60Rank/baseScore` 质量排序和非线性拥挤风险处理，而不是继续单项调参。
  - 追加 `docs/research/stages/001-observation-diagnosis/evidence.md`，新增目标3/4/5证据，并创建 `docs/research/stages/001-observation-diagnosis/next-stage.md`。
  - 新建 `docs/research/stages/002-candidate-repair-30/README.md`，更新 `docs/research/stages/README.md`、`docs/research/long-term-goal.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`，把活跃阶段推进到 `002`。
- 验证结果：
  - 已完成证据解析，未跑新回测，未修改策略代码、核心回测语义、数据库 volume、端口或凭据。
  - `python -m json.tool docs\research\research-runs.json` 通过。
  - `.\scripts\check-file.ps1` 已检查 `docs\research\stages\001-observation-diagnosis\evidence.md`、`README.md`、`next-stage.md`、`docs\research\stages\002-candidate-repair-30\README.md`、`docs\research\stages\README.md`、`docs\research\long-term-goal.md`、`docs\research\research-runs.json`、`docs\research\next-strategy-brief.md`，均通过。
  - `git diff --check -- docs/research/stages/001-observation-diagnosis/evidence.md docs/research/stages/001-observation-diagnosis/README.md docs/research/stages/001-observation-diagnosis/next-stage.md docs/research/stages/002-candidate-repair-30/README.md docs/research/stages/README.md docs/research/long-term-goal.md docs/research/research-runs.json docs/research/next-strategy-brief.md` 通过；仅提示工作区 LF 未来可能被 Git 转为 CRLF。
- 后续事项：当前活跃阶段为 `002-candidate-repair-30`。下一步应先实现并验证一个结构性修复 run，优先方向是行业状态过滤/行业内排序，不得跳过 `002/003` 直接推进 `004`。

## 2026-05-31 03:27 +08:00 - 002 第一组修复实验
- 阶段目标：推进 `002-candidate-repair-30`，把 `001` 形成的缺口预算、行业状态和 `high60/baseScore` 质量排序假设转成可复现 run，检查是否接近年化 `>=30%`、最大回撤 `<10%`、盈亏比 `>=2:1`、滚动窗口 `5/7`。
- 实际操作：
  - 新增 `scripts/research/run_portfolio_backtest.py` 和 `scripts/research/run_window_validation.py` 的 opt-in 参数，支持覆盖失败节流阈值和完整横截面评分权重；默认行为不变。
  - 运行 `002-repair-gap-budget-001`：缺口放宽到 `8%`，`>6%` 缺口降仓 `50%`。结果年化 `12.74%`、最大回撤 `-4.75%`、盈亏比 `2.64:1`，但尾部最差 `-11.72%` 破线。
  - 运行 `002-repair-industry-loss-throttle-001`：行业一周 `1` 次亏损后冷却 `20` 天。结果年化 `9.26%`、最大回撤 `-6.65%`、盈亏比 `2.40:1`、尾部最差 `-9.30%`，但收益被明显压低。
  - 运行 `002-repair-high60-quality-001`：`return20=1.0`、`return60=0.5`、`high60=3.0`、`recovery20=0.5`、`volume=0.25`、`base=0.4`。结果年化 `16.08%`、最大回撤 `-6.18%`、盈亏比 `2.49:1`、尾部最差 `-8.19%`。
  - 运行 `002-repair-high60-quality-001-window-validation`，滚动窗口仍只通过 `3/7`，失败窗口为 `Y1/Y2/R18-1/R18-4`，最差回撤 `-10.95%`。
  - 运行 `002-repair-high60-gap8-001`：质量排序基础上缺口放宽到 `8%`。结果年化 `17.73%`、最大回撤 `-6.46%`、盈亏比 `2.68:1`，但尾部最差 `-11.72%` 再次破线。
  - 新增 `docs/research/stages/002-candidate-repair-30/evidence.md`，更新 `docs/research/research-runs.json` 与 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机 Python 因缺少 `psycopg` 无法直接跑数据库脚本，已改用 `api` 容器执行；未修改数据库 volume 或持久化行情数据。
  - `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 通过。
  - `docker compose exec -T api python -m py_compile scripts/research/run_portfolio_backtest.py scripts/research/run_window_validation.py` 通过。
  - `.\scripts\check-file.ps1 scripts\research\run_portfolio_backtest.py` 通过。
  - `.\scripts\check-file.ps1 scripts\research\run_window_validation.py` 通过。
  - `python -m json.tool docs\research\research-runs.json` 通过。
  - `.\scripts\check-file.ps1 docs\research\stages\002-candidate-repair-30\evidence.md`、`docs\research\research-runs.json`、`docs\research\next-strategy-brief.md` 均通过。
- 后续事项：`002` 仍为 active，未通过。下一步应实现入场前行业状态过滤或行业内排序，优先解决 `Y1/Y2/R18-1` 的低收益；不要继续放宽缺口，也不要直接扩大仓位。

## 2026-05-31 03:28 +08:00 - 并行研究阶段 Web 工作台 MVP
- 阶段目标：把当前阶段研究证据呈现到 Web，区分正式整合 run、并行 session 和待整合证据，避免把另一个 research session 的半成品误算作阶段结论。
- 实际操作：
  - 修改 `backend/app/main.py`，新增只读 `GET /api/research/overview` 与 `GET /api/research/sessions/{session_id}`，从 `docs/research/research-runs.json`、当前阶段 `README.md`、`sessions/` 和当前阶段未整合 run 目录汇总研究状态。
  - 修改 `frontend/src/main.jsx`，新增 `研究阶段` 页签，展示阶段闸门、正式 run 对比账本、并行 session 空/进行中状态和证据收件箱；待整合 run 明确标注“不参与阶段通过结论”。
  - 修改 `frontend/src/styles.css`，按工业化风控终端风格补充研究阶段布局、闸门、session 卡片、待整合证据表格和响应式规则。
  - 重启 `frontend` 容器以清除 Vite 旧模块缓存；临时启动过 `15174` preview 辅助截图验证，验证后已删除临时容器。
- 验证结果：
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py` 通过。
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 backend\app\main.py` 通过。
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 frontend\src\main.jsx` 通过。
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-file.ps1 frontend\src\styles.css` 通过。
  - `docker compose run --rm frontend npm run build` 通过。
  - `GET http://localhost:18000/api/research/overview` 返回当前阶段 `002-candidate-repair-30`、正式 run `9` 条、session `0` 条、待整合 run `4` 条。
  - Browser 打开 `http://localhost:15173/?view=stage`，确认页面包含 `研究阶段`、`002-candidate-repair-30`、`证据收件箱` 和 `002-repair` 待整合证据，无前端 error/warn 日志。
- 后续事项：如果另一个 research session 后续创建 `docs/research/stages/002-candidate-repair-30/sessions/<session-id>/` 并写入 `session.md`/`evidence.md`，本页会自动显示；阶段是否通过仍需整合 session 更新全局证据后判断。

## 2026-05-31 03:58 +08:00 - 安装 A 股数据相关 Codex skills
- 阶段目标：把用户指定的两个外部 skill 安装到本仓库 `.codex/skills`，供后续本地研究会话识别。
- 实际操作：
  - 使用 `skill-installer` 检查并安装 `simonlin1212/a-stock-data` 根目录 skill，目标目录为 `.codex/skills/a-stock-data/`。
  - 检查 `waditu-tushare/skills` 仓库结构，按上游 README 推荐安装 `tushare-data` 子目录，目标目录为 `.codex/skills/tushare-data/`。
  - 未修改 `.env`、token、数据库 volume、端口或核心回测语义。
- 验证结果：
  - 已读取 `.codex/skills/a-stock-data/SKILL.md` 与 `.codex/skills/tushare-data/SKILL.md` 的 frontmatter，确认 `name` 分别为 `a-stock-data` 和 `tushare-data`。
- 后续事项：重启 Codex 后新安装的本仓库 skills 才会被新会话加载。

## 2026-05-31 04:24 +08:00 - 002 复合指标因子修复实验
- 阶段目标：根据用户提醒，把 002 下一轮研究从单纯 MA/缺口/行业状态修补调整为复合指标因子，验证 MACD、BOLL、RSI、MA 结构能否作为横截面排序增益。
- 实际操作：
  - 扩展 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py` 的 opt-in 横截面评分权重，新增 `macdHist`、`bollSqueeze`、`bollPosition`、`rsiBalance`、`maAlignment`；默认权重为 `0`，旧 run 口径不变。
  - 运行 `002-repair-high60-industry-state-001` 与滚动验证：全窗口年化 `19.98%`、最大回撤 `-6.07%`、盈亏比 `2.52:1`，但滚动仍 `3/7`。
  - 运行 `002-repair-indicator-composite-001`：强势指标线性叠加导致 `688018.SH` 跳空止损，尾部最差 `-32.21%`，判定为负证据。
  - 运行 `002-repair-indicator-composite-002`：保守复合因子尾部可控，但年化降至 `6.29%`。
  - 运行 `002-repair-indicator-composite-003`：轻量复合因子全窗口年化 `20.91%`、最大回撤 `-4.83%`、盈亏比 `2.54:1`、尾部最差 `-8.19%`。
  - 运行 `002-repair-indicator-composite-003-window-validation`：滚动窗口 `5/7` 通过，失败窗口为 `Y1/R18-1`，最低年化 `2.09%`、最深回撤 `-5.86%`。
  - 运行 `002-repair-indicator-composite-004`：行业状态 MA20 宽度阈值从 `50%` 放到 `45%` 后无明显增益，年化仍约 `20.90%`。
  - 更新 `docs/research/stages/002-candidate-repair-30/README.md`、`evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机与 `api` 容器内 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 均通过。
  - `docker compose exec -T api python scripts/research/run_portfolio_backtest.py --help` 已确认新增复合指标参数可见。
  - `.\scripts\check-file.ps1 scripts\research\run_portfolio_backtest.py` 与 `scripts\research\run_window_validation.py` 通过。
- 后续事项：`002` 仍为 active，未通过。下一步应对 `002-repair-indicator-composite-003` 做因子消融与过热降分，不得用扩大仓位、取消滑点或放宽缺口/振幅硬线补收益。

## 2026-05-31 04:46 +08:00 - 002 复合指标因子消融
- 阶段目标：对 `002-repair-indicator-composite-003` 做单因子消融，确认 MACD、BOLL、RSI、MA 结构在复合排序中的真实贡献。
- 实际操作：
  - 运行 `002-repair-indicator-ablate-macd-001`：去掉 `macdHist` 后年化 `20.11%`，低于基准 `20.91%`，说明 MACD 小权重有轻微正贡献。
  - 运行 `002-repair-indicator-ablate-boll-001`：去掉 `bollSqueeze` 后年化 `19.73%`，说明 BOLL 收口有正贡献。
  - 运行 `002-repair-indicator-ablate-rsi-001`：去掉 `rsiBalance` 后年化 `17.94%`、最大回撤 `-6.04%`，说明 RSI 均衡是关键贡献项。
  - 运行 `002-repair-indicator-ablate-ma-001`：去掉 `maAlignment` 后年化 `21.12%`、最大回撤 `-3.86%`，优于基准。
  - 运行 `002-repair-indicator-ablate-ma-001-window-validation`：滚动窗口 `5/7` 通过，最低年化 `3.51%`、最深回撤 `-5.75%`，失败窗口仍为 `Y1/R18-1`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 回测均在 `api` 容器内执行，未修改数据库 volume、端口、凭据或真实交易接口。
  - 当前最好观察候选更新为 `002-repair-indicator-ablate-ma-001`，但年化 `21.12%` 仍低于 002 门槛 `30%`。
- 后续事项：`002` 仍为 active，未通过。下一步聚焦 `Y1/R18-1` 的低收益和非 MA 右尾来源；不要再叠加 MA 类派生因子。

## 2026-05-31 05:17 +08:00 - 002 BOLL 过热降分与 RSI 权重上限
- 阶段目标：验证非 MA 过热处理和 RSI 权重提高能否把当前最好观察候选推近 002 年化 `30%`。
- 实际操作：
  - 新增 `bollPositionBalance` 可选评分因子，默认权重 `0`，用于测试 BOLL 中上轨附近最优、过热不加分的非线性位置评分；旧 run 口径不变。
  - 运行 `002-repair-boll-balance-001`：`bollPositionBalance=0.5`，年化 `18.63%`，弱于当前主线。
  - 运行 `002-repair-boll-balance-002`：`bollPositionBalance=0.25`，年化 `20.02%`，仍弱于当前主线。
  - 运行 `002-repair-rsi-balance-strong-001`：`rsiBalance=0.75`，年化 `21.34%`，全窗口小幅提高但未达标。
  - 运行 `002-repair-rsi-balance-strong-002`：`rsiBalance=1.0`，年化 `21.54%`，全窗口最高但仍低于 002 门槛。
  - 运行 `002-repair-rsi-balance-strong-003`：`rsiBalance=1.5`，年化回落到 `20.29%`，最大回撤扩大到 `-6.73%`。
  - 运行 `002-repair-rsi-balance-strong-002-window-validation`：滚动窗口仅 `3/7`，失败窗口为 `Y1/Y3/R18-1/R18-2`，不能替代当前主线。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机与 `api` 容器内 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 通过。
  - `.\scripts\check-file.ps1 scripts\research\run_portfolio_backtest.py` 与 `scripts\research\run_window_validation.py` 通过。
- 后续事项：`002` 仍为 active，未通过。当前稳健主线仍是 `002-repair-indicator-ablate-ma-001`；下一步应停止小权重搜索，专门解释 `Y1/R18-1` 低收益，并寻找非 MA 右尾来源。

## 2026-05-31 06:05 +08:00 - 002 行业相对强度与 Risk-On 日期扩展
- 阶段目标：继续落实“指标组合化”研究方向，验证行业强度、行业内相对强度、MACD 柱改善、量能/高点/基础分前置参数和 Risk-On 日期扩展能否补足 002 的收益缺口。
- 实际操作：
  - 在 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py` 新增默认关闭的 `industryReturn20`、`industryRelativeReturn20`、`macdHistDelta` 横截面评分权重，旧 run 口径不变。
  - 运行 `002-repair-industry-factor-001/002/003`：行业平均强度与行业内相对强度均未超过当前主线，最好 `industryRelativeReturn20=0.25` 年化 `21.04%`，仍低于主线 `21.12%`。
  - 运行 `002-repair-volume-ablate-001`、`002-repair-base-strong-001`、`002-repair-high60-light-001`、`002-repair-macd-delta-001`：去量能、放大基础分、削弱 high60、加入 MACD 柱改善均不优于主线。
  - 运行 `002-repair-market-up40-001`：`market minUpPct` 从 `0.45` 降到 `0.40` 后年化升至 `21.92%`，但 `688660.SH` 尾部亏损 `-10.35%` 破线。
  - 运行 `002-repair-market-up40-priorgap3-001`、`002-repair-market-up40-range7-001`、`002-repair-market-up40-range-penalty-001`、`002-repair-market-up40-rsi1-001`：尝试 prior gap、振幅硬过滤、振幅扣分和提高 RSI 均衡权重修复尾部，均无法同时保留收益增量和尾部安全。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `python -m json.tool docs\research\research-runs.json` 通过。
  - 主机与 `api` 容器内 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 均通过。
  - `.\scripts\check-file.ps1 scripts\research\run_portfolio_backtest.py`、`scripts\research\run_window_validation.py`、`docs\research\stages\002-candidate-repair-30\evidence.md`、`docs\research\research-runs.json`、`docs\research\next-strategy-brief.md` 通过。
  - `git diff --check` 通过，仅提示部分文件下次 Git 触碰时 LF 会转 CRLF。
- 后续事项：`002` 仍为 active，未通过。当前稳健主线仍是 `002-repair-indicator-ablate-ma-001`；下一步应停止 OHLCV 小权重搜索，优先研究资金流/成交额结构、基本面质量过滤或主题持续性这类新右尾来源。

## 2026-05-31 07:26 +08:00 - 002 daily_basic 因子与低开入场地板
- 阶段目标：继续落实“指标组合化”方向，验证 `daily_basic` 可当日获得字段和低开入场风险标签能否补足 002 收益缺口。
- 实际操作：
  - 在 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py` 新增默认关闭的 `turnoverRateF`、`volumeRatioBasic`、`lowVolumeRatioBasic`、`smallCircMv`、`priorGapStability` 横截面评分项。
  - 新增默认关闭的 `minEntryGapPct/minGapPct` 入场缺口地板，用于测试低开过深后追入的尾部风险；默认口径不变。
  - 运行 `002-repair-dailybasic-control-001`，确认接入 `daily_basic` 后所有新权重为 `0` 时结果与当前主线一致。
  - 运行 `002-repair-dailybasic-turnover-001`、`002-repair-dailybasic-volume-ratio-001`、`002-repair-dailybasic-low-volume-ratio-001/002/003`、`002-repair-dailybasic-small-circ-001`、`002-repair-prior-gap-stability-001`，均未超过当前主线。
  - 运行 `002-repair-market-up425-001`、`002-repair-market-up4125-001`、`002-repair-market-up4125-gapfloor3-001`：`minUpPct=0.4125` 可提高全窗口收益但会让 `688660.SH` 破尾；加入 `minGapPct=-3%` 后全窗口年化 `21.33%`、尾部恢复到 `-8.19%`。
  - 运行 `002-repair-market-up4125-gapfloor3-001-window-validation`：滚动窗口仅 `3/7`，失败窗口为 `Y1/Y3/R18-1/R18-2`，不能替代主线。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机与 `api` 容器内 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 通过。
  - `api` 容器内 `--help` 已确认 `low-volume`、`prior-gap-stability`、`min-entry-gap` 参数可见。
  - 回测均在本地 `api` 容器内执行，未修改数据库 volume、端口、凭据或真实交易接口。
- 后续事项：`002` 仍为 active，未通过。当前稳健主线仍是 `002-repair-indicator-ablate-ma-001`；`minGapPct=-3%` 只作为未来候选的尾部风控工具保留。下一步应转向资金流/成交额结构、主题持续性或可按公告日对齐的基本面质量，而不是继续小权重线性叠加。

## 2026-05-31 08:36 +08:00 - 002 成交额结构效率因子
- 阶段目标：继续落实“指标组合化”方向，验证成交额扩张与 20 日收益组合出的效率因子是否能补足 002 收益缺口。
- 实际操作：
  - 在 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py` 新增默认关闭的 `amountRatio`、`amountEfficiency20` 横截面评分项，默认口径不变。
  - 运行 `002-repair-amount-control-001`，确认成交额字段接入后新权重为 `0` 时结果与当前主线一致。
  - 诊断成交样本：`amountRatioRank` 几乎不能区分赢家和输家；`amountEfficiency20Rank` 赢家均值高于输家，适合作为候选因子测试。
  - 运行 `002-repair-amount-efficiency-001/002/003/004`：`amountEfficiency20=0.20/0.25` 可把全窗口年化小幅提高到 `21.28%/21.37%`，但仍低于 002 的 `30%` 门槛，且最大回撤扩大到 `-4.78%`。
  - 运行 `002-repair-amount-efficiency-001-window-validation` 与 `002-repair-amount-efficiency-004-window-validation`：均为 `5/7`，失败窗口 `Y1/R18-1`，最低年化 `-1.02%`、最差回撤 `-7.96%`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机与 `api` 容器内 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 已通过。
  - `api` 容器内 `--help` 已确认 `amount-ratio` 与 `amount-efficiency` 参数可见。
  - 本轮只做研究回测和文档归档，未修改数据库 volume、端口、凭据或真实交易接口。
- 后续事项：`002` 仍为 active，未通过。当前稳健主线仍是 `002-repair-indicator-ablate-ma-001`；`amountEfficiency20` 保留为“顺风期右尾增强”的观察证据，不进入主线。下一步优先解释 `Y1/R18-1` 弱窗口，或引入资金流、主题持续性、公告/财务质量等真正新增数据源。

## 2026-05-31 09:06 +08:00 - 002 成交额效率与 RSI 均衡反事实
- 阶段目标：验证第七组暴露的“成交额效率会替换进 RSI 极端票”是否能通过提高 `rsiBalance` 权重修复。
- 实际操作：
  - 运行 `002-repair-amount-rsi-balance-001`：在当前主线上加入 `amountEfficiency20=0.20`，并把 `rsiBalance` 从 `0.50` 提到 `0.75`；全窗口年化 `21.36%`、最大回撤 `-4.77%`、盈亏比 `2.49:1`、尾部最差 `-8.19%`。
  - 运行 `002-repair-amount-rsi-balance-001-window-validation`：滚动窗口 `4/7`，失败窗口 `Y1/Y3/R18-1`，最低年化 `2.19%`、最低 Sharpe `0.39`、最差回撤 `-5.83%`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 两个回测均在本地 `api` 容器内执行。
  - 本轮未新增代码，只使用已有默认关闭参数，未修改数据库 volume、端口、凭据或真实交易接口。
- 后续事项：`002` 仍为 active，未通过。全局提高 RSI 均衡权重不能修复成交额效率的弱窗口问题；下一步应停止 OHLCV 小权重线性搜索，优先做弱窗口保护诊断或扩展真正新增数据源。

## 2026-05-31 09:32 +08:00 - 002 成交额效率与 RSI 交互因子
- 阶段目标：验证“成交额效率必须同时经过 RSI 均衡确认才加分”的非线性交互，确认是否能替代前两组线性权重方案。
- 实际操作：
  - 在 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py` 新增默认关闭的 `amountEfficiencyRsi` 横截面评分项，旧 run 口径不变。
  - 运行 `002-repair-amount-rsi-interaction-001`：`amountEfficiencyRsi=0.25`，全窗口年化 `21.49%`、最大回撤 `-4.76%`、盈亏比 `2.55:1`、尾部最差 `-8.19%`。
  - 运行 `002-repair-amount-rsi-interaction-001-window-validation`：滚动窗口 `4/7`，失败窗口 `Y1/Y3/R18-1`，最低年化 `-2.69%`、最低 Sharpe `-0.44`、最差回撤 `-8.09%`。
  - 诊断失败替换：`Y1` 中交互因子独有 4 笔交易全亏，合计约 `-2729`；当前主线独有 6 笔交易合计约 `+2697`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机与 `api` 容器内 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 通过。
  - `api` 容器内 `--help` 已确认 `amount-efficiency-rsi` 参数可见。
  - 本轮未修改数据库 volume、端口、凭据或真实交易接口。
- 后续事项：`002` 仍为 active，未通过。`amountEfficiencyRsi` 只作为完整窗口右尾增强但弱窗口退化的负证据保留；下一步优先扩展资金流、主题持续性、公告/财务质量等新增数据语义，或做逐日弱窗口入场状态诊断。

## 2026-05-31 09:46 +08:00 - 002 弱窗口逐日入场状态诊断
- 阶段目标：不再继续 OHLCV 小权重搜索，拆解 `Y1/R18-1/Y3` 中实际买入、Risk-On 日未买入高分票和策略风险拦截票的前瞻表现。
- 实际操作：
  - 新增 `scripts/research/analyze_weak_window_entry_states.py`，复用当前主线 context 和信号生成逻辑，输出弱窗口 Risk-On 日候选诊断。
  - 运行 `002-weak-window-entry-state-001`，来源为 `002-repair-indicator-ablate-ma-001-window-validation`。
  - 诊断显示：`Y1` 实际买入 Fwd5 `3.27%`，未买入高分票 `-0.77%`，策略风险拦截票 `-0.89%`；`R18-1` 分别为 `2.66%/2.11%/0.10%`；`Y3` 分别为 `2.69%/1.42%/1.14%`。
  - 进一步对比真实成交收益：`Y1/R18-1/Y3` 实际买入 Fwd10 均值为 `5.56%/6.97%/4.96%`，但完成交易平均收益约为 `0.97%/2.19%/1.58%`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `api` 容器内诊断脚本成功生成 `docs/research/runs/002-weak-window-entry-state-001/`。
  - 本轮诊断不修改交易语义，不连接真实交易接口，不修改数据库 volume、端口或凭据。
- 后续事项：`002` 仍为 active，未通过。当前证据不支持简单放宽风险拦截或继续排序微调；下一步优先新增资金流/主题持续性/公告财务质量语义，或在明确不可比口径下测试状态化利润保护。

## 2026-05-31 09:55 +08:00 - 002 新增数据语义与资金流诊断
- 阶段目标：审计本地新增数据语义覆盖，验证前一交易日资金流是否能解释当前主线完成交易的赢家/输家差异。
- 实际操作：
  - 审计本地数据：`stock_daily_bars` 与 `stock_daily_basic` 各 `3,901,653` 行、`5,645` 只股票；`stock_financial_indicators` 仅 `60` 行、`5` 只股票；本地无资金流、主题、热榜、公告持久化表。
  - 探测 Tushare 接口：`moneyflow`、`moneyflow_hsgt`、`concept` 可用；`ths_hot` 可用但频率限制很紧。
  - 新增 `scripts/research/analyze_moneyflow_entry_edges.py`，读取当前主线完成交易，按每笔入场的前一交易日拉取 Tushare `moneyflow`，避免使用入场日收盘后数据。
  - 运行 `002-moneyflow-entry-edge-001`：完成交易 `125/125` 匹配到 moneyflow，唯一前一交易日 `80` 个。
  - 结果：全样本赢家相对输家的主力净流排名差 `+0.061`；`Y1` 为 `+0.052`，`R18-1` 为 `+0.134`，但 `Y3` 为 `-0.009`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `api` 容器内资金流诊断脚本成功生成 `docs/research/runs/002-moneyflow-entry-edge-001/`。
  - 本轮不落库、不修改交易语义、不连接真实交易接口，不修改数据库 volume、端口或凭据。
- 后续事项：`002` 仍为 active，未通过。资金流方向有弱正证据但单日信号不足；下一步应新增本地 point-in-time moneyflow 同步/缓存，并测试多日主力净流持续性因子。

## 2026-05-31 10:23 +08:00 - 002 MACD+BOLL+RSI 非线性 setup 因子
- 阶段目标：响应“不要只基于 MA，应做指标组合化”的调整意见，验证 MACD、BOLL、RSI 的非线性组合因子是否能改善当前 002 主线。
- 实际操作：
  - 在 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py` 新增默认关闭的 `indicatorSetup` 横截面评分项，定义为 `MACD柱排名 * BOLL收口排名 * RSI均衡排名 * 过热保护`；默认权重为 `0`，旧 run 口径不变。
  - 运行 `002-repair-indicator-setup-control-001`，确认默认关闭后结果与当前主线一致：年化 `21.12%`、最大回撤 `-3.86%`、盈亏比 `2.54:1`。
  - 运行 `002-repair-indicator-setup-001`：`indicatorSetup=0.25`，年化 `21.43%`、最大回撤 `-4.78%`、盈亏比 `2.51:1`。
  - 运行 `002-repair-indicator-setup-002`：`indicatorSetup=0.50`，年化降到 `18.39%`，说明该 setup 不是单调加权因子。
  - 运行 `002-repair-indicator-setup-amount-001`：`indicatorSetup=0.25 + amountEfficiency20=0.20`，年化 `21.79%`、最大回撤 `-4.86%`、盈亏比 `2.48:1`，仍远低于 002 的 `30%` 年化门槛。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 通过。
  - 四个组合回测均在本地 `api` 容器内执行，未修改数据库 volume、端口、凭据或真实交易接口。
- 后续事项：`002` 仍为 active，未通过。MACD/BOLL/RSI 非线性 setup 可保留为弱正辅助证据，但不继续在 OHLCV 技术指标内部做小权重搜索；下一步优先 point-in-time 资金流缓存和多日主力净流持续性因子。

## 2026-05-31 11:28 +08:00 - 002 point-in-time 主力资金流排序因子
- 阶段目标：落实新增数据语义路线，构建不落库的 point-in-time 资金流缓存，并验证前一交易日/3日/5日主力净流排名是否能修复 002 弱窗口。
- 实际操作：
  - 新增 `scripts/research/build_moneyflow_cache.py`，按允许入场日前 `N` 个交易日拉取 Tushare `moneyflow`，计算全市场资金流排名后仅缓存候选池股票；缓存写入 run 目录，不修改数据库 schema。
  - 在 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py` 新增 `--moneyflow-cache`、`moneyflowMainNetRank1/3/5` 默认关闭权重，并在启用资金流权重但未提供缓存时直接报错。
  - 运行 `002-moneyflow-cache-mainline-001`：候选池 `1430` 只，允许入场日 `178` 个，前置交易日 `358` 个，Tushare `moneyflow` 获取 `358/358`，错误 `0`。
  - 运行 `002-repair-moneyflow-control-001`：缓存接入但资金流权重为 `0`，结果与当前主线一致，年化 `21.12%`、最大回撤 `-3.86%`、盈亏比 `2.54:1`。
  - 运行 `002-repair-moneyflow-main1-001`：`moneyflowMainNetRank1=0.25`，全窗口年化 `22.01%`、最大回撤 `-4.57%`、盈亏比 `2.60:1`，为本组最好。
  - 运行 `002-repair-moneyflow-main3-001`：年化 `21.02%`、回撤 `-4.84%`，弱于控制组。
  - 运行 `002-repair-moneyflow-main5-001`：年化 `21.10%`、最大回撤 `-8.72%`，收益不增且回撤恶化。
  - 运行 `002-repair-moneyflow-main1-001-window-validation`：滚动 `5/7`，失败窗口 `Y1/R18-1`；`Y1` 年化 `-0.99%`，`R18-1` 年化 `5.20%`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机与 `api` 容器内语法检查已覆盖新增脚本和两个回测入口。
  - 回测和滚动验证均在本地 `api` 容器内执行；本轮未修改数据库 volume、端口、凭据或真实交易接口。
- 后续事项：`002` 仍为 active，未通过。前一交易日主力资金流排名只能作为弱辅助，不能修复早段弱窗口；下一步应做资金流与市场/行业状态交互或主题持续性 point-in-time 缓存，而不是继续单纯资金流均值排序。

## 2026-05-31 11:58 +08:00 - 002 资金流确认因子与交易替换诊断
- 阶段目标：响应“指标组合化而不是只基于 MA”的后续研究方向，诊断前一交易日主力资金流因子为何全窗口弱正但滚动窗口不稳，并验证行业状态/RSI 确认是否能修复。
- 实际操作：
  - 新增 `scripts/research/analyze_trade_delta.py`，用于对比两个组合 run 或两个滚动验证 run 的完成交易替换。
  - 在 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py` 新增默认关闭的 `moneyflowIndustryConfirm` 和 `moneyflowRsiConfirm` 权重；启用时仍强制要求 point-in-time `moneyflow-cache`。
  - 运行 `002-moneyflow-main1-trade-delta-001`：简单资金流 rank1 相对当前主线全窗口替换净差 `+2757.38`。
  - 运行 `002-moneyflow-main1-window-trade-delta-001`：简单资金流 rank1 在 `Y1/R18-1` 替换净差 `-4086.30/-3693.16`，在 `Y3/R18-4` 替换净差 `+5105.37/+5129.33`。
  - 运行 `002-repair-moneyflow-industry-confirm-001`：年化 `18.89%`、最大回撤 `-4.57%`、盈亏比 `2.60:1`，弱于当前主线。
  - 运行 `002-repair-moneyflow-rsi-confirm-001`：年化 `20.92%`、最大回撤 `-4.57%`、盈亏比 `2.58:1`，弱于当前主线。
  - 运行 `002-moneyflow-rsi-confirm-trade-delta-001`：RSI 确认相对当前主线全窗口替换净差 `-1707.90`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机与 `api` 容器内 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 通过。
  - `api` 容器内 `--help` 已确认 `moneyflow-rsi-confirm` 参数可见。
  - 回测和诊断均在本地 `api` 容器内执行；本轮未修改数据库 volume、端口、凭据或真实交易接口。
- 后续事项：`002` 仍为 active，未通过。粗粒度行业/RSI 确认不能修复资金流窗口不稳定；下一步优先做 point-in-time 主题/概念持续性、板块资金强弱或公告/事件风险标签，不继续围绕 MA 或 OHLCV 小权重搜索。

## 2026-05-31 12:21 +08:00 - 002 本地行业聚合主力资金流
- 阶段目标：继续验证新增数据语义，复用已有 point-in-time 个股资金流缓存，按本地行业聚合主力净流，确认“板块资金强弱”是否能修复 002。
- 实际操作：
  - 对当前主线完成交易做行业资金诊断：行业主力净流总额排名在 `Y1/R18-1/Y3` 的赢家-输家差异约为 `+0.094/+0.029/+0.148`。
  - 在 `scripts/research/run_portfolio_backtest.py` 新增默认关闭的 `industryMoneyflowSumNetRank1`，从 `moneyflow-cache` 读取前一交易日个股主力净流后按 `stocks.industry` 聚合排名。
  - 在 `scripts/research/run_window_validation.py` 透传 `--cross-section-industry-moneyflow-sum-net-rank1-weight`。
  - 运行 `002-repair-industry-moneyflow-sum-001`：`industryMoneyflowSumNetRank1=0.25`，年化 `17.98%`、最大回撤 `-8.66%`、盈亏比 `2.60:1`、完成交易 `128`。
  - 运行 `002-industry-moneyflow-sum-trade-delta-001`：相对当前主线全窗口替换净差 `-11174.97`，候选独有 `12` 笔交易胜率 `0.00%`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 主机与 `api` 容器内 `python -m py_compile scripts\research\run_portfolio_backtest.py scripts\research\run_window_validation.py` 通过。
  - 主机 `check-file.ps1` 覆盖两个回测入口。
  - 回测使用一次性 `docker compose run --rm api` 容器执行；未修改数据库 volume、端口、凭据或真实交易接口。
- 后续事项：`002` 仍为 active，未通过。行业资金强度不能作为独立追涨排序项；后续若继续使用资金流，应作为拥挤风险或事件/主题确认的上下文，下一步优先做 point-in-time 概念成分、主题扩散或公告风险标签诊断。

## 2026-05-31 12:01 +08:00 - 前端四 Tab 研究工作台改造开始
- 阶段目标：将前端主流程收敛为“选股池、单票验证、全市场验证、复盘诊断”四个 Tab；补齐全 A 基本面批量载入入口；移除或降级过时的极限组合、Risky8 和旧策略实验主入口。
- 实际操作：
  - 计划先审计 `frontend/src/main.jsx`、`frontend/src/styles.css`、`backend/app/main.py` 和 `backend/app/schemas.py` 的现有能力，再做最小改造。
  - 约束：不改变核心回测语义，不连接真实交易接口，不修改数据库 volume、端口或凭据。
- 验证结果：待完成代码修改后运行最小检查。
- 后续事项：完成前端四 Tab 重组、全 A 基本面接口和构建/语法验证后补充结束记录。

## 2026-05-31 12:13 +08:00 - 前端四 Tab 研究工作台改造完成
- 阶段目标：完成四 Tab 工作台改造，让选股池、单票验证、全市场验证和复盘诊断形成清晰研究路径。
- 实际操作：
  - 修改 `frontend/src/main.jsx`：主导航收敛为 4 个 Tab；新增“载入全A基本面”入口；将旧“策略实验”改为单票验证视角；新增全市场验证面板；合并质量诊断与 AI/本地策略复盘；移除可见的组合基线、Risky8 和旧策略实验入口。
  - 修改 `frontend/src/styles.css`：适配 4 Tab、全 A 基本面操作条、全市场验证面板和复盘诊断布局。
  - 修改 `backend/app/schemas.py` 与 `backend/app/main.py`：新增 `/api/tushare/sync-market-fundamentals`，用于按全 A 股票列表批量同步财务指标，配合既有股票列表和每日估值同步组成“全 A 基本面”载入。
- 验证结果：
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py` 通过。
  - 主机 `npm run build` 因本机 `node_modules` 缺失 Vite/Rolldown Windows optional dependency 未进入代码构建阶段；改用项目要求的 Docker 环境验证。
  - `docker compose run --rm frontend npm run build` 通过。
  - `http://localhost:18000/api/health` 返回 `ok`；OpenAPI 已包含 `/api/tushare/sync-market-fundamentals`。
  - 浏览器验证 `http://localhost:15173`：只显示 `选股池 / 单票验证 / 全市场验证 / 复盘诊断` 4 个 Tab；“载入全A基本面”可见；`极限组合`、`Risky8`、`策略实验` 不再可见；4 个 Tab 均可切换。
- 后续事项：全 A 财务指标同步会逐票调用 Tushare，首次运行可能较慢；如后续使用体验仍慢，再考虑后台任务化和更细进度条。

## 2026-05-31 12:46 +08:00 - 002 point-in-time 题材覆盖数量诊断
- 阶段目标：响应“指标组合化和新增数据语义”方向，验证 KPL 题材成分是否能作为当前 002 主线的 point-in-time 修复因子。
- 实际操作：
  - 使用 Context7/Tushare 文档和本地 API 探测确认 `dc_member`、`dc_daily`、`kpl_concept_cons` 的历史查询可用性。
  - 新增 `scripts/research/analyze_concept_entry_edges.py`，诊断当前主线完成交易在入场日前一交易日的 KPL 题材覆盖数量和热度差异。
  - 新增 `scripts/research/build_concept_cache.py`，为允许入场日前一交易日构建 run-local KPL 题材缓存，不落库、不改 schema。
  - 修改 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py`，新增默认关闭的 `--concept-cache` 和 `--cross-section-kpl-concept-count-rank1-weight`。
  - 运行 `002-concept-entry-edge-001`、`002-concept-cache-mainline-001`、`002-repair-kpl-concept-count-001` 和 `002-kpl-concept-count-trade-delta-001`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 诊断覆盖：KPL 匹配 `61/125` 笔完成交易，覆盖率 `48.80%`；题材覆盖数量赢家/输家排名差 `+0.083`，但题材热度排名为弱反向。
  - 全窗口回测：`kplConceptCountRank1=0.25` 总收益 `75.02%`、年化 `20.53%`、最大回撤 `-4.77%`、盈亏比 `2.59:1`，弱于当前主线且低于 002 年化 `30%` 门槛。
  - 交易替换：候选相对当前主线替换净差 `-2525.45`，候选独有胜率 `30.77%`；不进入滚动验证。
  - 本轮未修改数据库 volume、端口、凭据、真实交易接口或核心买卖语义。
- 后续事项：`002` 仍为 active，未通过。KPL 题材覆盖数量/热度不作为独立正向排序项；若继续使用题材，应转向事件或公告风险标签、概念扩散后的次日路径诊断。

## 2026-05-31 12:55 +08:00 - 002 东财概念板块强度诊断
- 阶段目标：继续验证主题/概念持续性，但从“股票覆盖多少题材”切换为“入场日前一交易日所属东财概念板块是否已经有涨幅、换手或成交额强度”。
- 实际操作：
  - 使用 Context7/Tushare 文档确认 `dc_daily` 与 `dc_member` 的历史查询口径。
  - 新增 `scripts/research/analyze_dc_concept_entry_edges.py`，对当前主线完成交易逐笔读取入场日前一交易日 `dc_daily` 概念板块行情和 `dc_member(con_code=股票)` 所属概念。
  - 运行 `002-dc-concept-entry-edge-001`，输出 `docs/research/runs/002-dc-concept-entry-edge-001/results.json` 与 `review.md`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 东财概念匹配 `79/125`，覆盖率 `63.20%`，拉取错误 `0`。
  - 全样本赢家-输家差异：概念涨幅最高排名 `+0.038`，概念涨幅均值排名 `-0.007`，概念换手最高排名 `+0.049`。
  - 关键弱窗口 `Y1/R18-1` 没有有效概念强度覆盖，不能作为 002 修复主线。
  - `dcConceptPctMeanRank>=0.7` 保留样本平均收益 `-4.29%`、胜率 `12.50%`，说明不能直接追随整体概念强度。
  - 本轮未修改数据库 volume、端口、凭据、真实交易接口或核心买卖语义。
- 后续事项：`002` 仍为 active，未通过。东财概念强度不作为独立正向排序项；下一步优先做入场后路径诊断，确认现有入场 alpha 是被退出规则消耗还是入场后快速衰减。

## 2026-05-31 13:01 +08:00 - 002 入场后路径衰减诊断
- 阶段目标：在不改变核心退出语义的前提下，验证当前主线的瓶颈是入场 alpha 衰减，还是浮盈捕捉效率不足。
- 实际操作：
  - 新增 `scripts/research/analyze_entry_path_decay.py`，读取当前主线 `002-repair-indicator-ablate-ma-001` 的完成交易和本地日线，计算 Fwd1/3/5/10/20、MFE/MAE、持有期内触及 3%/5%/10% 和止损前浮盈情况。
  - 运行 `002-entry-path-decay-001`，输出 `docs/research/runs/002-entry-path-decay-001/results.json` 与 `review.md`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 入场后日线覆盖 `125/125`。
  - 全样本实际均值 `3.61%`，Fwd5/Fwd10 收盘均值 `3.33%/5.36%`，10 日 MFE `18.14%`，持有内 MFE `11.84%`。
  - `Y1/R18-1` 的 Fwd10 分别为 `4.35%/8.57%`，持有内 MFE 分别为 `10.16%/11.10%`，说明弱窗口并非完全没有入场后 alpha。
  - `stop` 退出的 `72` 笔交易实际均值 `-3.63%`，持有内 MFE `5.45%`，`54.17%` 在止损前曾达到 `3%`，`44.44%` 曾达到 `5%`。
  - 本轮未修改数据库 volume、端口、凭据、真实交易接口或核心买卖语义。
- 后续事项：`002` 仍为 active，未通过。下一步若测试状态化利润保护、浮盈回吐约束或持仓时间管理，必须新建非可比实验口径并明确标注不能直接与旧主线横向比较。

## 2026-05-31 13:07 +08:00 - 002 非可比退出保护预筛
- 阶段目标：在不修改主线回测语义的前提下，预筛简单锁盈、保本和时间退出保护是否值得进入正式非可比组合回测。
- 实际操作：
  - 新增 `scripts/research/screen_exit_protection_variants.py`，只在当前主线已完成交易路径上模拟更早保护退出，不释放资金、不生成新买入。
  - 运行 `002-exit-protection-prescreen-001`，输出 `docs/research/runs/002-exit-protection-prescreen-001/results.json` 与 `review.md`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `time5_no_3` 保护后均值 `3.65%`，全样本增量仅 `+0.04%`，只改变 `5/125` 笔交易，不足以成为 002 修复主线。
  - `lock2_after_5` 全样本增量 `-0.88%`，虽对 `Y1/R18-1` 有 `+0.37%/+0.61%`，但对 `Y3/R18-4` 伤害为 `-1.12%/-1.63%`。
  - `lock3_after_5`、`trail50_after_5`、`be_after_3` 均明显降低全样本均值，主要问题是截断右尾。
  - 本轮未修改数据库 volume、端口、凭据、真实交易接口或主线核心买卖语义。
- 后续事项：`002` 仍为 active，未通过。不得全局启用简单锁盈/保本/时间退出；如继续退出语义线，应先诊断可观察弱状态触发条件，再新建明确非可比组合回测口径。

## 2026-05-31 13:06 +08:00 - 单票验证策略预设改为 docs 策略
- 阶段目标：响应浏览器评论，将单票验证页顶部旧硬编码策略预设替换为 `docs/research/runs` 中已沉淀的研究策略。
- 实际操作：
  - 修改 `frontend/src/main.jsx`：新增 docs 策略选择卡片，读取 `/api/research/runs` 列表；点击卡片时读取 run 详情并套用 `strategy.config`/`payload.config` 到当前表单和回测请求；单票与全市场验证共用选中的 docs 策略配置。
  - 修改 `frontend/src/styles.css`：新增 docs 策略卡片、状态标签、指标小格和响应式布局；删除单票页旧 `preset-grid` 可见入口。
  - 重启 `frontend` 容器，让 Vite 重新读取挂载源码；未修改数据库 volume、端口、凭据或真实交易接口。
- 验证结果：
  - `git diff --check -- frontend\src\main.jsx frontend\src\styles.css` 通过。
  - `docker compose run --rm frontend npm run build` 通过。
  - 浏览器验证 `http://localhost:15173`：单票验证页显示 `docs 策略`、`12 个研究运行` 和 run 卡片；旧 `策略预设`、`BOLL下轨反弹`、`BOLL收口突破`、`BOLL上轨突破`、`RSI超卖反转`、`MA趋势跟随` 不再出现在该策略区域。
  - 点击 `113-portfolio-cross-section-risk8-pos135-entry-gap6-volume025-netreturn-limitdelay-slip10bp-gap` 后，侧栏“当前策略”切到 docs run，止盈参数显示为 docs 配置的 `10% / 20%`。
  - 尝试 `docker compose up -d --build frontend` 时 Docker 镜像源对 `python:3.12-slim` 返回 `429 Too Many Requests`；本轮用前端构建验证和容器重启完成运行态验证。
- 后续事项：当前卡片展示最新 12 个 docs run；如果后续 docs run 很多，可以再加搜索或按状态/阶段筛选。

## 2026-05-31 13:19 +08:00 - 002 退出保护状态触发诊断
- 阶段目标：落实“不要只基于 MA，要把 MA/MACD/BOLL/RSI 等指标组合化验证”的研究调整，检查简单退出保护是否能由入场当时可观察的复合指标状态触发。
- 实际操作：
  - 新增 `scripts/research/analyze_exit_protection_state_triggers.py`，读取 `002-exit-protection-prescreen-001` 的保护候选增量，并按 `entryRiskMetrics` 与 `entryScoreParts` 中的 MACD/BOLL/RSI、振幅、缺口、量能等状态分桶。
  - 运行 `002-exit-protection-state-trigger-001`，输出 `docs/research/runs/002-exit-protection-state-trigger-001/results.json` 与 `review.md`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - 最好可观察条件为 `time5_no_3 + MACD柱排名>=80%`，命中 `60/125`，选中样本均值增量 `+0.10%`，定向全样本增量仅 `+0.05%`，低于 `+0.20%` 观察线。
  - `time5_no_3 + BOLL位置排名>=80%` 定向全样本增量 `+0.04%`，但 `Y3/R18-4` 为负；`trail50_after_5 + 入场上影>=3%` 定向全样本增量仅 `+0.03%`。
  - 事后 `actual_stop_exit` 能解释浮盈回吐，但属于未来标签，不可作为交易触发。
  - `python -m json.tool docs\research\research-runs.json > $null` 通过。
  - 主机与容器 `python -m py_compile` 覆盖本次诊断脚本及相关研究脚本，通过。
  - `.\scripts\check-file.ps1` 覆盖脚本、阶段文档、brief、run 索引和新 run 输出，通过。
- 后续事项：`002` 仍为 active，未通过。不得把粗状态退出保护推进到正式组合回测；下一步优先寻找 point-in-time 新数据语义或更窄的失败窗口诊断。

## 2026-05-31 14:32 +08:00 - 002 资金流市场状态门控实验
- 阶段目标：继续推进“指标/数据语义组合化因子”，验证前一交易日主力净流是否需要市场宽度强势确认，避免简单 `moneyflowMainNetRank1=0.25` 在 `Y1/R18-1` 替换坏交易。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py`：新增默认关闭的 `moneyflowMarketStrong` 因子，仅在前一交易日 `aboveMa20Pct>=0.50` 且 `upPct>=0.50` 时启用前一交易日主力净流排名。
  - 修改 `scripts/research/analyze_trade_delta.py`：把 `moneyflowMarketStrongRank` 纳入替换样本评分摘要。
  - 运行 `002-repair-moneyflow-main1-light-001`、`002-moneyflow-main1-light-trade-delta-001`、`002-repair-moneyflow-market-strong-001` 和 `002-moneyflow-market-strong-trade-delta-001`。
  - 运行 `002-repair-moneyflow-market-strong-001-window-validation` 和 `002-moneyflow-market-strong-window-trade-delta-001`，检查独立滚动窗口内的交易替换。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `moneyflowMainNetRank1=0.10` 年化 `21.17%`，较当前主线 `21.12%` 只提升约 `0.05` 个百分点；交易替换净差 `+123.99`，只替换 `4` 笔。
  - `moneyflowMarketStrong=0.25` 年化 `21.98%`、最大回撤 `-3.86%`、盈亏比 `2.60:1`，仍低于 002 年化 `30%` 门槛。
  - `moneyflowMarketStrong=0.25` 全样本替换净差 `+2648.59`，但 `Y1/R18-1` 仅 `+40.02`，早段失败窗口没有被实质修复。
  - 独立滚动验证为 `5/7`，失败窗口仍是 `Y1/R18-1`；窗口级交易替换显示 `Y1/R18-1` 候选独有交易胜率均为 `0.00%`，替换净差分别为 `-4086.30/-1289.93`。
  - 本轮未修改数据库 volume、端口、凭据、真实交易接口或主线核心买卖语义。
- 后续事项：`002` 仍为 active，未通过。资金流市场门控仅在后段窗口表现为观察型正证据，不再继续只调资金流权重或简单市场强势门控；下一步优先寻找早段 `Y1/R18-1` 的事件/公告风险、行业持续性衰减或市场结构语义。

## 2026-05-31 15:03 +08:00 - 002 资金流市场质量复合因子实验
- 阶段目标：继续落实“指标组合化形成量化因子”的研究调整，验证 `moneyflowMarketStrongRank * rsiBalanceRank * industryReturn20Rank` 是否能减少早段坏替换，同时保留后段右尾。
- 实际操作：
  - 修改 `scripts/research/run_portfolio_backtest.py` 与 `scripts/research/run_window_validation.py`：新增默认关闭的 `moneyflowMarketQuality` 权重，不改变旧 run 口径。
  - 修改 `scripts/research/analyze_trade_delta.py`：将 `moneyflowMarketQualityRank` 纳入替换样本评分摘要。
  - 运行 `002-repair-moneyflow-market-quality-001` 和 `002-moneyflow-market-quality-trade-delta-001`。
  - 更新 `docs/research/stages/002-candidate-repair-30/evidence.md`、`docs/research/stages/002-candidate-repair-30/README.md`、`docs/research/research-runs.json` 和 `docs/research/next-strategy-brief.md`。
- 验证结果：
  - `moneyflowMarketQuality=0.25` 全窗口年化 `19.86%`、最大回撤 `-4.76%`、盈亏比 `2.51:1`，弱于当前主线 `21.12%`，不进入滚动验证。
  - 交易替换全样本净差 `-5207.05`；`Y1/R18-1` 替换净差均为 `+1284.53`，但 `Y3/R18-4` 替换净差均为 `-6491.58`。
  - 本轮未修改数据库 volume、端口、凭据、真实交易接口或核心买卖/退出语义。
- 后续事项：`002` 仍为 active，未通过。全局资金流乘法复合因子淘汰；若继续使用该线索，必须先做分阶段或市场状态触发诊断，证明不会伤害后段右尾。

## 2026-05-31 15:29 +08:00 - 架构精简与性能热点优化
- 阶段目标：在不改变三容器架构、回测语义、数据库持久化数据和端口约定的前提下，优化研究 run 列表/概览接口与选股筛选接口的慢路径。
- 实际操作：
  - 任务开始前记录基线：`/api/research/runs?limit=120` 约 `6-8s`，`/api/research/overview` 约 `5.5s`，`/api/stocks/screen?limit=200` 约 `7s`。
  - 修改 `backend/app/main.py`：`/api/research/runs` 默认只返回 `research-runs.json` 中已整合 run 的摘要，新增 `include_unmerged=true` 用于需要时扫描未整合目录；`/api/research/overview` 使用轻量 run 摘要和目录级文件路径，避免默认全量解析大型 `results.json`。
  - 修改 `backend/app/main.py`：`/api/stocks/screen` 批量读取候选日线和最新基本面，替代逐票查询；筛选评分逻辑仍复用原 `enrich_rows` 与 `classify_screen_signal`。
  - 修改 `frontend/src/main.jsx`：研究 run 列表和 overview 改为进入单票/全市场相关页面后懒加载，首屏选股池不再自动请求研究目录。
  - 未修改数据库 volume、端口、凭据、真实交易接口或核心回测/买卖语义。
- 验证结果：
  - `python -m py_compile backend\app\database.py backend\app\models.py backend\app\schemas.py backend\app\backtest_engine.py backend\app\tushare_client.py backend\app\ai_client.py backend\app\main.py` 通过。
  - `docker compose run --rm frontend npm run build` 通过。
  - `git diff --check -- backend\app\main.py frontend\src\main.jsx 操作日志.md` 通过。
  - `.\scripts\check-file.ps1 backend\app\main.py`、`.\scripts\check-file.ps1 frontend\src\main.jsx`、`.\scripts\check-file.ps1 操作日志.md` 通过；前端检查仍有既有未使用代码 warning，无 error。
  - 复测：`/api/research/runs?limit=120` 约 `0.8s`；`/api/research/overview` 约 `1.1s`；`/api/research/runs?limit=120&include_unmerged=true` 约 `3.0s`；`/api/stocks/screen?limit=200&technical=all&rank_by=composite` 约 `3.7-4.2s`。
- 后续事项：若继续压缩筛选耗时，下一步应专项优化 `enrich_rows` 的筛选页计算路径或建立可复用技术指标缓存；这会更接近策略语义边界，动手前需先定义是否允许近似/缓存口径。
