# TODO: 用 A 股验证策略，反哺美股持仓操作

## 最终目标

本仓库最终要服务一个闭环：

```text
想策略 / 想交易纪律
  -> 用 A 股 Tushare 数据做大样本测试和验证
  -> 形成可复用的规则、边界和失败条件
  -> 反哺美股持仓、观察池和后续操作分析
```

美股是主要操作对象；A 股是数据验证沙盘；策略库沉淀想法；回测报告沉淀证据。

本仓库仍然只做研究、复盘、模拟和人工辅助分析，不连接券商，不自动下单，不处理真实资金。

## 四区架构

仓库后续按四个部分收拢，不再让持仓、数据、策略想法和报告混在一起。

### 1. 美股相关的持仓信息 + 分析结果

定位：真实操作层。回答“我现在的美股持仓和观察池后续该怎么处理”。

应包含：

- 美股持仓快照：ticker、工具类型、数量、成本、主题、杠杆系数、风险标签。
- 美股观察池：AI 算力、存储、互联、供电、散热、封装、数据中心、SpaceX/Starlink 上游。
- 个股和主题分析：逻辑、催化、风险、估值框架、动态 PE 或期权型估值。
- 操作标签：`继续持有`、`只等回调`、`止跌后小加`、`减仓降风险`、`观察不动`。
- 预测台账：我猜的数字、市场共识、复核日期、证伪条件和事后校准。

边界：

- 真实持仓和成交记录默认只读本地 CSV，不提交到 Git。
- 报告只能写人工研究辅助，不写成真实买卖指令。
- 不连接 HSBC、券商或任何可触发真实资金变化的接口。

建议落点：

```text
my_quant/us_research/
  README.md
  config/
  data/
  reports/
  scripts/
  tests/
```

### 2. A 股相关的数据

定位：数据底座和验证沙盘。继续保留 Docker PostgreSQL 和 Tushare 数据，因为这是当前最方便、最稳定、已付费的数据源。

应包含：

- Tushare 股票基础列表、日线、估值、财务指标。
- A 股研究池、同步记录、数据质量检查。
- 数据源说明、字段口径、缓存和刷新规则。
- 数据库备份、非破坏性 schema 初始化和 Windows 启动说明。

边界：

- 不删除 Docker volume。
- 不执行 `docker compose down -v`。
- 第一阶段不把美股数据塞进现有 A 股表。
- 修改数据库结构前必须先写备份和回滚方案。

建议落点：

```text
docs/research/a-share-data/
backend/app/               # 现有 A 股 API 和模型暂时保留
docker-compose.yml         # 继续作为三容器入口
```

### 3. 之前想过的策略和交易纪律

定位：策略思想库。这里保存“我想过什么规则、为什么想、怎么验证、什么时候算失败”。

应包含：

- 策略假设：趋势、回调、突破、止跌确认、财报前减风险、同因子集中度。
- 规则卡片：每条规则的适用场景、验证口径、成功标准、失败条件。
- 负证据：哪些想法已经证明无效，避免反复调参。
- A 股验证到美股操作的映射边界：什么能迁移，什么不能迁移。

优先沉淀的规则：

- [ ] 不追高：强趋势票大幅跳涨后，是否应等待回调确认。
- [ ] 止跌后加仓：低位不是入场信号，止跌才是入场信号。
- [ ] 同因子杠杆预算：`SNXX + MVLL + CRDU + BEX` 一类敞口如何限制。
- [ ] 财报前仓位：财报/催化前默认不加仓还是降风险。
- [ ] 趋势破坏退出：跌破 MA20/MA50/BBI 后是否减仓或观察。
- [ ] 过热识别：距 52 周高点、短期涨幅、成交量异常和波动率扩张。

建议落点：

```text
docs/research/strategy-lab/
  README.md
  rules/
  ideas/
  negative-evidence/
```

### 4. 回测相关的报告和证据

定位：证据档案馆。所有回测、HTML、CSV、manifest、阶段结论和复盘摘要都应该能在这里被找到。

应包含：

- 回测 run 索引：run id、策略、数据窗口、参数、输出路径。
- HTML 报告：净值曲线、交易分布、买卖明细、失败窗口。
- Markdown 结论：阶段通过、观察或淘汰。
- 规则验证报告：A 股验证结果如何映射到美股操作。

边界：

- 报告是证据，不是策略源码。
- 关键指标尽量从 CSV/JSON/manifest 生成，不手填。
- 每份报告必须写明输入数据、生成命令、验证命令和未解决问题。

建议落点：

```text
docs/research/backtest-reports/
my_quant/strategy_research/web_report/
my_quant/strategy_research/results/
```

## 阶段性改造路线

### Phase 0: 先立边界，不搬大文件

- [ ] 在根 README 中写清四区架构和最终闭环。
- [ ] 在 `AGENTS.md` 中补充四区职责，明确美股是操作层、A 股是验证沙盘。
- [ ] 更新 `docs/agent-code-map.md`，让后续 Agent 能按四区找代码。
- [ ] 保留现有 Docker PostgreSQL 和 A 股表，不做破坏性迁移。
- [ ] 盘点 `/Users/jettlin/code/投资分析` 中可迁移的脚本、配置、报告和敏感文件。

验收：

- 后续 Agent 读根目录文档后，能说清四区分别放什么。
- 不产生任何真实持仓、token、`.env.local` 的提交风险。

### Phase 1: 建立美股操作层的文件化闭环

- [ ] 新增 `my_quant/us_research/README.md`。
- [ ] 新增 sample 级别的 `holdings_sample.csv`，不提交真实持仓。
- [ ] 新增美股观察池配置，优先从 `/Users/jettlin/code/投资分析/watchlist_symbols_2026.csv` 的结构迁移，不直接搬敏感数据。
- [ ] 新增美股快照脚本，优先使用 `yfinance`，可选 Finnhub，输出带 `fetched_at`、`source`、`is_stale` 的 JSON/CSV。
- [ ] 新增美股操作报告生成脚本，输出 HTML + Markdown。
- [ ] 报告必须显示：持仓、主题、趋势、风险、操作标签、数据新鲜度。

验收：

- 能在不连接券商、不读取真实凭据的情况下生成一份美股操作报告。
- 数据源失败时报告显示 `partial` 或 `stale`。

### Phase 2: 建立策略思想库

- [ ] 新增 `docs/research/strategy-lab/README.md`。
- [ ] 为每条核心纪律写规则卡片：规则、假设、A 股验证口径、美股映射边界。
- [ ] 把“低位不是入场信号，止跌才是入场信号”固化成第一条规则。
- [ ] 把“不追高”固化成第二条规则。
- [ ] 把“同因子杠杆预算”固化成第三条规则。
- [ ] 为已失败或过拟合的策略建立 `negative-evidence/`。

验收：

- 每条规则都能回答：为什么存在、怎么验证、什么时候失效、怎么映射到美股。

### Phase 3: 用 A 股数据验证第一批规则

- [ ] 选择第一条验证规则：建议先做“不追高”。
- [ ] 用 Tushare A 股日线构建验证样本：高开、突破、短期涨幅、回调确认。
- [ ] 比较直接追入 vs 等回调/止跌确认后的收益、回撤、胜率和尾部亏损。
- [ ] 输出规则验证报告到 `docs/research/backtest-reports/` 或 `docs/research/strategy-lab/rules/`。
- [ ] 把验证结论写成可以被美股报告引用的规则标签。

验收：

- 至少一条美股操作标签能引用 A 股大样本验证结论，而不是纯主观判断。
- 报告必须写清 A 股验证不能直接证明美股单票收益。

### Phase 4: 建立回测报告索引

- [ ] 新增 `docs/research/backtest-reports/README.md`。
- [ ] 索引现有 `my_quant/strategy_research/results/` 和 `web_report/` 中的重要产物。
- [ ] 为每个重要 run 记录：run id、策略、窗口、参数、关键指标、结论、报告路径。
- [ ] 区分 `阶段通过`、`观察`、`淘汰`。
- [ ] 标出哪些报告能服务美股操作纪律，哪些只是历史 A 股实验。

验收：

- 后续 Agent 不需要翻目录，就能找到某条策略或规则的验证证据。

### Phase 5: 把验证结论反哺美股持仓报告

- [ ] 在美股操作报告中新增“规则证据”区。
- [ ] 每个持仓动作标签必须引用至少一个证据来源：美股本地趋势/基本面证据，或 A 股规则验证卡片。
- [ ] 增加同因子集中度检查，例如 AI 硬件、存储、互联、供电、空间/国防。
- [ ] 增加杠杆敞口折算，例如 2x 单股 ETF 按等效敞口显示。
- [ ] 增加催化/财报前风险提醒。

验收：

- 报告能直接回答：当前持仓哪些继续拿、哪些不追、哪些等止跌、哪些要降风险。
- 结论仍然保持研究辅助，不自动下单。

### Phase 6: 可选数据库扩展

只有当前五个阶段跑顺后才考虑。

- [ ] 先写数据库备份脚本，支持 Windows PowerShell。
- [ ] 设计跨市场资产表，不复用 A 股专用字段名强行装美股。
- [ ] 新增非破坏性 schema 初始化脚本。
- [ ] 验证旧 A 股接口仍可用。
- [ ] 再考虑是否把美股快照、观察池、报告索引入库。

验收：

- 不删除旧表。
- 不删除 volume。
- 不破坏现有 A 股数据同步和回测能力。

## 优先级排序

近期优先级：

1. 根目录文档和四区边界。
2. `my_quant/us_research/` 文件化美股操作报告。
3. `docs/research/strategy-lab/` 规则卡片。
4. “不追高”A 股验证。
5. 回测报告索引。

暂缓：

- React 前端大改。
- Docker 数据库跨市场入库。
- Alembic 等迁移框架。
- 自动化调度。
- 真实券商/账户连接。

## 红线

- [ ] 不提交真实 `.env`、token、密码。
- [ ] 不提交真实持仓和成交明细，除非用户明确要求并已脱敏。
- [ ] 不执行 `docker compose down -v`。
- [ ] 不把 A 股验证写成美股单票收益保证。
- [ ] 不把研究信号写成自动买卖指令。
- [ ] 不为了追求漂亮回测取消成本、滑点、涨跌停、停牌或数据新鲜度检查。

## 第一条推荐闭环

建议第一个真正跑通的闭环是：

```text
美股问题：AI 主线票涨太快，要不要追？
  -> 策略规则：不追高，等回调或止跌确认
  -> A 股验证：高开/突破后直接买 vs 回调确认后买
  -> 回测报告：收益、回撤、胜率、尾部亏损
  -> 美股应用：SNXX / MVLL / CRDU 等高 beta 或杠杆仓位标记为“只等回调”或“止跌后小加”
```

这条闭环足够小，也最贴近当前持仓风险。
