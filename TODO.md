# PostgreSQL 量化数据底座 TODO

本 TODO 规划“哪些数据需要补充进当前 PostgreSQL”。仓库允许使用统一协议做离线研究，但不恢复旧策略链路，不提供交易信号、真实账户或自动交易。

## 原则

- Tushare 作为长期、可回放、可校验的主数据源。
- `a-stock-data` 只作为实时题材、板块、研报和情绪类补充源，不直接替代 Tushare 行情主库。
- 每张新增表都必须有自然键或唯一约束，支持重复同步时幂等 upsert。
- 前端只展示数据覆盖和只读数据，不输出买卖评级、交易建议或收益承诺。

## P0 必补数据（代码与远端 sandbox 已完成）

| 任务 | 任务目标 | 修改范围 | 验收条件 |
| --- | --- | --- | --- |
| 交易日历 | 建立所有窗口计算和同步任务的交易日基准。 | 新增 `trade_calendars` 表；新增 Tushare `trade_cal` 同步；DB overview 展示最新交易日。 | 可查询指定日期是否开市；可返回最近 N 个交易日；重复同步不产生重复行。 |
| 复权因子 | 让长期收益率、均线、回撤、突破计算不被除权除息污染。 | 新增 `stock_adjust_factors` 表；同步 Tushare `adj_factor`；保留原始未复权日线不改口径。 | 任意个股可查日期范围内复权因子；与 `stock_daily_bars` 按 `ts_code + trade_date` 可关联；重复同步幂等。 |
| 指数基础信息 | 建立宽基、行业、主题指数的统一资产目录。 | 新增 `indices` 表；同步 Tushare `index_basic`，优先 CSI、SSE、SZSE、SW。 | 能查到沪深300、中证500、中证1000、创业板指、上证指数，以及半导体相关指数如 `931743.CSI`、`931793.CSI`、`801081.SI`。 |
| 指数日线 | 给策略评估、市场状态和主题强弱提供正式基准。 | 新增 `index_daily_bars` 表；同步 Tushare `index_daily`；先覆盖核心宽基和半导体主题指数白名单。 | 每个白名单指数有日期覆盖、行数和最新交易日；可用于和个股/ETF同日期对齐。 |
| ETF 基础信息 | 建立可交易 ETF 代理资产目录，尤其行业/主题 ETF。 | 新增 `funds` 表；同步 Tushare `fund_basic(market='E')`；标记 ETF、LOF 等类型。 | 可查到半导体/芯片 ETF，如 `512480.SH`、`512760.SH`、`159995.SZ`、`588200.SH`。 |
| ETF 日线 | 用可交易 ETF 作为行业代理和宽基替代。 | 新增 `fund_daily_bars` 表；同步 Tushare `fund_daily`；先覆盖宽基 ETF 和半导体 ETF 白名单。 | ETF 日线可按 `ts_code + trade_date` 去重；日期覆盖和最新价可在 DB overview 或 API 中验证。 |
| 申万行业分类 | 建立行业层级和个股行业归属的正式口径。 | 新增 `industry_classifications` 和 `industry_members` 表；同步 `index_classify` 与 `index_member_all`。 | 可查 `801081.SI 半导体` 及其 L3 子行业；成分股有 `in_date/out_date/is_new`，可按日期还原行业池。 |

## P1 研究可信性数据（本分支代码和内存库合同已完成，真实 PG 未迁移）

| 任务 | 任务目标 | 修改范围 | 验收条件 |
| --- | --- | --- | --- |
| 历史上市状态 | 消除只使用当前上市股票造成的幸存者偏差。 | 新增 `stock_listings`；同步 `stock_basic` 的 L/D/P/G 状态、上市日和退市日。 | 可按历史日期还原当时已上市且未退市股票池；重复同步幂等。 |
| 停复牌事件 | 过滤“有信号但买不到/卖不掉”的样本。 | 新增 `stock_suspend_events`；同步 Tushare `suspend_d`。 | 可查询指定日期停牌/复牌事件；无行情或停牌样本不会被静默当作零收益。 |
| 涨跌停价格 | 支撑 A 股特有的涨跌停、封板、不可成交判断。 | 新增 `stock_limit_prices`；同步 Tushare `stk_limit`。 | 指定股票日期可查涨停价/跌停价；可识别开盘无法成交的涨跌停样本。 |
| ETF 复权因子 | 避免 ETF 分红和份额调整污染长期收益。 | 新增 `fund_adjust_factors`；同步 Tushare `fund_adj`。 | ETF 研究 readiness 在复权因子缺失时 blocked；重复同步幂等。 |
| 指数成分权重 | 解释指数涨跌贡献，支持成分池基准。 | 新增 `index_weights` 表；优先同步 Tushare `index_weight` 可用范围。 | 对沪深300等宽基可查成分权重；若半导体指数权重接口无数据，任务标记为部分完成并保留原因。 |
| 行业代理净值 | 即使官方行业日线缺权限，也能用成分股生成行业等权/市值加权代理。 | 新增派生表或物化视图 `industry_proxy_daily`；由 `industry_members` + `stock_daily_bars` + `stock_daily_basic` 计算。 | 半导体行业可生成等权净值和市值加权净值；每日样本数、缺失数、收益率可追溯。 |
| 资金流与融资融券 | 作为确认因子和风险观察，不做主买卖信号。 | 新增 `stock_money_flows`、`margin_trading_daily`；优先 Tushare，必要时用 `a-stock-data` 东财源做补充快照。 | 可查询个股近 N 日资金流/两融变化；来源字段明确，失败或缺失不影响主行情同步。 |
| 行业/题材快照 | 记录市场当日资金偏好的解释层。 | 新增 `sector_snapshots`；从 `a-stock-data` 东财行业板块、概念归属抓每日快照。 | 可查 `BK1036 半导体` 当日涨跌、涨跌家数、领涨股；快照带 source 和 collected_at。 |
| 研报元数据 | 记录行业研究热度和主题变化，不存 PDF 全文。 | 新增 `research_report_metadata`；先接东财行业研报和个股研报元数据。 | 可按行业码 `1036` 查询半导体研报标题、机构、发布日期、链接；不把 PDF 文件或全文塞入 PG。 |

## P2 暂缓数据

| 任务 | 任务目标 | 修改范围 | 验收条件 |
| --- | --- | --- | --- |
| 分钟线与 Tick | 仅在明确做日内策略时再引入。 | 暂不建主表；必要时先离线文件或独立实验表。 | 用户明确开启日内研究前不进入主 PG。 |
| ETF 期权 | 用于波动率、期权策略或市场情绪研究。 | 暂不入库；可先通过 `a-stock-data` 临时查询。 | 有明确期权研究需求后再定义合约、报价、希腊字母 schema。 |
| 龙虎榜席位与打板情绪 | 短线事件研究有用，但噪声和口径变化大。 | 暂不进入核心行情 schema；最多做每日快照表。 | 未开启短线事件研究前，不阻塞 P0/P1。 |
| 互动易、热榜、人气榜 | 用于舆情解释，不适合当核心量化主数据。 | 暂不入库；未来可做轻量文本元数据表。 | 只在需要事件归因或题材报告时接入。 |

## 推荐实施顺序

1. 交易日历、复权因子。
2. 指数基础信息、指数日线。
3. ETF 基础信息、ETF 日线。
4. 申万行业分类、行业成分。
5. 历史上市状态、ETF 复权、停复牌事件、涨跌停价格。
6. 行业代理净值、指数历史成分与权重。
7. 资金流、两融、行业/题材快照、研报元数据。

## 总体验收

- 新增数据表都有唯一约束和幂等 upsert。
- `GET /api/db/overview` 能展示新增数据覆盖情况。
- 后端 py_compile 和 backend tests 通过。
- 涉及前端展示时，前端仍是只读数据工作台，不出现交易信号、买卖评级、回测执行或真实账户入口。
- 远端部署时不执行 `docker compose down -v`，不删除 PostgreSQL volume。

## 真实 PG 迁移门槛

全部 P0/P1 任务在本地或临时验证库通过后，再迁移到真实 PostgreSQL 数据库。迁移前必须满足：

- 已有真实 PG 完整备份，并确认可回滚。
- schema 迁移只做前向新增或兼容变更，不删除既有表、字段、索引或 volume。
- 同步脚本在临时库完成全量 dry-run，并验证行数、日期覆盖、唯一约束和重复同步幂等。
- 真实 PG 迁移后重新运行覆盖查询、API health、DB overview 和关键样本查询。
- 迁移结果写入 `操作日志.md`，包含迁移时间、变更表、同步范围、验证命令和残留风险。
