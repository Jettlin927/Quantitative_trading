# 新服务器通过并行验收接替生产

> 历史决策：双服务器迁移与回滚观察阶段已结束。当前生产拓扑由
> [ADR 0008：单一生产服务器与旧服务器退役](0008-single-production-server-and-retired-legacy-host.md)
> 定义。

新服务器将承载同一套 API、前端、PostgreSQL、数据 Worker、研究 Worker、编排器和研究工件，旧服务器只作为迁移期回滚源。迁移采用暂停写入后的逻辑备份恢复、工件同步和多层读回校验；旧数据、volume 和备份在稳定观察期结束后仍须人工确认才能删除。
