# 领域文档

本仓库采用 single-context 领域文档布局。

## 工作前读取

- 根目录 `CONTEXT.md`。
- 与工作范围有关的 `docs/adr/` 决策记录。

文件尚不存在时可以继续；在 Wayfinder、领域建模或产品讨论真正解决术语和重要决策时再创建，避免预先编造内容。

## 约定

- Issue、规格、测试和代码统一使用 `CONTEXT.md` 定义的术语。
- 新概念尚未定义时，应先判断是语言漂移还是领域缺口。
- 新方案与既有 ADR 冲突时必须明确指出，不能静默覆盖。
- 系统级、难以逆转的决定写入 `docs/adr/`。
