# 尾盘分钟入场价数据源探测

- Run: `002-tail-active-minute-mootdx-latest-base-paged-001`
- 覆盖: `2026-05-25` 至 `2026-06-04`
- 目标时间: `14:30:00`
- 候选口径: `base`
- 分钟源: `mootdx`
- 候选日期: `15`，选中候选: `15`，分钟匹配: `15`
- 覆盖率: `100.00%`，请求错误: `0`
- 数据源状态: `insufficient_coverage`，可晋级回测: `False`
- 状态原因: 分钟匹配数或覆盖率不足，不能进入全量分钟回测。

## 收益对比

- 分钟入场至次日收盘均值: `-3.21%`，中位数: `-2.89%`
- 日线收盘入场至次日收盘均值: `-3.67%`，中位数: `-3.73%`
- 日线收盘相对 14:30 入场价均值差: `0.46%`，中位数: `0.78%`

## 口径说明

- 候选日期来自本地日线和 daily_basic，候选口径为 `base`。
- 分钟数据源可选 `tushare.stk_mins(freq=1min)`、`eastmoney-recent` 或 `mootdx`；只作为小批量探测源，覆盖率必须写入证据。
- 本脚本不写数据库，只把候选和分钟匹配结果写入 run 目录，避免污染长期数据口径。
