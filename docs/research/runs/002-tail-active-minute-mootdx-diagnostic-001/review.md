# 尾盘分钟入场价数据源探测

- Run: `002-tail-active-minute-mootdx-diagnostic-001`
- 覆盖: `2023-01-01` 至 `2026-06-04`
- 目标时间: `14:30:00`
- 候选口径: `base`
- 分钟源: `mootdx`
- 候选日期: `3753`，选中候选: `5`，分钟匹配: `0`
- 覆盖率: `0.00%`，请求错误: `1`

## 收益对比

- 分钟入场至次日收盘均值: `--`，中位数: `--`
- 日线收盘入场至次日收盘均值: `--`，中位数: `--`
- 日线收盘相对 14:30 入场价均值差: `--`，中位数: `--`

## 口径说明

- 候选日期来自本地日线和 daily_basic，候选口径为 `base`。
- 分钟数据源可选 `tushare.stk_mins(freq=1min)`、`eastmoney-recent` 或 `mootdx`；只作为小批量探测源，覆盖率必须写入证据。
- 本脚本不写数据库，只把候选和分钟匹配结果写入 run 目录，避免污染长期数据口径。
