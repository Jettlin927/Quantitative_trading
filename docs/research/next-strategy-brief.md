# 下一轮组合策略输入

当前阶段：`002-candidate-repair-30`，仍为 `active`。

当前完整三年最高观察：`002-repair-volume-inefficiency-crowding-penalty-003`。

当前已滚动验证最好观察：`002-repair-indicator-confluence-moneyflow-crowding-penalty-001`，滚动 `5/7`，失败窗口仍是 `Y1/R18-1`。

结论：强观察，未通过阶段。

关键数字：

- 完整三年总收益：`113.98%`
- 年化收益：`28.88%`
- 最大回撤：`-3.57%`
- 盈亏比：`2.68:1`
- Profit factor：`2.90:1`
- 胜率：`52.03%`
- 完成交易数：`123`
- 成交标的后 10 亏损/回撤审计：通过
- 成交标的后 10 盈亏比证据：未通过
- 成交标的尾部最差收益：`-8.19%`
- 成交标的尾部资本影响合计：`-7.72%`

有效因子：

- `priorVolumeRatioBasic>=2.0` 且 `amountEfficiencyRsiRank<=0.2` 扣 `1.0` 分。
- 相对上一完整三年最高观察，替换净差：完整 `+3651.69`、`Y1 +2843.91`、`R18-1 +2799.75`、`Y3 +851.94`、`R18-4 +851.94`。
- 财务质量数据已对当前最高观察的 `123` 笔完成交易补齐 point-in-time 覆盖，但该证据仍属于已成交样本诊断，不是候选全集排序因子。

新增负证据：

- 同阈值扣 `1.5` 年化降至 `26.35%`，替换净差 `-8050.99`，主要损害 `R18-4`。
- `industryReturn20Rank>=0.95` 扣 `0.5` 年化 `26.51%`；虽然 `Y1 +5082.55`，但完整 `-7699.00`、`R18-4 -9484.60`。
- `stockSpecificBreakoutQuality=0.10` 年化 `24.92%`，完整替换净差 `-14215.28`。
- `bollSqueezeRank>=0.17` 且 `industryMoneyflowSumNetRank1Rank<=0.34` 扣 `0.5` 年化 `27.08%`，完整替换净差 `-7147.63`。
- 止盈下调到 `8%/16%` 年化 `18.61%`，盈亏比 `1.69:1`，尾部最差 `-19.31%`。
- `002-financial-quality-edge-001` 显示 `q_profit_yoy`、低负债和高流动性等传统直觉阈值在 `Y1/R18-1` 与 `R18-4` 方向不一致；不能把当前已成交样本的财务差异直接全局化。
- `002-run-preentry-path-volume-inefficiency-001` 显示当前最高观察的亏损样本普遍 RSI 三日跟随偏弱，但 `macdHistDelta1d`、`entryRangePct`、`amountRatio` 等高值条件早段偏坏、后段显著正贡献，不能从已成交样本均值直接反推全局扣分。

禁止重复：

- 不继续加大 `volumeInefficiencyCrowding` 强度或放宽阈值。
- 不把行业 20 日涨幅过热、无行业资金支持的 BOLL 收口、或 `stockSpecificBreakoutQuality` 简单全局化。
- 不把当前组合因子的止盈改成 `8%/16%` 或 `12%/24%`。
- 不在候选全集财务覆盖不足时，把定向同步后的成交标的财务字段直接加进排序回测。
- 不把当前最高观察的已成交样本路径阈值直接变成排序加分/扣分；下一步路径研究必须上升到候选全集截面，而不是只看完成交易赢家/输家。
- 不把当前结果写成阶段通过；完整年化仍低于 `30%`，且未做 7 窗口验收。

下一轮建议：

- 以 `002-repair-volume-inefficiency-crowding-penalty-003` 为完整窗口观察基准。
- 暂停同一 OHLCV/排序扣分里的阈值小搜。若继续财务因子，先决定是否扩大持久化 `fina_indicator` 同步到候选全集；若不扩大数据覆盖，则优先做公告事件后的可交易风险标签或入场前多日路径的系统建模。
- 若继续做技术/量价，只做候选全集级别诊断：比较当日可买候选、实际成交、被持仓/周频限制挤掉的候选在后续 `1/3/5/10` 日路径上的差异；不要再用完成交易样本均值直接新增扣分。
